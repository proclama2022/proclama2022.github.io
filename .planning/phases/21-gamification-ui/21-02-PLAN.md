---
phase: 21-gamification-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [components/Gamification/WeeklyStreakCalendar.tsx, app/gamification.tsx, types/gamification.ts]
autonomous: true
requirements: [GMUI-05]
user_setup: []
must_haves:
  truths:
    - "User sees weekly streak calendar in gamification hub"
    - "User sees completed days as filled circles"
    - "User sees current day with highlight border"
    - "User sees future days as empty circles"
    - "User sees freeze days with snowflake icon"
    - "User sees streak count and freeze remaining below calendar"
  artifacts:
    - path: "components/Gamification/WeeklyStreakCalendar.tsx"
      provides: "Weekly streak calendar visualization"
    - path: "app/gamification.tsx"
      provides: "Integration of calendar in Badge tab"
  key_links:
    - from: "components/Gamification/WeeklyStreakCalendar.tsx"
      to: "app/gamification.tsx"
      via: "Import and render in Badge tab content"
      pattern: "WeeklyStreakCalendar.*streak.*freezeRemaining"
---

<objective>
Create WeeklyStreakCalendar component showing 7-day streak visualization with completed days, current day highlight, freeze indicator, and streak/freeze stats.

Purpose: Visualize user's weekly streak progress in the gamification hub
Output: WeeklyStreakCalendar component + integration in Badge tab
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-gamification-ui/21-CONTEXT.md

<interfaces>
From types/gamification.ts:
```typescript
export interface UserProgress {
  watering_streak: number;
  streak_freeze_remaining: number;
  // ... other fields
}
```

From 21-CONTEXT.md (DayStatus interface):
```typescript
interface DayStatus {
  day: 'L' | 'M' | 'M' | 'G' | 'V' | 'S' | 'D';
  status: 'completed' | 'current' | 'future' | 'freeze';
}
```

From components/Gamification/GamificationStats.tsx:
- Streak freeze indicator pattern with emoji styling
- freezeIndicator style: fontSize 14, fontWeight 500
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeeklyStreakCalendar component</name>
  <files>components/Gamification/WeeklyStreakCalendar.tsx</files>
  <action>
    Create a new component that displays a 7-day streak calendar per CONTEXT.md spec.

    Props interface:
    ```typescript
    interface WeeklyStreakCalendarProps {
      streak: number;
      freezeRemaining: number;
      weekData?: DayStatus[];  // Optional - compute if not provided
    }
    ```

    Layout structure per CONTEXT.md:
    ```
       L   M   M   G   V   S   D
      (●) (●) (●) [●] ○   ❄️  ○

      🔥 7 giorni   ❄️(1)
    ```

    Implementation details:
    1. **Day labels row**: L, M, M, G, V, S, D (Italian weekday initials)
       - Use i18n keys: `gamification.weekdays.mon`, etc.
       - Text style: fontSize 12, color textSecondary

    2. **Circles row**: 7 circles horizontally
       - `completed`: filled circle (brand color fill, 24px diameter)
       - `current`: highlighted border (2px brand + 2px white border, empty inside if not completed)
       - `future`: outlined circle (gray border, empty inside)
       - `freeze`: snowflake emoji instead of circle, color #81D4FA

    3. **Stats row below**: Streak count + freeze remaining
       - `🔥 {streak} giorni` - use i18n `gamification.streak.days`
       - `❄️({freezeRemaining})` - only if freezeRemaining > 0

    Default weekData computation (if not provided):
    - Calculate current day of week (0=Sunday, map to Italian order L-M-M-G-V-S-D)
    - Mark past days with watering as 'completed' based on streak count
    - Mark current day as 'current'
    - Mark future days as 'future'
    - For simplicity, use a heuristic: if streak > 0 and day < currentDayIndex, mark as 'completed'

    Styles:
    - container: marginVertical 16, paddingHorizontal 16
    - dayRow: flexDirection row, justifyContent space-around, marginBottom 8
    - dayColumn: alignItems center, width for each day
    - circle: width 24, height 24, borderRadius 12, borderWidth 2
    - dayLabel: fontSize 12, marginBottom 4
    - statsRow: flexDirection row, justifyContent center, gap 16, marginTop 12
    - streakText: fontSize 16, fontWeight 600
    - freezeText: fontSize 14

    Use:
    - useColorScheme() + Colors for theming
    - useTranslation() for i18n
  </action>
  <verify>
    <automated>grep -n "WeeklyStreakCalendar" components/Gamification/WeeklyStreakCalendar.tsx | head -3</automated>
  </verify>
  <done>
    WeeklyStreakCalendar component renders 7-day calendar with correct day states
  </done>
</task>

<task type="auto">
  <name>Task 2: Add DayStatus type and integrate calendar in gamification hub</name>
  <files>types/gamification.ts, app/gamification.tsx</files>
  <action>
    1. Add DayStatus interface to types/gamification.ts:
    ```typescript
    export type DayStatusValue = 'completed' | 'current' | 'future' | 'freeze';

    export interface DayStatus {
      day: string;  // Localized day initial
      status: DayStatusValue;
    }
    ```

    2. In app/gamification.tsx, add WeeklyStreakCalendar to the Badge tab content:

    Find the renderBadgesContent() function and add after the BadgeGrid:
    ```tsx
    {/* Weekly Streak Calendar */}
    {summary && (
      <WeeklyStreakCalendar
        streak={summary.progress.watering_streak}
        freezeRemaining={summary.progress.streak_freeze_remaining}
      />
    )}
    ```

    3. Import the component:
    ```tsx
    import { WeeklyStreakCalendar } from '@/components/Gamification/WeeklyStreakCalendar';
    ```

    Position: After BadgeGrid, before the recent activity section (if any).
    Add 16px margin separation between BadgeGrid and calendar.
  </action>
  <verify>
    <automated>grep -n "WeeklyStreakCalendar" app/gamification.tsx | head -3</automated>
  </verify>
  <done>
    Gamification hub Badge tab shows weekly streak calendar with current streak and freeze count
  </done>
</task>

</tasks>

<verification>
- WeeklyStreakCalendar component exists with proper typing
- Component shows 7 day circles with correct states
- Stats row shows streak count and freeze remaining
- Gamification hub renders calendar in Badge tab
</verification>

<success_criteria>
- GMUI-05: User sees weekly streak calendar in gamification hub
- Calendar correctly shows completed days, current day, and freeze indicator
- Streak count and freeze remaining display below calendar
</success_criteria>

<output>
After completion, create `.planning/phases/21-gamification-ui/21-02-SUMMARY.md`
</output>
