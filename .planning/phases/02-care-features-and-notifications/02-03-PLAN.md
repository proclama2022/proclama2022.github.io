---
phase: 02-care-features-and-notifications
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/plant/[id].tsx
  - app/(tabs)/settings.tsx
  - services/notificationService.ts
autonomous: true
requirements:
  - WATER-01
  - WATER-03
  - WATER-06

must_haves:
  truths:
    - "Mark Watered button visible above care info on plant detail screen"
    - "Watering History, Compliance Bar, and Streak Badge sections render below care info"
    - "Settings screen has notification permission toggle and time picker (default 08:00)"
    - "Daily digest notification fires at 08:00 listing all due plants"
    - "Notification persists if app uninstalled and reinstalled (AsyncStorage)"
  artifacts:
    - path: "app/plant/[id].tsx"
      provides: "Plant detail screen with watering UI"
      contains: "MarkWateredButton", "WateringHistory", "ComplianceBar"
    - path: "app/(tabs)/settings.tsx"
      provides: "Settings screen with notification controls"
      contains: "notification permission", "notification time"
    - path: "services/notificationService.ts"
      provides: "Daily digest scheduling function"
      exports: ["scheduleDailyDigest"]
  key_links:
    - from: "app/plant/[id].tsx"
      to: "components/Detail/MarkWateredButton"
      via: "Import and render component"
      pattern: "<MarkWateredButton\\s+plant=\\{plant\\}\\s+/>"
    - from: "app/plant/[id].tsx"
      to: "stores/plantsStore.removePlant"
      via: "Update removePlant call to await async version"
      pattern: "await\\s+removePlant\\(plant\\.id\\)"
    - from: "app/(tabs)/settings.tsx"
      to: "services/notificationService.scheduleDailyDigest"
      via: "Call after notification time changes"
      pattern: "scheduleDailyDigest\\(plants\\)"
</context>

---

<objective>
Integrate watering components into plant detail screen and add notification settings to Settings screen. Implement the 08:00 daily digest notification that lists all plants due for watering.

Purpose: This completes the user-facing watering features — users can now mark plants watered, see their history, track compliance, and receive daily reminders.

Output: Updated plant detail screen with watering UI, Settings screen with notification controls, daily digest notification service.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-care-features-and-notifications/02-CONTEXT.md
@.planning/phases/02-care-features-and-notifications/02-RESEARCH.md

@types/index.ts
@stores/plantsStore.ts
@stores/settingsStore.ts
@services/wateringService.ts
@services/notificationService.ts
@services/careDB.ts
@i18n/en.ts
@i18n/it.ts

@app/plant/[id].tsx (existing plant detail screen)
@app/(tabs)/settings.tsx (existing settings screen)
</context>

<tasks>

<task type="auto">
  <name>Add watering components to plant detail screen</name>
  <files>app/plant/[id].tsx</files>
  <action>
Update app/plant/[id].tsx to integrate Phase 2 watering UI:

1. Import components:
   - import { MarkWateredButton } from '@/components/Detail/MarkWateredButton'
   - import { WateringHistory } from '@/components/Detail/WateringHistory'
   - import { ComplianceBar } from '@/components/Detail/ComplianceBar'

2. Import services/stores:
   - import { removePlant } from '@/stores/plantsStore' (already imported)
   - import { scheduleDailyDigest } from '@/services/notificationService'

3. Update removePlant call:
   - Change from `removePlant(plant.id)` to `await removePlant(plant.id)`
   - Make delete handler async

4. Add MarkWateredButton above CareInfo:
   - Position: immediately before CareInfo component (user decision: "above care info")
   - Pass plant prop: `<MarkWateredButton plant={plant} />`

5. Add watering sections below CareInfo:
   - ComplianceBar: `<ComplianceBar plant={plant} />`
   - WateringHistory: `<WateringHistory plant={plant} />`
   - Order: Compliance first (immediate progress feedback), then History (detailed view)

6. Scroll structure:
   - All components should be in existing ScrollView
   - Ensure proper spacing (16px padding between sections)

7. Trigger daily digest on plant save:
   - After marking plant as watered, call scheduleDailyDigest(allPlants)
   - Use usePlantsStore.getState().plants to get all plants

Follow existing layout patterns: section headers, consistent padding, same text styles as CareInfo.
  </action>
  <verify>grep -E "MarkWateredButton|WateringHistory|ComplianceBar" app/plant/[id].tsx returns matches</verify>
  <done>Plant detail screen shows Mark Watered button above care info, Compliance and History sections below care info, all components render without layout issues</done>
</task>

<task type="auto">
  <name>Implement daily digest notification service</name>
  <files>services/notificationService.ts</files>
  <action>
Add to services/notificationService.ts (extend existing service):

scheduleDailyDigest(plants: SavedPlant[], time: string = "08:00"): Promise&lt;void&gt;
   - Parse time string to hour/minute (format: "HH:mm")
   - Cancel existing daily digest notification (identifier: "daily-watering-digest")
   - Filter plants where nextWateringDate === today (ISO date comparison)
   - If no due plants, return early (don't show empty notification)
   - Build plant names list: [nickname || commonName || species].join(', ')
   - Truncate list if >3 plants: "Monstera, Ficus, and 2 more..."
   - Schedule notification:
     * Title: "Watering Reminder" (or i18n equivalent)
     * Body: "{plantNames} need water today"
     * Identifier: "daily-watering-digest" (fixed, for cancellation)
     * Category: "watering" (for future action buttons)
     * Trigger: platform-specific daily at hour/minute

Platform-specific trigger logic:
- Android: { type: 'daily', hour, minute }
- iOS: { type: 'calendar', repeats: true, dateComponents: { hour, minute } }

Call this function:
- At app startup (if notifications enabled)
- After any plant is marked watered
- When notification time changes in settings

Per RESEARCH.md Pattern 5: Cancel and reschedule to ensure digest is always up-to-date.

Export the function for use in settings screen.
  </action>
  <verify>grep -E "scheduleDailyDigest" services/notificationService.ts returns match</verify>
  <done>scheduleDailyDigest function exported, cancels existing digest before scheduling, handles empty plant list, formats plant names correctly</done>
</task>

<task type="auto">
  <name>Add notification settings to Settings screen</name>
  <files>app/(tabs)/settings.tsx</files>
  <action>
Update app/(tabs)/settings.tsx to add notification controls:

1. Import settings store and notification service:
   - import { useSettingsStore } from '@/stores/settingsStore'
   - import * as Notifications from '@/services/notificationService'

2. Add notification permission section:
   - Section header: "Notifications" (use i18n if available, else hardcoded)
   - Permission status indicator: "Enabled" / "Disabled" / "Undetermined"
   - Enable button: calls requestPermission() on first press
   - Show settings screen guidance if denied ("Enable in system settings")

3. Add notification time picker:
   - Label: "Notification Time" (t('watering.notificationTime'))
   - Display current time (default 08:00)
   - On press, show time picker modal (use expo-document-picker or native iOS/Android picker)
   - On change: update settingsStore.setNotificationTime(newTime)
   - Trigger scheduleDailyDigest with new time

4. Toggle notification enable/disable:
   - Switch component for on/off
   - On enable: request permission, schedule daily digest
   - On disable: cancel all notifications via cancelAllPlantNotifications

5. Layout:
   - Place notification section above PlantNet attribution (bottom of screen)
   - Use same card/row style as existing settings items
   - Consistent spacing and typography

Manual opt-in only (user decision): DO NOT auto-prompt on first plant save. User must explicitly enable in Settings.

Per CONTEXT.md: Notification time configurable in Settings, default 08:00.
  </action>
  <verify>grep -E "notification|requestPermission|scheduleDailyDigest|setNotificationTime" app/(tabs)/settings.tsx returns matches</verify>
  <done>Settings screen has notification permission toggle, time picker, enable/disable switch, manual opt-in only (no auto-prompt)</done>
</task>

<task type="auto">
  <name>Initialize notifications at app startup</name>
  <files>app/_layout.tsx or app/(tabs)/_layout.tsx</files>
  <action>
Add notification initialization to root layout (app/_layout.tsx or tab layout):

In a useEffect hook on mount:
1. Check notification permission status via checkPermission()
2. If granted and notificationEnabled in settingsStore:
   - Call scheduleDailyDigest(plants, notificationTime)
3. This ensures daily digest is scheduled on app launch

Location: Add to existing useEffect in layout file (after font loading, before splash hide).

Use useSettingsStore to get notificationEnabled and notificationTime.
Use usePlantsStore to get plants array.

Per WATER-06: Notification IDs persisted in AsyncStorage via plantsStore persist middleware, so notifications are restored on app reinstall (IDs persist, then rescheduled at startup).

DO NOT request permissions automatically at startup — manual opt-in only per user decision.
  </action>
  <verify>grep -E "scheduleDailyDigest|checkPermission|useEffect" app/_layout.tsx returns matches in same block</verify>
  <done>Notifications initialized at app startup, daily digest scheduled if enabled, no automatic permission requests</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Plant detail screen renders all watering components without layout breaks
2. Mark Watered button is prominent and positioned above care info
3. Settings screen shows notification controls
4. Daily digest notification schedules correctly at 08:00 (or user-configured time)
5. Changing notification time reschedules digest
6. Disabling notifications cancels all scheduled notifications
7. Notification IDs persist across app restarts (WATER-06 compliance)
8. Manual opt-in only — no auto-prompts
</verification>

<success_criteria>
1. User can mark plant as watered from detail screen
2. Watering history and compliance visible below care info
3. User can enable notifications from Settings (manual opt-in)
4. User can configure notification time (default 08:00)
5. Daily digest fires at configured time listing due plants
6. Deleting a plant cancels its notification
7. All notification state persists via AsyncStorage
</success_criteria>

<output>
After completion, create `.planning/phases/02-care-features-and-notifications/02-03-SUMMARY.md`
</output>
