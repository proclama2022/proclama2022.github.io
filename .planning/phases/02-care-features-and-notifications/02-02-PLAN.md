---
phase: 02-care-features-and-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - stores/plantsStore.ts
  - stores/settingsStore.ts
  - components/Detail/MarkWateredButton.tsx
  - components/Detail/WateringHistory.tsx
  - components/Detail/ComplianceBar.tsx
  - components/Toast.tsx
autonomous: true
requirements:
  - WATER-04
  - WATER-05

must_haves:
  truths:
    - "User can see 30-day watering history as calendar dots view on plant detail screen"
    - "Calendar shows green dots for watered days, red for missed, gray for future"
    - "Horizontal progress bar displays rolling 7-day compliance rate with percentage label"
    - "Streak badge appears when user has 7+ day watering streak"
    - "Tapping calendar day shows optional notes for that watering event"
  artifacts:
    - path: "components/Detail/WateringHistory.tsx"
      provides: "Calendar dots visualization for 30-day history"
      min_lines: 50
    - path: "components/Detail/ComplianceBar.tsx"
      provides: "Horizontal progress bar with compliance percentage"
      min_lines: 30
    - path: "components/Detail/MarkWateredButton.tsx"
      provides: "Mark Watered button with toast and undo"
      min_lines: 60
    - path: "components/Toast.tsx"
      provides: "Reusable toast component with undo action"
      min_lines: 40
  key_links:
    - from: "app/plant/[id].tsx"
      to: "components/Detail/WateringHistory"
      via: "Import and render component"
      pattern: "<WateringHistory\\s+plant=\\{plant\\}\\s+/>"
    - from: "components/Detail/WateringHistory"
      to: "react-native-calendars"
      via: "Calendar component import"
      pattern: "from\\s+['\"]react-native-calenders['\"]"
    - from: "components/Detail/WateringHistory"
      to: "services/wateringService.generateMarkedDates"
      via: "Generate marked dates for calendar"
      pattern: "generateMarkedDates\\(plant\\)"
    - from: "components/Detail/ComplianceBar"
      to: "react-native-progress"
      via: "Progress.Bar component"
      pattern: "Progress\\.Bar"
</context>

---

<objective>
Build the UI components for watering history visualization and compliance tracking. Users can see their last 30 days of watering as an intuitive calendar with color-coded dots, track their compliance rate with a progress bar, and celebrate streaks.

Purpose: Visual feedback creates the habit-forming loop — seeing green dots and streaks motivates consistent watering behavior.

Output: WateringHistory calendar component, ComplianceBar progress component, MarkWateredButton with toast undo, Toast component, stores extended with notification cleanup.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-care-features-and-notifications/02-CONTEXT.md
@.planning/phases/02-care-features-and-notifications/02-RESEARCH.md

@types/index.ts
@stores/plantsStore.ts
@services/careDB.ts
@i18n/en.ts
@i18n/it.ts

@components/Detail/CareInfo.tsx (reference for component structure)
</context>

<tasks>

<task type="auto">
  <name>Extend plants store with notification cleanup</name>
  <files>stores/plantsStore.ts</files>
  <action>
Modify stores/plantsStore.ts to support notification management:

1. Change removePlant signature from `(id: string) => void` to `(id: string) => Promise&lt;void&gt;`
2. Update removePlant implementation:
   - Before filtering plant from array, check if plant.scheduledNotificationId exists
   - If notificationId exists, import { cancelPlantNotification } from '@/services/notificationService'
   - Call await cancelPlantNotification(plant.scheduledNotificationId)
   - Then filter plant from array
3. Add cancelAllNotifications method: `() => Promise&lt;void&gt;`
   - Get all plants and extract scheduledNotificationIds (filter out undefined)
   - Import notificationService and call cancelAllPlantNotifications(ids)
4. Persist notificationTimePreference (for WATER-03 daily digest):
   - Add field to state interface: `notificationTimePreference: string`
   - Default to "08:00"
   - Add setNotificationTime method

Follow existing Zustand persist pattern from Phase 01 — use createJSONStorage(() => AsyncStorage).
  </action>
  <verify>grep -E "cancelPlantNotification|cancelAllNotifications|notificationTimePreference" stores/plantsStore.ts returns matches</verify>
  <done>removePlant is async and cancels notifications, cancelAllNotifications method exists, notificationTimePreference persisted</done>
</task>

<task type="auto">
  <name>Create settings store for notification preferences</name>
  <files>stores/settingsStore.ts</files>
  <action>
Create stores/settingsStore.ts (new file) with Zustand + persist:

Interface SettingsState:
- notificationEnabled: boolean (default false)
- notificationTime: string (default "08:00" — HH:mm format)
- notificationPermission: 'granted' | 'denied' | 'undetermined' (default 'undetermined')
- setNotificationEnabled: (enabled: boolean) => void
- setNotificationTime: (time: string) => void
- setNotificationPermission: (status: 'granted' | 'denied' | 'undetermined') => void

Use persist middleware with name 'plantid-settings-storage' and createJSONStorage(() => AsyncStorage).
This stores user's notification preferences across app restarts.

Follow same pattern as plantsStore from Phase 01:
```typescript
export const useSettingsStore = create<SettingsState>()(
  persist(
    (set) => ({
      notificationEnabled: false,
      notificationTime: "08:00",
      notificationPermission: 'undetermined',
      setNotificationEnabled: (enabled) => set({ notificationEnabled: enabled }),
      setNotificationTime: (time) => set({ notificationTime: time }),
      setNotificationPermission: (status) => set({ notificationPermission: status }),
    }),
    {
      name: 'plantid-settings-storage',
      storage: createJSONStorage(() => AsyncStorage),
    }
  )
);
```
  </action>
  <verify>grep -E "export.*useSettingsStore|notificationEnabled|notificationTime|notificationPermission" stores/settingsStore.ts returns matches</verify>
  <done>Settings store created with all notification preference fields, persisted via AsyncStorage</done>
</task>

<task type="auto">
  <name>Create Toast component with undo support</name>
  <files>components/Toast.tsx</files>
  <action>
Create components/Toast.tsx for undoable toasts per user decision:

Props interface:
- message: string
- type: 'success' | 'info'
- visible: boolean
- undoAction?: () => void
- onDismiss: () => void

Component behavior:
1. Render as animated View (fade in/out) at bottom of screen with padding
2. Show checkmark icon for success type
3. Display message text
4. If undoAction provided, show "Undo" button (TouchableOpacity) on right
5. Auto-dismiss after 5 seconds if no undo action pressed
6. Use Modal with transparent overlay for backdrop

Styling per user decision:
- Green background (#2e7d32) for success
- White text
- Border radius 8px
- Shadow for elevation
- 5s undo window

Use Expo_haptics for haptic feedback on show (NotificationFeedbackType.Success).
Follow Modal pattern from Phase 01 delete confirmation.

DO NOT use third-party toast libraries — build custom component per project conventions.
  </action>
  <verify>grep -E "export.*Toast|undoAction|onDismiss" components/Toast.tsx returns matches</verify>
  <done>Toast component renders with message, undo button, 5s auto-dismiss, haptic feedback on show</done>
</task>

<task type="auto">
  <name>Create MarkWateredButton component</name>
  <files>components/Detail/MarkWateredButton.tsx</files>
  <action>
Create components/Detail/MarkWateredButton.tsx implementing user decision flow:

Props: { plant: SavedPlant }

Component structure:
1. TouchableOpacity button positioned above CareInfo (prominent location per CONTEXT.md)
2. On press:
   - Call Haptics.notificationAsync(NotificationFeedbackType.Success)
   - Call markAsWatered(plant.id) from wateringService
   - Calculate next date via getNextWateringDate(plant.id)
   - Schedule notification via schedulePlantNotification(plant.id, plantName, nextDate)
   - Update plant with nextWateringDate and scheduledNotificationId
   - Show toast with "Marked as watered!" message and 5s undo window
3. Undo action:
   - Remove last water event from waterHistory
   - Cancel scheduled notification
   - Hide toast

Button styling:
- Green background (#2e7d32) to match care theme
- White text "Mark Watered" (use i18n t('watering.markWatered'))
- Full width, padding 16px, border radius 8px
- Icon: water drop (can use text emoji 💧 for simplicity, or Ionicons if already in project)

Toast integration:
- Use Toast component with message: t('watering.markedAsWatered')
- Pass undo handler that removes last water event
- Auto-hide after 5 seconds per user decision

Follow existing component patterns from CareInfo.tsx (use t() for all text, handle null/undefined gracefully).
  </action>
  <verify>grep -E "MarkWateredButton|markAsWatered|schedulePlantNotification|Marked as watered" components/Detail/MarkWateredButton.tsx returns matches</verify>
  <done>MarkWateredButton records watering, schedules notification, shows toast with undo, uses translations for all text</done>
</task>

<task type="auto">
  <name>Create WateringHistory calendar component</name>
  <files>components/Detail/WateringHistory.tsx</files>
  <action>
Create components/Detail/WateringHistory.tsx implementing calendar dots visualization per user decision:

Props: { plant: SavedPlant }

Component structure:
1. Section header "Watering History" (t('watering.wateringHistory'))
2. Calendar from react-native-calendars with props:
   - markingType="multi-dot"
   - markedDates={generateMarkedDates(plant)} from wateringService
   - theme: dotStyle { width: 8, height: 8, marginTop: 2 }
   - Hidden month arrows (lock to 30-day view)
3. Streak badge (conditional):
   - Calculate streak via getWateringStreak(plant.id)
   - If streak >= 7, show badge with "{streak}-day watering streak! 🔥"
   - Yellow background (#fff9c4), bold orange text (#f57f17)
4. Day press handler:
   - Find water event for selected day
   - If event.notes exists, show Alert.alert with notes
   - Use i18n for date formatting

Dot colors per CONTEXT.md:
- Green (#2e7d32): watered on schedule
- Red (#c62828): missed watering
- Gray (#e0e0e0): future/skipped

Calendar scope:
- Show last 30 days only (no navigation beyond)
- Current day highlighted with light green background

Follow CareInfo.tsx structure: section header → content component, use t() for all text.
  </action>
  <verify>grep -E "WateringHistory|Calendar|generateMarkedDates|getWateringStreak" components/Detail/WateringHistory.tsx returns matches</verify>
  <done>WateringHistory shows 30-day calendar with colored dots, displays streak badge for 7+ day streaks, shows notes on day press</done>
</task>

<task type="auto">
  <name>Create ComplianceBar progress component</name>
  <files>components/Detail/ComplianceBar.tsx</files>
  <action>
Create components/Detail/ComplianceBar.tsx implementing rolling 7-day compliance per user decision:

Props: { plant: SavedPlant }

Component structure:
1. Calculate compliance: getComplianceRate(plant.id, 7) from wateringService
2. Render Progress.Bar from react-native-progress:
   - progress: compliance.rate / 100
   - width: null (full width)
   - height: 12
   - color: "#2e7d32" (green)
   - unfilledColor: "#e0e0e0" (gray)
   - borderWidth: 0
   - borderRadius: 6
3. Show percentage label below bar: "{rate}% compliance this week" (use i18n with {{rate}} interpolation)
4. Optional: animate prop for smooth transitions when compliance updates

Positioning:
- Center-align content (alignItems: 'center')
- Add padding (16px top/bottom)
- Show below WateringHistory component on plant detail screen

Per CONTEXT.md: Only show positive progress — don't highlight missed/overdue days. The progress bar speaks for itself.

Follow existing component patterns: concise layout, use t() for text, handle missing care info gracefully (show 0% if no care data).
  </action>
  <verify>grep -E "ComplianceBar|Progress\\.Bar|getComplianceRate|compliance this week" components/Detail/ComplianceBar.tsx returns matches</verify>
  <done>ComplianceBar displays horizontal progress bar, shows percentage label, uses 7-day rolling window, handles missing care data</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. All components follow existing patterns from CareInfo.tsx
3. All UI text uses i18n translation keys
4. Toast undo removes last water event correctly
5. Calendar dots display correctly for all three states (green/red/gray)
6. Compliance bar shows accurate percentage
7. Streak badge appears only when streak >= 7
8. Plant deletion cancels scheduled notifications
</verification>

<success_criteria>
1. 30-day watering history visible as calendar dots on plant detail screen
2. Compliance rate displayed with horizontal progress bar and percentage
3. Streak badge highlights 7+ day watering streaks
4. Mark Watered button records watering and schedules next reminder
5. Toast with undo appears after marking watered
6. Plant deletion cleans up orphaned notifications
</success_criteria>

<output>
After completion, create `.planning/phases/02-care-features-and-notifications/02-02-SUMMARY.md`
</output>
