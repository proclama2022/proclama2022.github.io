---
phase: 09-care-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/calendar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Watering dot on calendar grid shows blue/teal (colors.tint), not green"
    - "Watering legend dot shows blue/teal (colors.tint), not green"
    - "Watering task row icon background is a soft tint-tinted color, not #e8f5e9 green"
    - "Watering task row icon color is colors.tint, not #2e7d32 green"
    - "All other calendar functionality (navigation, task completion, reminders) is unchanged"
  artifacts:
    - path: "app/calendar.tsx"
      provides: "Care calendar with theme-consistent watering color tokens"
      contains: "colors.tint"
  key_links:
    - from: "app/calendar.tsx"
      to: "useThemeColors"
      via: "colors.tint substitution"
      pattern: "colors\\.tint"
---

<objective>
Replace 4 hardcoded green (#2e7d32 / #e8f5e9) values in `app/calendar.tsx` with `colors.tint` and a tint-derived alpha background, so watering indicators are visually consistent with the rest of the app (statistics bar chart, filter chips, FAB).

Purpose: The calendar is already fully implemented. This is a single-file color token substitution — no logic changes.
Output: `app/calendar.tsx` with `colors.tint` used for all watering color indicators.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-care-calendar/09-CONTEXT.md

<interfaces>
<!-- Existing patterns in app/calendar.tsx — executor should match these exactly -->

From app/calendar.tsx (current state):
```typescript
// useThemeColors already imported and used
const colors = useThemeColors();

// Line 228 — calendar grid dot (REPLACE)
{hasWatering && <View style={[styles.dot, { backgroundColor: '#2e7d32' }]} />}

// Line 240 — legend dot (REPLACE)
<View style={[styles.legendDot, { backgroundColor: '#2e7d32' }]} />

// Line 260 — task icon background (REPLACE)
<View style={[styles.taskIcon, { backgroundColor: task.type === 'watering' ? '#e8f5e9' : '#fff3e0' }]}>

// Line 264 — task icon color (REPLACE)
color={task.type === 'watering' ? '#2e7d32' : '#f57c00'}

// Line 437 — static style default borderColor (REPLACE for consistency)
borderColor: '#2e7d32',
```

Replacement pattern:
- `'#2e7d32'` → `colors.tint`  (for direct color values)
- `'#e8f5e9'` → `colors.tint + '20'`  (20 = ~12% alpha, keeps soft pastel feel)
- Line 437 static style cannot use `colors.tint` directly (StyleSheet.create context). Leave it as-is — it is always overridden at runtime by the inline style on line 274: `{ backgroundColor: colors.chipActiveBg, borderColor: colors.tint }`. No change needed on line 437.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded green values with colors.tint in calendar.tsx</name>
  <files>app/calendar.tsx</files>
  <action>
    Make exactly 4 targeted replacements in `app/calendar.tsx`:

    1. **Line 228** — Calendar grid dot background:
       Change: `{ backgroundColor: '#2e7d32' }`
       To:     `{ backgroundColor: colors.tint }`

    2. **Line 240** — Legend dot background:
       Change: `{ backgroundColor: '#2e7d32' }`
       To:     `{ backgroundColor: colors.tint }`

    3. **Line 260** — Task icon background (watering branch only):
       Change: `task.type === 'watering' ? '#e8f5e9' : '#fff3e0'`
       To:     `task.type === 'watering' ? colors.tint + '20' : '#fff3e0'`

    4. **Line 264** — Task icon Ionicons color (watering branch only):
       Change: `task.type === 'watering' ? '#2e7d32' : '#f57c00'`
       To:     `task.type === 'watering' ? colors.tint : '#f57c00'`

    Do NOT touch:
    - Line 437 (`borderColor: '#2e7d32'` in StyleSheet.create) — already overridden at runtime, leave as-is
    - Reminder colors (#f57c00, #fff3e0) — intentionally unchanged per user decision
    - All other logic, layout, or styles

    NOTE: `colors.tint + '20'` produces a hex string like `#00897b20` — this is valid in React Native's inline style backgroundColor.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep calendar || echo "No TS errors in calendar.tsx"</automated>
  </verify>
  <done>
    - `app/calendar.tsx` contains zero occurrences of `#2e7d32` in inline styles (lines 228, 240, 264)
    - `app/calendar.tsx` contains zero occurrences of `#e8f5e9` (line 260)
    - TypeScript compilation reports no errors for calendar.tsx
    - All 4 changed locations use `colors.tint` (or `colors.tint + '20'`)
    - Reminder colors (#f57c00, #fff3e0) unchanged
  </done>
</task>

</tasks>

<verification>
After task completion:

1. Grep confirms zero hardcoded green in inline styles:
   `grep -n "#2e7d32\|#e8f5e9" app/calendar.tsx` — should return only line 437 (static style, intentionally kept)

2. Grep confirms colors.tint used in all 4 locations:
   `grep -n "colors.tint" app/calendar.tsx` — should show lines 228, 240, 260, 264 in addition to existing usages

3. TypeScript: `npx tsc --noEmit` — no new errors

4. Manual visual check (optional): run app, navigate to Calendar tab, verify watering dots are blue/teal not green
</verification>

<success_criteria>
Phase 9 complete when:
- Watering dots on calendar grid use `colors.tint` (blue/teal) — visible change from green
- Legend "Watering" dot uses `colors.tint`
- Watering task row icon and its background use tint-based colors
- Zero TypeScript errors introduced
- Reminder indicators (#f57c00 orange) unchanged
- All calendar interactions (navigate months, tap day, complete task) still functional
</success_criteria>

<output>
After completion, create `.planning/phases/09-care-calendar/09-01-SUMMARY.md`
</output>
