---
phase: 14-follow-system-engagement-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/supabase/posts.ts, lib/supabase/likes.ts, services/likeService.ts, stores/feedStore.ts]
autonomous: true
requirements: [FOLL-04]
user_setup: []

must_haves:
  truths:
    - "User can filter community feed to show only posts from followed users"
    - "Following filter respects existing follow relationships"
    - "Feed displays empty state when user follows no one"
  artifacts:
    - path: "lib/supabase/posts.ts"
      provides: "Supabase queries for posts including following filter"
      exports: ["getFollowingFeed", "getRecentFeed", "getPopularFeed"]
    - path: "lib/supabase/likes.ts"
      provides: "Supabase queries for likes operations"
      exports: ["getPostLikes", "getUserLikedPosts"]
    - path: "services/likeService.ts"
      provides: "Business logic for like operations"
      exports: ["getLikedPosts", "getPostLikesList"]
    - path: "stores/feedStore.ts"
      provides: "Zustand state for feed filtering and pagination"
      exports: ["useFeedStore"]
  key_links:
    - from: "stores/feedStore.ts"
      to: "lib/supabase/posts.ts"
      via: "getFollowingFeed call"
      pattern: "getFollowingFeed\\(userId"
    - from: "lib/supabase/posts.ts"
      to: "follows table"
      via: "INNER JOIN for filter"
      pattern: "INNER JOIN follows"
---

<objective>
Implement Following Feed Filter backend infrastructure

Purpose: Enable users to see posts only from people they follow, creating a personalized feed experience
Output: Supabase query layer and Zustand store for feed filtering
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From lib/supabase/profiles.ts:
```typescript
export interface Profile {
  id: string;
  display_name: string;
  bio: string | null;
  avatar_url: string | null;
  created_at: string;
  updated_at: string;
}

export const isFollowing = async (followerId: string, followingId: string): Promise<boolean>;
export const getFollowing = async (userId: string): Promise<Profile[]>;
```

From services/followService.ts:
```typescript
export interface FollowServiceResult {
  success: boolean;
  error?: string;
}

export const checkIsFollowing = async (followerId: string, followingId: string): Promise<boolean>;
```

From stores/profileStore.ts (pattern to follow):
```typescript
export const useProfileStore = create<ProfileState>((set, get) => ({
  currentProfile: null,
  isLoading: false,
  error: null,
  // Actions follow { success, data?, error? } pattern
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Supabase posts query layer</name>
  <files>lib/supabase/posts.ts</files>
  <behavior>
    - Test 1: getFollowingFeed returns posts from followed users only
    - Test 2: getFollowingFeed includes user's own posts (self-follow pattern)
    - Test 3: getFollowingFeed returns empty array when user follows no one
    - Test 4: getRecentFeed returns all public posts ordered by created_at DESC
    - Test 5: getPopularFeed returns posts ordered by like_count DESC (last 7 days)
  </behavior>
  <action>
Create lib/supabase/posts.ts with the following functions:

1. **PostWithAuthor interface** - Post data with nested author profile:
   - id, photo_url, plant_name, caption, like_count, comment_count, created_at
   - profiles: { id, display_name, avatar_url }

2. **getFollowingFeed(userId, limit=20, cursor?)** - Posts from followed users:
   - Use INNER JOIN with follows table on posts.user_id = follows.following_id
   - Include user's own posts via OR posts.user_id = userId
   - Filter is_public = true
   - Order by created_at DESC
   - Cursor-based pagination using created_at < cursor

3. **getRecentFeed(limit=20, cursor?)** - All public posts newest first:
   - Select from posts with is_public = true
   - Include profiles join for author data
   - Order by created_at DESC

4. **getPopularFeed(limit=20, cursor?)** - Top posts by likes (7 days):
   - Filter created_at >= now() - interval '7 days'
   - Order by like_count DESC, then created_at DESC

Follow existing patterns from lib/supabase/profiles.ts:
- Import getSupabaseClient from './client'
- Use try/catch with error handling
- Return structured data or throw ProfileError-style errors

Note: This assumes posts table exists from Phase 13. If posts table schema differs, adapt queries accordingly.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="posts.test" --passWithNoTests</automated>
  </verify>
  <done>
    - lib/supabase/posts.ts exports getFollowingFeed, getRecentFeed, getPopularFeed
    - Functions return PostWithAuthor[] with correct data structure
    - Following filter correctly joins with follows table
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Supabase likes query layer</name>
  <files>lib/supabase/likes.ts</files>
  <behavior>
    - Test 1: getPostLikes returns users who liked a post with profile data
    - Test 2: getUserLikedPosts returns posts the user has liked
    - Test 3: checkUserLikedPost returns boolean for like status
  </behavior>
  <action>
Create lib/supabase/likes.ts with the following functions:

1. **LikeWithProfile interface** - Like data with user profile:
   - user_id, created_at
   - profiles: { id, display_name, avatar_url }

2. **getPostLikes(postId, limit=50, offset=0)** - Users who liked a post:
   - Query post_likes table with profiles join
   - Order by created_at DESC (most recent likes first)
   - Use range() for offset pagination

3. **getUserLikedPosts(userId, limit=20, cursor?)** - Posts user has liked:
   - Query posts where id IN (select post_id from post_likes where user_id = userId)
   - Include profiles join for post authors
   - Order by post_likes.created_at DESC (recently liked)
   - Cursor-based on post_likes.created_at

4. **checkUserLikedPost(userId, postId)** - Check if user liked a post:
   - Query post_likes for existence
   - Return boolean

Follow existing patterns from lib/supabase/profiles.ts for error handling.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="likes.test" --passWithNoTests</automated>
  </verify>
  <done>
    - lib/supabase/likes.ts exports getPostLikes, getUserLikedPosts, checkUserLikedPost
    - Functions return structured data with profile joins
    - Ready for use by likeService and LikesList component
  </done>
</task>

<task type="auto">
  <name>Task 3: Create like service business layer</name>
  <files>services/likeService.ts</files>
  <action>
Create services/likeService.ts as business logic layer (pattern from services/followService.ts):

1. **LikeServiceResult interface** - Standard { success, data?, error? } pattern

2. **getPostLikesList(postId, limit?, offset?)** - Get users who liked a post:
   - Call getPostLikes from lib/supabase/likes
   - Wrap in try/catch with user-friendly errors
   - Return { success: true, data } or { success: false, error }

3. **getLikedPosts(userId, limit?, cursor?)** - Get posts user has liked:
   - Call getUserLikedPosts from lib/supabase/likes
   - Return structured result

4. **checkLiked(userId, postId)** - Check like status:
   - Call checkUserLikedPost
   - Return boolean

Error messages (follow followService.ts pattern):
- LIKES_FETCH_FAILED: 'Failed to load likes'
- LIKED_POSTS_FAILED: 'Failed to load liked posts'
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="likeService.test" --passWithNoTests</automated>
  </verify>
  <done>
    - services/likeService.ts exports getPostLikesList, getLikedPosts, checkLiked
    - All functions return structured LikeServiceResult
    - Error handling matches existing service patterns
  </done>
</task>

<task type="auto">
  <name>Task 4: Create feed Zustand store</name>
  <files>stores/feedStore.ts</files>
  <action>
Create stores/feedStore.ts following the pattern from stores/profileStore.ts:

1. **FeedFilter type** - 'recent' | 'popular' | 'following'

2. **Post interface** - Match PostWithAuthor from lib/supabase/posts.ts

3. **FeedState interface**:
   - posts: Post[]
   - filter: FeedFilter (default: 'recent')
   - isLoading: boolean
   - isLoadingMore: boolean
   - hasMore: boolean
   - error: string | null
   - refreshing: boolean

4. **Actions**:
   - setFilter(filter: FeedFilter) - Change active filter, clear posts
   - fetchPosts(userId?: string) - Initial load based on current filter
   - fetchMorePosts(userId?: string) - Load next page with cursor
   - refreshPosts(userId?: string) - Pull-to-refresh, reset cursor
   - clearFeed() - Clear all state (used on sign out)

5. **Filter routing logic** in fetchPosts:
   - 'recent' -> getRecentFeed()
   - 'popular' -> getPopularFeed()
   - 'following' -> getFollowingFeed(userId) // requires userId

Use dynamic imports for lib/supabase/posts to match profileStore pattern.

Cursor management: Store last post's created_at as cursor for next page.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="feedStore.test" --passWithNoTests</automated>
  </verify>
  <done>
    - stores/feedStore.ts exports useFeedStore hook
    - Filter state changes trigger correct query functions
    - Pagination state managed correctly
    - Store follows existing Zustand patterns in project
  </done>
</task>

</tasks>

<verification>
- All files export expected functions with correct signatures
- TypeScript compiles without errors: npx tsc --noEmit
- Test files pass (once Wave 0 creates them)
</verification>

<success_criteria>
- lib/supabase/posts.ts: getFollowingFeed joins with follows table, returns filtered posts
- lib/supabase/likes.ts: getPostLikes and getUserLikedPosts return profile data
- services/likeService.ts: Business layer wraps Supabase queries with error handling
- stores/feedStore.ts: Zustand store manages feed filter state and pagination
</success_criteria>

<output>
After completion, create `.planning/phases/14-follow-system-engagement-polish/14-01-SUMMARY.md`
</output>
