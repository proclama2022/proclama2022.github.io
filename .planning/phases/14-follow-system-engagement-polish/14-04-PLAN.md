---
phase: 14-follow-system-engagement-polish
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified: [components/community/FeedFilterTabs.tsx, components/community/EmptyFeedState.tsx]
autonomous: true
requirements: [FOLL-04]
user_setup: []

must_haves:
  truths:
    - "User can switch between Recent, Popular, and Following feed filters"
    - "Active filter is visually highlighted"
    - "Following filter shows personalized empty state when not following anyone"
  artifacts:
    - path: "components/community/FeedFilterTabs.tsx"
      provides: "Horizontal tab buttons for feed filtering"
      exports: ["FeedFilterTabs", "FeedFilter"]
    - path: "components/community/EmptyFeedState.tsx"
      provides: "Contextual empty state for different filters"
      exports: ["EmptyFeedState"]
  key_links:
    - from: "components/FeedFilterTabs.tsx"
      to: "stores/feedStore.ts"
      via: "setFilter action"
      pattern: "setFilter\\("
    - from: "components/EmptyFeedState.tsx"
      to: "i18n resources"
      via: "useTranslation hook"
      pattern: "t\\('community"
---

<objective>
Create Feed Filter UI components

Purpose: Provide user interface for switching feed filters and displaying empty states
Output: Filter tab component and contextual empty state component
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types from Plan 14-01 -->

From stores/feedStore.ts:
```typescript
export type FeedFilter = 'recent' | 'popular' | 'following';

export interface FeedState {
  posts: Post[];
  filter: FeedFilter;
  isLoading: boolean;
  // ...
}

export const useFeedStore: () => FeedState & {
  setFilter: (filter: FeedFilter) => void;
  // ...
};
```

From i18n/resources/en.json (added in Plan 02):
```json
"community": {
  "followingFilter": "Following",
  "recentFilter": "Recent",
  "popularFilter": "Popular",
  "emptyFollowing": "You're not following anyone yet",
  "emptyFollowingCTA": "Discover plant lovers"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FeedFilterTabs component</name>
  <files>components/community/FeedFilterTabs.tsx</files>
  <behavior>
    - Test 1: Renders three filter tabs: Recent, Popular, Following
    - Test 2: Active filter is visually distinct
    - Test 3: Calls setFilter when tab is pressed
    - Test 4: Tab labels are translated
  </behavior>
  <action>
Create components/community/FeedFilterTabs.tsx:

```typescript
import React from 'react';
import { View, TouchableOpacity, StyleSheet } from 'react-native';
import { useTranslation } from 'react-i18next';

import { ThemedText } from '@/components/Themed';
import { useColorScheme } from '@/components/useColorScheme';
import Colors from '@/constants/Colors';
import type { FeedFilter } from '@/stores/feedStore';

export interface FeedFilterTabsProps {
  activeFilter: FeedFilter;
  onFilterChange: (filter: FeedFilter) => void;
}

export const FeedFilterTabs: React.FC<FeedFilterTabsProps> = ({
  activeFilter,
  onFilterChange,
}) => {
  const { t } = useTranslation();
  const colorScheme = useColorScheme() ?? 'light';
  const colors = Colors[colorScheme];

  const filters: { key: FeedFilter; label: string }[] = [
    { key: 'recent', label: t('community.recentFilter') },
    { key: 'popular', label: t('community.popularFilter') },
    { key: 'following', label: t('community.followingFilter') },
  ];

  return (
    <View style={[styles.container, { backgroundColor: colors.background }]}>
      {filters.map((filter) => {
        const isActive = activeFilter === filter.key;
        return (
          <TouchableOpacity
            key={filter.key}
            style={[
              styles.tab,
              isActive && { backgroundColor: colors.tint },
            ]}
            onPress={() => onFilterChange(filter.key)}
            activeOpacity={0.7}
          >
            <ThemedText
              style={[
                styles.tabText,
                isActive && styles.activeTabText,
              ]}
            >
              {filter.label}
            </ThemedText>
          </TouchableOpacity>
        );
      })}
    </View>
  );
};

// Re-export FeedFilter type for convenience
export type { FeedFilter } from '@/stores/feedStore';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    paddingHorizontal: 16,
    paddingVertical: 12,
    gap: 8,
  },
  tab: {
    paddingVertical: 8,
    paddingHorizontal: 16,
    borderRadius: 20,
    backgroundColor: 'rgba(128, 128, 128, 0.15)',
  },
  tabText: {
    fontSize: 14,
    fontWeight: '500',
  },
  activeTabText: {
    color: '#fff',
  },
});
```

Design follows CONTEXT.md decisions:
- Horizontal tabs styled as pill buttons
- Active tab highlighted with colors.tint background
- Labels from i18n translations
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="FeedFilterTabs.test" --passWithNoTests</automated>
  </verify>
  <done>
    - components/community/FeedFilterTabs.tsx exports FeedFilterTabs
    - Three tabs render: Recent, Popular, Following
    - Active tab has tint background
    - onFilterChange fires on tab press
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create EmptyFeedState component</name>
  <files>components/community/EmptyFeedState.tsx</files>
  <behavior>
    - Test 1: Shows "no posts" message for recent/popular filters
    - Test 2: Shows "not following" message for following filter
    - Test 3: Shows CTA button for following empty state
    - Test 4: Renders illustration/icon for visual appeal
  </behavior>
  <action>
Create components/community/EmptyFeedState.tsx:

```typescript
import React from 'react';
import { View, TouchableOpacity, StyleSheet } from 'react-native';
import { useTranslation } from 'react-i18next';
import { Ionicons } from '@expo/vector-icons';

import { ThemedText } from '@/components/Themed';
import { useColorScheme } from '@/components/useColorScheme';
import Colors from '@/constants/Colors';
import type { FeedFilter } from '@/stores/feedStore';

export interface EmptyFeedStateProps {
  filter: FeedFilter;
  onDiscoverPress?: () => void;
}

export const EmptyFeedState: React.FC<EmptyFeedStateProps> = ({
  filter,
  onDiscoverPress,
}) => {
  const { t } = useTranslation();
  const colorScheme = useColorScheme() ?? 'light';
  const colors = Colors[colorScheme];

  // Following filter has special empty state
  if (filter === 'following') {
    return (
      <View style={styles.container}>
        <Ionicons
          name="people-outline"
          size={64}
          color={colors.tabIconDefault}
        />
        <ThemedText style={styles.title}>
          {t('community.emptyFollowing')}
        </ThemedText>
        <ThemedText style={styles.subtitle}>
          {t('community.emptyFollowingCTA')}
        </ThemedText>
        {onDiscoverPress && (
          <TouchableOpacity
            style={[styles.ctaButton, { backgroundColor: colors.tint }]}
            onPress={onDiscoverPress}
          >
            <ThemedText style={styles.ctaText}>
              {t('community.emptyFollowingCTA')}
            </ThemedText>
          </TouchableOpacity>
        )}
      </View>
    );
  }

  // Generic empty state for recent/popular
  return (
    <View style={styles.container}>
      <Ionicons
        name="leaf-outline"
        size={64}
        color={colors.tabIconDefault}
      />
      <ThemedText style={styles.title}>
        {t('community.emptyFeed')}
      </ThemedText>
      <ThemedText style={styles.subtitle}>
        {t('community.emptyFeedCTA')}
      </ThemedText>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 40,
  },
  title: {
    fontSize: 18,
    fontWeight: '600',
    marginTop: 16,
    textAlign: 'center',
  },
  subtitle: {
    fontSize: 14,
    opacity: 0.6,
    marginTop: 8,
    textAlign: 'center',
  },
  ctaButton: {
    marginTop: 24,
    paddingVertical: 12,
    paddingHorizontal: 24,
    borderRadius: 24,
  },
  ctaText: {
    color: '#fff',
    fontSize: 15,
    fontWeight: '600',
  },
});
```

Empty states per CONTEXT.md:
- Following: "You're not following anyone yet" + "Discover plant lovers" CTA
- Generic: "No posts yet" + "Be the first to share a plant!"
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="EmptyFeedState.test" --passWithNoTests</automated>
  </verify>
  <done>
    - components/community/EmptyFeedState.tsx exports EmptyFeedState
    - Following filter shows special empty state with CTA
    - Recent/Popular show generic empty state
    - Translations used for all text
  </done>
</task>

<task type="auto">
  <name>Task 3: Add remaining community translations</name>
  <files>i18n/resources/en.json, i18n/resources/it.json</files>
  <action>
Verify/complete community section translations in both files:

**en.json** - ensure community section has all keys:
```json
"community": {
  "likes": "Likes",
  "noLikes": "No likes yet",
  "beFirst": "Be the first to like this",
  "followingFilter": "Following",
  "recentFilter": "Recent",
  "popularFilter": "Popular",
  "emptyFollowing": "You're not following anyone yet",
  "emptyFollowingCTA": "Discover plant lovers",
  "emptyFeed": "No posts yet",
  "emptyFeedCTA": "Be the first to share a plant!",
  "noLikedPosts": "Posts you like will appear here"
}
```

**it.json** - ensure community section has all keys:
```json
"community": {
  "likes": "Mi piace",
  "noLikes": "Nessun mi piace ancora",
  "beFirst": "Sii il primo a mettere mi piace",
  "followingFilter": "Seguiti",
  "recentFilter": "Recenti",
  "popularFilter": "Popolari",
  "emptyFollowing": "Non segui ancora nessuno",
  "emptyFollowingCTA": "Scopri altri amanti delle piante",
  "emptyFeed": "Nessun post ancora",
  "emptyFeedCTA": "Sii il primo a condividere una pianta!",
  "noLikedPosts": "I post che ti piacciono appariranno qui"
}
```

Note: Some keys may have been added in previous plans. Verify all exist.
  </action>
  <verify>
    <automated>node -e "const en=require('./i18n/resources/en.json'); const it=require('./i18n/resources/it.json'); const keys=['likes','noLikes','followingFilter','recentFilter','popularFilter','emptyFollowing','emptyFeed']; const missing = keys.filter(k => !en.community?.[k] || !it.community?.[k]); if(missing.length) { console.log('Missing:', missing); process.exit(1); } else { console.log('All translations present'); }"</automated>
  </verify>
  <done>
    - All community translation keys present in en.json
    - All community translation keys present in it.json
    - Both files valid JSON
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: npx tsc --noEmit
- FeedFilterTabs renders with all three tabs
- EmptyFeedState shows correct message per filter
</verification>

<success_criteria>
- FeedFilterTabs component renders Recent/Popular/Following tabs
- Active tab visually highlighted
- EmptyFeedState shows contextual message for each filter
- All translations available in EN and IT
</success_criteria>

<output>
After completion, create `.planning/phases/14-follow-system-engagement-polish/14-04-SUMMARY.md`
</output>
