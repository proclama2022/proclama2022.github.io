---
phase: 05-multi-photo-gallery
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01 — Data model migration complete"
  - "05-02 — PhotoGallery and AddPhotoButton components exist"
files_modified:
  - components/Detail/PhotoLightbox.tsx
  - components/Detail/PhotoGallery.tsx
  - components/Detail/AddPhotoButton.tsx
  - app/plant/[id].tsx
autonomous: false
requirements:
  - GALLERY-01
  - GALLERY-02
  - PHOTO-07

must_haves:
  truths:
    - "Lightbox opens full-screen when tapping thumbnail"
    - "Swipe left/right navigates between photos"
    - "Pinch-to-zoom works for zooming in (or zoom disabled if too complex)"
    - "Set Primary button sets current photo as primary"
    - "Delete button shows confirmation dialog"
    - "Delete removes photo and updates array (sets new primary if deleting was primary)"
    - "Add Photo button launches camera/gallery picker"
    - "New photo becomes primary automatically"
    - "Photos are compressed on upload (max 1024px dimension, JPEG 0.7 quality)"
  artifacts:
    - path: "components/Detail/PhotoLightbox.tsx"
      provides: "Full-screen modal viewer with swipe and actions"
      min_lines: 150
    - path: "components/Detail/AddPhotoButton.tsx"
      exports: ["handleAddPhoto function with expo-image-picker and expo-image-manipulator"]
    - path: "components/Detail/PhotoGallery.tsx"
      exports: ["integrated lightbox state and handlers"]
    - path: "app/plant/[id].tsx"
      exports: ["PhotoGallery rendered in InfoTab"]
  key_links:
    - from: "PhotoLightbox.tsx"
      to: "expo-image-picker"
      via: "launchImageLibraryAsync and launchCameraAsync"
      pattern: "ImagePicker\\.(launchImageLibraryAsync|launchCameraAsync)"
    - from: "AddPhotoButton.tsx"
      to: "expo-image-manipulator"
      via: "ImageManipulator.manipulateAsync for compression"
      pattern: "ImageManipulator\\.manipulateAsync"
    - from: "PhotoLightbox.tsx"
      to: "expo-file-system"
      via: "FileSystem.copyAsync for persistent URIs"
      pattern: "FileSystem\\.copyAsync"
    - from: "AddPhotoButton.tsx"
      to: "plantsStore"
      via: "updatePlant to add new photo"
      pattern: "updatePlant"
    - from: "PhotoGallery.tsx"
      to: "PhotoLightbox.tsx"
      via: "onPhotoPress callback opens lightbox"
      pattern: "setLightboxVisible"
    - from: "PhotoLightbox.tsx"
      to: "plantsStore"
      via: "set primary and delete operations"
      pattern: "updatePlant.*isPrimary"
---

<objective>
Build PhotoLightbox full-screen viewer, implement add/set primary/delete photo operations, integrate gallery into InfoTab with camera/gallery picker and photo compression
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-photo-gallery/05-CONTEXT.md
@.planning/phases/05-multi-photo-gallery/05-RESEARCH.md

# Integration points
@app/plant/[id].tsx — Tab host where PhotoGallery will render
@app/camera.tsx — Reference for expo-image-picker usage pattern
@components/Detail/InfoTab.tsx — Integration target (PhotoGallery replaces single photo)
@services/plantsStore.ts — updatePlant for photo CRUD operations
</context>

<tasks>

<task type="auto">
  <name>Create PhotoLightbox full-screen viewer component</name>
  <files>components/Detail/PhotoLightbox.tsx</files>
  <action>
    Create PhotoLightbox component implementing CONTEXT.md specs:

    Interface:
    ```typescript
    interface PhotoLightboxProps {
      visible: boolean;
      photos: PlantPhoto[];
      initialIndex: number;
      plantId: string;
      onClose: () => void;
    }
    ```

    Component structure:
    - Modal with transparent background, fade animation
    - Close button (top-left, white icon)
    - ScrollView horizontal with pagingEnabled for swipe navigation
    - Image component in each page (resizeMode: 'contain')
    - Action bar at bottom (Set Primary + Delete buttons)
    - Page indicator showing "X / Y" count

    State management:
    - currentIndex state tracks visible photo
    - scrollViewRef for programmatic scrolling
    - useEffect to scroll to initialIndex on open

    Swipe navigation:
    - onMomentumScrollEnd calculates index from contentOffset.x / SCREEN_WIDTH
    - Math.round to nearest integer for accurate index

    Action bar buttons:
    - Set Primary: calls handleSetPrimary(currentIndex)
    - Delete: calls handleDelete(currentIndex) with Alert confirmation
    - Semi-transparent background (rgba white with alpha)

    Set Primary logic:
    - Map photos array: set isPrimary=true for current index, false for all others
    - Call updatePlant(plantId, { photos: updatedPhotos })
    - Haptic feedback on success

    Delete logic:
    - Alert.alert with "Delete Photo?" title
    - If photos.length === 1: show special warning "This is the only photo..."
    - On confirm: filter array, update primary if needed, call FileSystem.deleteAsync
    - If deleted was primary: set first remaining as primary

    Reference RESEARCH.md Pattern 4 for exact implementation. Use Modal (not navigation router) per anti-pattern warning.

    PINCH-TO-ZOOM: Start WITHOUT zoom (use basic Image component). If time permits and gesture-handler already installed, add simple transform scale. CONTEXT.md lists this under "Claude's discretion" — basic version acceptable for MVP.
  </action>
  <verify>
    grep -n "Modal" components/Detail/PhotoLightbox.tsx — Modal must be used
    grep -n "pagingEnabled" components/Detail/PhotoLightbox.tsx — Swipe must be enabled
    grep -n "handleSetPrimary\|handleDelete" components/Detail/PhotoLightbox.tsx — Action handlers must exist
    wc -l components/Detail/PhotoLightbox.tsx — Component should be 150-220 lines
  </verify>
  <done>
    PhotoLightbox component renders full-screen modal
    Swipe left/right navigates between photos
    Set Primary button sets current photo as primary with haptic feedback
    Delete button shows confirmation dialog and removes photo
    Page indicator shows current position
    Close button dismisses lightbox
  </done>
</task>

<task type="auto">
  <name>Implement photo add functionality with compression in AddPhotoButton</name>
  <files>components/Detail/AddPhotoButton.tsx</files>
  <action>
    Enhance AddPhotoButton from Plan 02 to implement photo adding with compression:

    Update interface to accept plantId:
    ```typescript
    interface AddPhotoButtonProps {
      plantId: string;
      onPhotoAdded?: () => void;
      size?: number;
    }
    ```

    Import expo-image-manipulator:
    ```typescript
    import * as ImageManipulator from 'expo-image-manipulator';
    ```

    Implement handleAddPhoto function:
    - Show action sheet using Alert.alert with 3 buttons: ["Camera", "Gallery", "Cancel"]
    - Camera: call ImagePicker.launchCameraAsync
    - Gallery: call ImagePicker.launchImageLibraryAsync
    - Both use: mediaTypes: 'images', allowsEditing: false, quality: 1

    On photo selected (result.canceled === false):
    - Get URI from result.assets[0].uri

    COMPRESS IMAGE (PHOTO-07 requirement):
    - Call ImageManipulator.manipulateAsync with:
      - actions: [ImageManipulator.Action.resize({ width: 1024 })]
        - Note: Only specify width, height calculated automatically to maintain aspect ratio
      - compress: 0.7 (JPEG quality as per PHOTO-07)
      - format: ImageManipulator.SaveFormat.JPEG
    - This returns compressed URI

    Copy compressed result to document directory:
    - Use FileSystem.copyAsync with compressed URI
    - Generate filename: `plant_${plantId}_${Date.now()}.jpg`
    - Dest URI: FileSystem.documentDirectory + filename

    Create new PlantPhoto:
    ```typescript
    const newPhoto: PlantPhoto = {
      uri: destUri,
      addedDate: new Date().toISOString(),
      isPrimary: true,
    };
    ```

    Fetch current plant from store, update photos array:
    - Mark all existing photos as isPrimary: false
    - Append newPhoto to array
    - Call updatePlant(plantId, { photos: updatedPhotos })

    Add haptic feedback on success.

    Reference RESEARCH.md "Add Photo with expo-image-picker" example. Reference camera.tsx for existing ImagePicker usage pattern.

    Handle errors gracefully (Alert.alert on failure).

    PER PHOTO-07: All uploaded photos must be compressed to max 1024px dimension and 0.7 JPEG quality before saving.
  </action>
  <verify>
    grep -n "ImagePicker\\.(launchCameraAsync|launchImageLibraryAsync)" components/Detail/AddPhotoButton.tsx — Picker calls must exist
    grep -n "ImageManipulator\\.manipulateAsync" components/Detail/AddPhotoButton.tsx — Compression step must exist
    grep -n "resize.*1024" components/Detail/AddPhotoButton.tsx — Max dimension resize must be 1024px
    grep -n "compress.*0\\.7" components/Detail/AddPhotoButton.tsx — JPEG quality must be 0.7
    grep -n "FileSystem\\.copyAsync" components/Detail/AddPhotoButton.tsx — File copy must exist
    grep -n "updatePlant" components/Detail/AddPhotoButton.tsx — Store update must exist
  </verify>
  <done>
    AddPhotoButton launches camera/gallery picker via action sheet
    Selected photo compressed using expo-image-manipulator (max 1024px, JPEG 0.7 quality)
    Compressed photo copied to app document directory
    New photo added to plant's photos array as primary
    All existing photos marked as non-primary
    Haptic feedback on successful add
    Error handling with user-friendly alerts
    PHOTO-07 satisfied: photos compressed before storage
  </done>
</task>

<task type="auto">
  <name>Integrate PhotoGallery with lightbox in InfoTab</name>
  <files>components/Detail/PhotoGallery.tsx, app/plant/[id].tsx</files>
  <action>
    Update PhotoGallery to manage lightbox state and wire handlers:

    Add state to PhotoGallery:
    ```typescript
    const [lightboxVisible, setLightboxVisible] = useState(false);
    const [lightboxIndex, setLightboxIndex] = useState(0);
    ```

    Update onPhotoPress handler to open lightbox:
    ```typescript
    const handlePhotoPress = (index: number) => {
      setLightboxIndex(index);
      setLightboxVisible(true);
    };
    ```

    Add handlers for lightbox actions:
    ```typescript
    const handleCloseLightbox = () => setLightboxVisible(false);
    const handleSetPrimary = (index: number) => {
      // Delegate to lightbox component
    };
    const handleDelete = (index: number) => {
      // Delegate to lightbox component
    };
    const handlePhotoAdded = () => {
      // Refresh plant data from store
    };
    ```

    Render PhotoLightbox at bottom of PhotoGallery:
    - Conditionally render if lightboxVisible
    - Pass visible, photos, initialIndex, plantId, onClose
    - Pass action handlers

    Update AddPhotoButton in ListFooterComponent:
    - Pass plantId prop
    - Pass onPhotoAdded callback

    Then integrate into app/plant/[id].tsx:
    - Read current InfoTab implementation
    - Replace single Image component (lines ~100-120) with PhotoGallery
    - Keep photo in header (60x60 thumbnail) unchanged
    - PhotoGallery renders below names block in scroll content

    Follow InfoTab pattern: useLocalSearchParams, usePlantsStore.getPlant, conditional not-found rendering.

    PhotoGallery placement: after the "Added on" date block, before CareInfo section (around line 130-140 in InfoTab).
  </action>
  <verify>
    grep -n "PhotoLightbox" components/Detail/PhotoGallery.tsx — Lightbox import and render must exist
    grep -n "setLightboxVisible" components/Detail/PhotoGallery.tsx — State management must exist
    grep -n "PhotoGallery" app/plant/\[id\].tsx — Integration must be present
  </verify>
  <done>
    PhotoGallery manages lightbox visibility and index state
    Tapping thumbnail opens full-screen lightbox
    Lightbox renders with all photos and actions
    Set Primary updates plant data and refreshes UI
    Delete removes photo and updates array
    Add Photo launches picker and updates gallery
    PhotoGallery integrated into InfoTab below identification details
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete multi-photo gallery feature end-to-end</name>
  <files>none</files>
  <action>Human verifies the complete multi-photo gallery feature by running the app and testing thumbnail grid, lightbox, add/set primary/delete operations per the how-to-verify steps below.</action>
  <verify>
    <automated>cd "/Users/martha2022/Documents/Claude code/Plantid" && npx tsc --noEmit 2>&1 | head -5</automated>
    <manual>
Test full photo gallery flow:

1. Open existing plant detail (should see migrated photo in grid)
2. Tap thumbnail → lightbox opens full-screen
3. Swipe left/right → navigate between photos
4. Tap "Set Primary" button → star badge updates on thumbnail
5. Tap "Delete" button → confirmation dialog appears
6. Confirm delete → photo removed from grid
7. Tap "Add Photo" button (+) → action sheet appears
8. Select "Camera" → camera opens
9. Take photo → new photo appears in grid with star badge
10. Test empty state: delete all photos → should show placeholder with + button

Verify PHOTO-07 compression:
- Add a high-resolution photo (2000px+ from gallery)
- Photo should be added successfully without storage bloat
- File size should be reasonable (typically <500KB for 1024px JPEG 0.7)

Verify backward compatibility:
- Plants created before v1.2 show migrated photo
- Old plant.photo field not used in UI (photos array only)

Verify file persistence:
- Add photo from gallery
- Delete photo from device gallery
- App still shows photo (copied to document directory)
    </manual>
    <sampling_rate>run after this task completes, before phase verification</sampling_rate>
  </verify>
  <done>Human confirms all photo gallery operations work correctly: thumbnail grid display, lightbox navigation, set primary, delete with confirmation, add photo from camera/gallery with compression (max 1024px, JPEG 0.7 quality), empty state handling, backward compatibility with migrated photos, and file persistence.</done>
  <resume-signal>Type "approved" if all tests pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. **TypeScript compilation:** `npx tsc --noEmit` must pass
2. **Compression present:** `grep -c "ImageManipulator" components/Detail/AddPhotoButton.tsx` returns 2+ (import + usage)
3. **Component exports:** `grep -l "export.*PhotoLightbox\|export.*PhotoGallery\|export.*AddPhotoButton" components/Detail/*.tsx` finds all files
4. **Compression parameters:** `grep "compress.*0\.7" components/Detail/AddPhotoButton.tsx` confirms JPEG quality
</verification>

<success_criteria>
- PhotoLightbox component with full-screen modal, swipe navigation, and actions
- AddPhotoButton with expo-image-picker and expo-image-manipulator compression
- Photos compressed to max 1024px dimension, JPEG 0.7 quality (PHOTO-07 satisfied)
- PhotoGallery integrated into InfoTab with lightbox functionality
- Set Primary, Delete, and Add Photo operations working
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-photo-gallery/05-03-SUMMARY.md`
</output>
