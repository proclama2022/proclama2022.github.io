---
phase: 05-multi-photo-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/index.ts
  - stores/plantsStore.ts
autonomous: true
requirements:
  - GALLERY-03

must_haves:
  truths:
    - "Existing plants with single photo migrate to photos array on app load"
    - "New plants created with photos array instead of deprecated photo field"
    - "Migration runs once and never re-runs (flag prevents duplicate migrations)"
    - "Backward compatibility maintained (photo field still readable)"
  artifacts:
    - path: "types/index.ts"
      provides: "PlantPhoto interface, SavedPlant.photos optional field"
      contains: "PlantPhoto { uri, addedDate, isPrimary }"
    - path: "stores/plantsStore.ts"
      provides: "Migration logic in onRehydrateStorage hook"
      exports: ["migrateToPhotos", "_version"]
  key_links:
    - from: "stores/plantsStore.ts"
      to: "types/index.ts"
      via: "import SavedPlant, PlantPhoto"
      pattern: "import.*SavedPlant.*from.*types"
    - from: "app/_layout.tsx"
      to: "stores/plantsStore.ts"
      via: "usePlantsStore.getState().migrateToPhotos()"
      pattern: "migrateToPhotos"
---

<objective>
Add PlantPhoto type, extend SavedPlant with photos array, implement one-time migration from photo string to photos array in Zustand store

Purpose: Enable multi-photo storage while preserving existing user data
Output: Migration-ready data model with automatic photo→photos transformation
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-photo-gallery/05-CONTEXT.md
@.planning/phases/05-multi-photo-gallery/05-RESEARCH.md

# Phase patterns to follow
@.planning/phases/03-monetization/03-01-SUMMARY.md — Zustand persist middleware patterns
@.planning/phases/04-tabbed-layout-and-content-reorganization/04-01-SUMMARY.md — Additive optional type extensions
</context>

<tasks>

<task type="auto">
  <name>Add PlantPhoto interface and extend SavedPlant type</name>
  <files>types/index.ts</files>
  <action>
    Add PlantPhoto interface after WaterEvent (around line 78):

    ```typescript
    export interface PlantPhoto {
      uri: string;              // File URI (not base64)
      addedDate: string;        // ISO timestamp
      isPrimary: boolean;       // true for main photo
    }
    ```

    Extend SavedPlant interface (around line 55-73) by adding photos field after the photo field:
    - Keep existing `photo: string;` field (line 62) — DEPRECATED but maintained for backward compatibility
    - Add `photos?: PlantPhoto[];` after line 62 (new field, optional)

    Do NOT modify any other types. This is a minimal additive change following Phase 04 pattern (optional fields = zero migration complexity).
  </action>
  <verify>
    npx tsc --noEmit — TypeScript compilation must pass without errors
  </verify>
  <done>
    PlantPhoto interface defined with uri/addedDate/isPrimary fields
    SavedPlant extended with optional photos array field
    Existing photo field preserved for backward compatibility
  </done>
</task>

<task type="auto">
  <name>Implement migration logic in plantsStore</name>
  <files>stores/plantsStore.ts</files>
  <action>
    Import PlantPhoto type from types (add to line 4 import).

    Add migration version constant at top of file (after MAX_PLANTS_FREE):
    ```typescript
    const MIGRATION_VERSION = 1;
    ```

    Extend PlantsState interface (after line 19) to add:
    ```typescript
    _version: number;
    migrateToPhotos: () => void;
    ```

    Update initial state in persist config (line 24): add `_version: MIGRATION_VERSION,`

    Implement migrateToPhotos method (after cancelAllNotifications, around line 63):
    - Check `if (_version >= MIGRATION_VERSION) return;` — already migrated
    - Map plants: if `plant.photos?.length > 0` return unchanged (already migrated)
    - Otherwise: transform `photo: string` → `photos: [{ uri: plant.photo, addedDate: plant.addedDate, isPrimary: true }]`
    - Set state with migrated plants and `_version: MIGRATION_VERSION`

    Add onRehydrateStorage hook to persist config (after storage config, line 67):
    ```typescript
    onRehydrateStorage: () => (state) => {
      state?.migrateToPhotos();
    },
    ```

    Reference RESEARCH.md Pattern 2 for exact implementation. Use Zustand's onRehydrateStorage pattern — NOT manual migration in app/_layout.tsx.
  </action>
  <verify>
    grep -n "onRehydrateStorage" stores/plantsStore.ts — Hook must be present in persist config
    grep -n "migrateToPhotos" stores/plantsStore.ts — Method must be defined
    grep -n "_version" stores/plantsStore.ts — Version tracking must exist
  </verify>
  <done>
    migrateToPhotos method transforms photo→photos array with proper PlantPhoto structure
    Migration version flag prevents re-running on every app launch
    onRehydrateStorage hook calls migration automatically after Zustand loads from AsyncStorage
  </done>
</task>

<task type="auto">
  <name>Trigger migration on app startup</name>
  <files>app/_layout.tsx</files>
  <action>
    Read existing app/_layout.tsx to understand current structure.

    Add useEffect hook in root layout (after existing hooks, inside main component):
    - Import usePlantsStore from '@/stores/plantsStore'
    - Call `usePlantsStore.getState().migrateToPhotos()` in useEffect with empty deps
    - Wrap in try/catch for safety (log error but don't crash)

    Pattern reference: Phase 02 notification permission check pattern — defensive async init in root layout.

    DO NOT wait for migration to complete before rendering — migration is synchronous and runs instantly on state hydration.
  </action>
  <verify>
    grep -A 5 "migrateToPhotos" app/_layout.tsx — useEffect must call migration on mount
  </verify>
  <done>
    Migration triggered on app startup via useEffect in root layout
    Migration completes synchronously before UI renders
    Existing user data with photo field transformed to photos array
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **TypeScript compilation:** `npx tsc --noEmit` must pass without errors
2. **Migration present:** `grep -c "migrateToPhotos" stores/plantsStore.ts` returns 2+ (definition + onRehydrateStorage call)
3. **Type extension:** `grep "photos?: PlantPhoto\[\]" types/index.ts` confirms new field
4. **Backward compatibility:** `grep "photo: string;" types/index.ts` still exists (not deleted)

Test migration manually:
- Create a test plant with `photo: string` field (simulated old data)
- Run app once
- Check that plant now has `photos: [{ uri, addedDate, isPrimary: true }]` array
- Re-run app — migration should NOT run again (_version flag prevents it)
</verification>

<success_criteria>
- PlantPhoto interface defined with uri, addedDate, isPrimary fields
- SavedPlant.photos optional field added, photo field preserved
- Migration logic transforms photo→photos array for existing plants
- Migration runs once via onRehydrateStorage, flagged by _version
- TypeScript compiles without errors
- No breaking changes to existing plant data structure
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-photo-gallery/05-01-SUMMARY.md`
</output>
