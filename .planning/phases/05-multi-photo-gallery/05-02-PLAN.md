---
phase: 05-multi-photo-gallery
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01 — PlantPhoto type and migration logic in place"
files_modified:
  - components/Detail/PhotoGallery.tsx
  - components/Detail/AddPhotoButton.tsx
  - i18n/resources/en.json
  - i18n/resources/it.json
autonomous: true
requirements:
  - GALLERY-01

must_haves:
  truths:
    - "3-column thumbnail grid displays square-cropped photos"
    - "Primary photo shows star badge indicator in top-right corner"
    - "Add photo button visible in grid with dashed border"
    - "Empty state shows placeholder when plant has no photos"
    - "Tapping thumbnail opens lightbox (pending Plan 03)"
  artifacts:
    - path: "components/Detail/PhotoGallery.tsx"
      provides: "Thumbnail grid component with FlatList"
      min_lines: 80
    - path: "components/Detail/AddPhotoButton.tsx"
      provides: "Reusable add photo button component"
      min_lines: 30
    - path: "i18n/resources/en.json"
      contains: "gallery keys (addPhoto, emptyState, etc.)"
    - path: "i18n/resources/it.json"
      contains: "gallery translations"
  key_links:
    - from: "PhotoGallery.tsx"
      to: "types/index.ts"
      via: "PlantPhoto interface"
      pattern: "PlantPhoto"
    - from: "PhotoGallery.tsx"
      to: "plantsStore"
      via: "usePlantsStore for photos data"
      pattern: "usePlantsStore"
    - from: "AddPhotoButton.tsx"
      to: "PhotoGallery.tsx"
      via: "onAddPress callback prop"
      pattern: "onAddPress"
---

<objective>
Build PhotoGallery thumbnail grid component (3-column, square thumbnails, primary badge) and AddPhotoButton component with i18n support

Purpose: Display multiple photos in Instagram-style grid with add functionality
Output: Working thumbnail grid ready for lightbox integration
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-photo-gallery/05-CONTEXT.md
@.planning/phases/05-multi-photo-gallery/05-RESEARCH.md

# Component patterns to follow
@components/Detail/InfoTab.tsx — Component structure, useLocalSearchParams pattern
@components/PlantGrid.tsx — FlatList with numColumns pattern
@.planning/phases/04-tabbed-layout-and-content-reorganization/04-03-SUMMARY.md — Bilingual i18n pattern
</context>

<tasks>

<task type="auto">
  <name>Add gallery i18n translation keys</name>
  <files>i18n/resources/en.json, i18n/resources/it.json</files>
  <action>
    Add gallery section to both EN and IT translation files.

    English (en.json), add after detail.notes section:
    ```json
    "gallery": {
      "title": "Photos",
      "addPhoto": "Add Photo",
      "emptyState": "No photos yet",
      "emptyHint": "Tap + to add your first photo",
      "setPrimary": "Set as Primary",
      "deletePhoto": "Delete Photo",
      "deleteWarning": "This photo will be permanently removed.",
      "deleteOnlyWarning": "This is the only photo. Deleting it will remove the primary image.",
      "cancel": "Cancel",
      "delete": "Delete",
      "photoCount": "{count} photo",
      "photoCount_plural": "{count} photos"
    }
    ```

    Italian (it.json), add after detail.notes section:
    ```json
    "gallery": {
      "title": "Foto",
      "addPhoto": "Aggiungi Foto",
      "emptyState": "Nessuna foto",
      "emptyHint": "Tocca + per aggiungere la prima foto",
      "setPrimary": "Imposta come principale",
      "deletePhoto": "Elimina foto",
      "deleteWarning": "Questa foto verrà rimossa permanentemente.",
      "deleteOnlyWarning": "Questa è l'unica foto. L'eliminazione rimuoverà l'immagine principale.",
      "cancel": "Annulla",
      "delete": "Elimina",
      "photoCount": "{count} foto",
      "photoCount_plural": "{count} foto"
    }
    ```

    Follow Phase 04 bilingual pattern: same key structure in both languages.
  </action>
  <verify>
    grep -A 15 '"gallery"' i18n/resources/en.json — All 11 keys must be present
    grep -A 15 '"gallery"' i18n/resources/it.json — All 11 keys must be present in Italian
  </verify>
  <done>
    Gallery section added to both translation files with 11 keys each
    English and Italian translations follow same structure
    Pluralization support included (photoCount key)
  </done>
</task>

<task type="auto">
  <name>Create AddPhotoButton component</name>
  <files>components/Detail/AddPhotoButton.tsx</files>
  <action>
    Create reusable AddPhotoButton component following CONTEXT.md specifications:
    - Dashed border (borderStyle: 'dashed', borderColor: '#ddd', borderWidth: 2)
    - Centered "+" icon (Ionicons 'add', size 32, color '#999')
    - Square aspect matching thumbnail size
    - TouchableOpacity for press handling
    - Accepts onPress prop callback
    - Optional size prop (default: thumbnail size)

    Component signature:
    ```typescript
    interface AddPhotoButtonProps {
      onPress: () => void;
      size?: number;
    }

    export function AddPhotoButton({ onPress, size = 110 }: AddPhotoButtonProps)
    ```

    Style pattern:
    - backgroundColor: '#fafafa'
    - alignItems: 'center', justifyContent: 'center'
    - borderRadius: 4

    Reference RESEARCH.md PhotoGallery example for exact styling. Keep component simple — single-purpose button following React Native component patterns.
  </action>
  <verify>
    grep -n "AddPhotoButton" components/Detail/AddPhotoButton.tsx — Export must exist
    wc -l components/Detail/AddPhotoButton.tsx — Component should be 30-50 lines
  </verify>
  <done>
    AddPhotoButton component created with dashed border styling
    Component accepts onPress and size props
    Matches thumbnail grid visual style
  </done>
</task>

<task type="auto">
  <name>Create PhotoGallery thumbnail grid component</name>
  <files>components/Detail/PhotoGallery.tsx</files>
  <action>
    Create PhotoGallery component implementing CONTEXT.md thumbnail grid specs:

    Interface:
    ```typescript
    interface PhotoGalleryProps {
      plantId?: string;
      onPhotoPress: (index: number) => void;
      onAddPress: () => void;
    }
    ```

    Component requirements:
    - Use useLocalSearchParams for plantId (follow InfoTab pattern)
    - Load plant via usePlantsStore.getPlant(plantId)
    - Fallback to plant.photo for backward compatibility (convert to PlantPhoto array if photos undefined)
    - 3-column FlatList with numColumns={3}
    - Square thumbnails using Dimensions.get('window').width calculation
    - ITEM_SIZE = (SCREEN_WIDTH - 32) / 3 (16px padding)
    - 2px gap between thumbnails
    - Primary photo badge: star icon in top-right corner (absolute position, semi-transparent black background)

    renderItem:
    - TouchableOpacity for each thumbnail
    - Image with source={{ uri: item.uri }}
    - Conditional star badge if item.isPrimary
    - ActiveOpacity 0.9 for feedback

    Empty state:
    - Show when photos array is empty
    - Display centered AddPhotoButton
    - Show "No photos yet" text below button

    ListFooterComponent:
    - Always-visible AddPhotoButton inline with grid
    - Only show if photos.length > 0 (otherwise empty state covers it)

    Follow RESEARCH.md Pattern 3 for implementation details. Reference PlantGrid.tsx for FlatList with columns pattern.
  </action>
  <verify>
    grep -n "numColumns={3}" components/Detail/PhotoGallery.tsx — 3-column layout must exist
    grep -n "isPrimary" components/Detail/PhotoGallery.tsx — Badge logic must be present
    grep -n "onPhotoPress" components/Detail/PhotoGallery.tsx — Press handler must exist
    wc -l components/Detail/PhotoGallery.tsx — Component should be 100-150 lines
  </verify>
  <done>
    PhotoGallery component renders 3-column thumbnail grid
    Square thumbnails with 2px gaps
    Primary photo displays star badge indicator
    Empty state shows AddPhotoButton with text
    Tapping thumbnail triggers onPhotoPress callback (lightbox pending Plan 03)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **TypeScript compilation:** `npx tsc --noEmit` must pass
2. **i18n keys:** `jq .gallery i18n/resources/en.json` returns all 11 keys
3. **Component exports:** `grep -l "export.*PhotoGallery\|export.*AddPhotoButton" components/Detail/*.tsx` finds both files
4. **Grid layout:** `grep -c "numColumns={3}" components/Detail/PhotoGallery.tsx` returns 1

Test manually (after Plan 03 integration):
- Add photo via AddPhotoButton — should appear in grid
- Primary photo shows star badge
- Empty state displays when no photos
- Tapping thumbnail triggers onPhotoPress callback
</verification>

<success_criteria>
- AddPhotoButton component with dashed border styling
- PhotoGallery component with 3-column FlatList grid
- Square thumbnails with primary badge indicator
- Empty state handling with centered button
- i18n support in EN and IT for all gallery text
- Backward compatibility with plant.photo field
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-photo-gallery/05-02-SUMMARY.md`
</output>
