---
phase: 01-foundation-and-core-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - stores/plantsStore.ts
  - stores/onboardingStore.ts
  - stores/settingsStore.ts
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-06

must_haves:
  truths:
    - "Plant collection persists across app restarts"
    - "Onboarding completion flag persists"
    - "Language preference persists"
    - "Plants store provides CRUD operations"
  artifacts:
    - path: "stores/plantsStore.ts"
      provides: "Plant collection state with persistence"
      exports: ["usePlantsStore"]
    - path: "stores/onboardingStore.ts"
      provides: "Onboarding state tracking"
      exports: ["useOnboardingStore"]
    - path: "stores/settingsStore.ts"
      provides: "Settings persistence"
      exports: ["useSettingsStore"]
  key_links:
    - from: "stores/*.ts"
      to: "zustand"
      via: "npm install zustand"
      pattern: "import.*create.*from 'zustand'"
    - from: "stores/*.ts"
      to: "AsyncStorage"
      via: "persist middleware"
      pattern: "createJSONStorage"
---

<objective>
Set up Zustand stores with AsyncStorage persist for plant collection, onboarding, and settings.

Purpose: Data layer foundation - all screens will consume these stores.
Output: Three Zustand stores with automatic persistence
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-core-loop/01-RESEARCH.md
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create Zustand stores with AsyncStorage persistence</name>
  <files>stores/plantsStore.ts, stores/onboardingStore.ts, stores/settingsStore.ts, package.json</files>
  <action>
1. Install Zustand: npm install zustand

2. Create stores/plantsStore.ts with plants array, addPlant, removePlant, getPlant
3. Create stores/onboardingStore.ts with hasSeenOnboarding flag
4. Create stores/settingsStore.ts with language state
5. All stores use persist middleware with createJSONStorage(() => AsyncStorage)
6. Run: npx tsc --noEmit

Use pattern from RESEARCH.md Zustand section.
  </action>
  <verify>
npx tsc --noEmit succeeds
  </verify>
  <done>
Three stores created with persist middleware, TypeScript compiles
  </done>
</task>

</tasks>

<verification>
1. stores/ exists with 3 files
2. All use Zustand persist middleware
3. Plants store has addPlant, removePlant, getPlant
4. TypeScript compiles
</verification>

<success_criteria>
- [ ] Zustand installed
- [ ] Three stores with persist middleware
- [ ] Plants store CRUD complete
- [ ] TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-core-loop/01-03-SUMMARY.md`
</output>
