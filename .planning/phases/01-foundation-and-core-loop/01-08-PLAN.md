---
phase: 01-foundation-and-core-loop
plan: 08
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-07
files_modified:
  - app/results.tsx
  - components/Results/ResultsCarousel.tsx
  - components/Results/ResultCard.tsx
  - package.json
autonomous: true
requirements:
  - ID-04
  - ID-05
  - ID-06
  - ID-07
  - CARE-01
  - UI-04
  - LEGAL-01

must_haves:
  truths:
    - "Card carousel displays plant matches (swipeable)"
    - "Each card shows photo, name, confidence bar"
    - "Tap card to expand care info"
    - "Low confidence warning (<50%)"
    - "Add to collection button on card"
    - "PlantNet attribution visible"
    - "Care info from database or 'coming soon'"
  artifacts:
    - path: "app/results.tsx"
      provides: "Results screen with carousel"
      min_lines: 100
    - path: "components/Results/ResultsCarousel.tsx"
      provides: "Swipeable card carousel"
      exports: ["ResultsCarousel"]
      min_lines: 60
    - path: "components/Results/ResultCard.tsx"
      provides: "Plant result card with care info"
      exports: ["ResultCard"]
      min_lines: 100
  key_links:
    - from: "app/results.tsx"
      to: "services/plantnet.ts"
      via: "identifyPlant call with imageUri and organ"
      pattern: "identifyPlant"
    - from: "app/results.tsx"
      to: "services/cache.ts"
      via: "getCachedResult/setCachedResult"
      pattern: "CachedResult"
    - from: "components/Results/ResultCard.tsx"
      to: "services/careDB.ts"
      via: "getCareInfo(scientificName)"
      pattern: "getCareInfo"
    - from: "components/Results/ResultCard.tsx"
      to: "stores/plantsStore.ts"
      via: "addPlant call"
      pattern: "addPlant"
---

<objective>
Create results screen with card carousel, confidence visualization, care info expansion, and "Add to collection" button. User locks: card carousel, compact+expand care info, warn if confidence <50%, add button on card.

Purpose: Display identification results in swipeable cards with care info and collection save action.
Output: Results screen, ResultsCarousel, ResultCard with care info and save button.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-core-loop/01-CONTEXT.md
@.planning/phases/01-foundation-and-core-loop/01-RESEARCH.md
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Build results screen with carousel, care info, and save button</name>
  <files>app/results.tsx, components/Results/ResultsCarousel.tsx, components/Results/ResultCard.tsx, package.json</files>
  <action>
1. Install react-native-snap-carousel:
   npm install react-native-snap-carousel

2. Create app/results.tsx:
   - Get imageUri and organ from route params
   - Show loading state while calling identifyPlant()
   - Check cache first (getCachedResult)
   - If no cache, call API then save to cache
   - Pass results to ResultsCarousel
   - Handle error state
   - Show PlantNet attribution

3. Create components/Results/ResultsCarousel.tsx:
   - Use react-native-snap-carousel
   - Config: sliderWidth=window width, itemWidth=width-40
   - onSnapToItem callback for active index tracking
   - Render ResultCard for each item
   - Pagination dots indicator

4. Create components/Results/ResultCard.tsx:
   - Props: PlantNetResult, onAddToCollection
   - Compact view: photo, scientific name, common names, confidence bar
   - Confidence bar: colored (green>80%, yellow>50%, red<50%)
   - If score <50%: show warning banner
   - "View Care" button: expands care info section
   - Care info section: getCareInfo(scientificName)
   - If no care data: show "Care info coming soon"
   - "Add to Collection" button: calls addPlant with photo, names, care data
   - Use i18n for all text

5. Confidence bar implementation:
   - View with width based on score * 100%
   - Color based on thresholds
   - Animated width change

Per CONTEXT.md: "Card carousel layout", "compact info with expand", "warn if confidence <50%", "add button on card".
  </action>
  <verify>
npx tsc --noEmit succeeds; ResultsCarousel uses snap-carousel; ResultCard shows confidence bar and care info
  </verify>
  <done>
Results screen with carousel, confidence visualization, care expand, low confidence warning, add to collection button
  </done>
</task>

</tasks>

<verification>
1. app/results.tsx calls identifyPlant with imageUri and organ
2. Cache checked before API call
3. ResultsCarousel uses react-native-snap-carousel
4. ResultCard shows photo, names, confidence bar
5. Confidence bar colored by score (>80% green, >50% yellow, <50% red)
6. Warning shown when score <50%
7. "View Care" expands care section
8. getCareInfo called for care data
9. "Add to Collection" button calls addPlant
10. PlantNet attribution visible
11. TypeScript compiles
</verification>

<success_criteria>
- [ ] Card carousel swipeable
- [ ] Confidence bar with color coding
- [ ] Low confidence warning (<50%)
- [ ] Care info expandable
- [ ] Add to collection button on card
- [ ] PlantNet attribution
- [ ] TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-core-loop/01-08-SUMMARY.md`
</output>
