---
phase: 01-foundation-and-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - services/cache.ts
  - services/rateLimiter.ts
  - services/plantnet.ts
autonomous: true
requirements:
  - ID-05
  - ID-06
  - RATE-01
  - RATE-03
  - RATE-04
  - RATE-05
  - LEGAL-03

must_haves:
  truths:
    - Cache uses SHA-256 hashing (collision-resistant)
    - Cache has LRU cap of 100 entries max
    - Cache entries expire after 7 days
    - Rate limiter uses mutex to prevent race conditions
    - Rate limiter resets at user's local midnight
    - API calls route through Cloudflare Workers proxy (not direct PlantNet)
  artifacts:
    - path: "services/cache.ts"
      provides: "LRU cache with SHA-256 hashing"
      contains: "LRU"
      min_lines: 50
    - path: "services/rateLimiter.ts"
      provides: "Mutex-protected rate limiting"
      contains: "Mutex"
      min_lines: 60
    - path: "services/plantnet.ts"
      provides: "API proxy integration"
      contains: "PROXY_URL"
      min_lines: 80
    - path: "package.json"
      provides: "New dependencies"
      contains: "lru-cache"
  key_links:
    - from: "services/plantnet.ts"
      to: "services/cache.ts"
      via: "getCachedResult/setCachedResult calls"
      pattern: "getCachedResult|setCachedResult"
    - from: "services/plantnet.ts"
      to: "services/rateLimiter.ts"
      via: "canIdentify/incrementIdentificationCount calls"
      pattern: "canIdentify|incrementIdentificationCount"
    - from: "services/cache.ts"
      to: "lru-cache"
      via: "import LRU from 'lru-cache'"
      pattern: "import.*lru-cache"
    - from: "services/rateLimiter.ts"
      to: "async-mutex"
      via: "import { Mutex } from 'async-mutex'"
      pattern: "import.*async-mutex"
---

<objective>
Fix critical service bugs: replace vulnerable cache with LRU library, fix rate limiter race conditions with mutex, and secure API key via Cloudflare Workers proxy. These fixes must happen before any UI work because the existing bugs cause hash collisions (wrong results), unlimited memory growth (crashes), and API key exposure (security violation).

Purpose: Prevent data corruption, app crashes, and security breaches before building user-facing features.
Output: Three hardened service modules with proper caching, atomic rate limiting, and secure API proxy routing.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-core-loop/01-RESEARCH.md
@services/cache.ts
@services/rateLimiter.ts
@services/plantnet.ts
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cache with LRU library and SHA-256 hashing</name>
  <files>services/cache.ts, package.json</files>
  <action>
1. Install lru-cache and expo-crypto:
   npm install lru-cache && npm install -D @types/lru-cache

2. Completely rewrite services/cache.ts:
   - Import LRU from 'lru-cache' and * as Crypto from 'expo-crypto'
   - Create LRU instance: max=100 entries, ttl=7 days
   - Replace hashString() with hashImage() using Crypto.digestStringAsync(Crypto.CryptoDigestAlgorithm.SHA256, imageUri)
   - Implement getCachedResult(): get hash, return cache.get(hash) || null
   - Implement setCachedResult(): set cache.set(hash, result)
   - Remove all AsyncStorage operations (LRU cache is in-memory, auto-evicts)
   - Keep clearCache() for testing: cache.clear()
   - Keep getCacheSize(): return cache.size

Reference existing code structure but DO NOT keep the vulnerable hashString() or AsyncStorage implementation.
  </action>
  <verify>
npm run build (or tsc --noEmit) passes without type errors
  </verify>
  <done>
LRU cache initialized with max=100, ttl=7days; hashImage() uses SHA-256; all functions use LRU instead of AsyncStorage
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix rate limiter race conditions with mutex</name>
  <files>services/rateLimiter.ts, package.json</files>
  <action>
1. Install async-mutex:
   npm install async-mutex

2. Rewrite services/rateLimiter.ts to fix race conditions:
   - Import { Mutex } from 'async-mutex'
   - Create mutex: const mutex = new Mutex()
   - Fix getTodayString() to use LOCAL timezone: new Date(now.getFullYear(), now.getMonth(), now.getDate()).toISOString().split('T')[0] (not new Date().toISOString() which gives UTC)
   - Wrap canIdentify() body in mutex.runExclusive(async () => { ... })
   - Wrap incrementIdentificationCount() body in mutex.runExclusive(async () => { ... })
   - Keep same function signatures and return types
   - Keep resetRateLimit() for testing

This ensures read-modify-write cycles are atomic—two concurrent API calls cannot both pass the limit check.
  </action>
  <verify>
npm run build (or tsc --noEmit) passes without type errors
  </verify>
  <done>
canIdentify() and incrementIdentificationCount() wrapped in mutex.runExclusive(); getTodayString() uses local timezone
  </done>
</task>

<task type="auto">
  <name>Task 3: Route API calls through Cloudflare Workers proxy</name>
  <files>services/plantnet.ts</files>
  <action>
1. Add proxy URL constant at top of services/plantnet.ts:
   const PROXY_URL = __DEV__
     ? process.env.EXPO_PUBLIC_PLANTNET_API_URL || 'https://my-api.plantnet.org/v2/identify/all'
     : 'https://plantid-api-proxy.YOUR_SUBDOMAIN.workers.dev';

2. Update identifyPlant() function:
   - Replace PLANTNET_API_URL with PROXY_URL
   - Keep EXPO_PUBLIC_PLANTNET_API_KEY check for development only
   - Remove 'api-key' from URL query params (proxy adds it)
   - Update comment to explain: "Production uses Cloudflare Workers proxy to hide API key"
   - Keep all other logic (FormData, organ, lang, error handling)

3. Add comment explaining Cloudflare Workers deployment (this task does NOT deploy it—user must do that manually):
   // Cloudflare Workers setup required:
   // 1. npm install -g wrangler
   // 2. Create worker.js with proxy logic (see RESEARCH.md)
   // 3. wrangler deploy
   // 4. Update PROXY_URL above with deployed URL

DO NOT remove the existing API key check—dev builds still need it.
  </action>
  <verify>
npm run build (or tsc --noEmit) passes; grep -n "api-key" services/plantnet.ts shows no query param usage
  </verify>
  <done>
identifyPlant() uses PROXY_URL; 'api-key' removed from query params; comment explains Cloudflare Workers deployment
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. LRU cache instance created with max=100, ttl=604800000 (7 days)
3. hashImage() uses Crypto.digestStringAsync with SHA-256
4. Rate limiter uses mutex.runExclusive() for atomic operations
5. getTodayString() constructs local date (not UTC)
6. PROXY_URL constant exists and is used in identifyPlant()
7. No 'api-key' query parameter in production code path
</verification>

<success_criteria>
- Cache is collision-resistant (SHA-256) and memory-safe (LRU cap)
- Rate limiter is race-condition-free (mutex) and timezone-aware (local midnight)
- API key is not exposed in client bundle (proxy routing)
- All existing tests (if any) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-core-loop/01-01-SUMMARY.md`
</output>
