---
phase: 01-foundation-and-core-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - i18n/index.ts
  - i18n/resources/en.json
  - i18n/resources/it.json
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-04

must_haves:
  truths:
    - i18next configured with IT and EN languages
    - App auto-detects device language on first launch
    - User can switch language from Settings
    - Language preference persists across app restarts
    - PlantNet API calls use selected language
    - Fallback to English if translation missing
  artifacts:
    - path: "i18n/index.ts"
      provides: "i18next config with AsyncStorage persistence"
      exports: ["initI18n", "changeLanguage", "t"]
      min_lines: 40
    - path: "i18n/resources/en.json"
      provides: "English translations"
      contains: '"common"'
      min_lines: 100
    - path: "i18n/resources/it.json"
      provides: "Italian translations"
      contains: '"common"'
      min_lines: 100
    - path: "package.json"
      provides: "i18next dependencies"
      contains: "i18next"
  key_links:
    - from: "app/_layout.tsx"
      to: "i18n/index.ts"
      via: "import initI18n and call in root layout"
      pattern: "initI18n.*init"
    - from: "Any screen/component"
      to: "i18n/index.ts"
      via: "import { t } from 'react-i18next'"
      pattern: "useTranslation|import.*t.*from.*i18next"
    - from: "services/plantnet.ts"
      to: "i18n"
      via: "Pass i18n.language to identifyPlant() lang param"
      pattern: "identifyPlant.*lang.*i18n"
---

<objective>
Set up i18next internationalization with Italian and English translations, device language auto-detection, and AsyncStorage persistence for language preference. All user-facing text must use translations—no hardcoded strings in UI.

Purpose: Support bilingual users (IT/EN) from day one, with extensible architecture for future languages.
Output: i18next configured, translation files with all UI text, language detection and persistence working.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-core-loop/01-RESEARCH.md
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install i18next dependencies and create translation files</name>
  <files>package.json, i18n/resources/en.json, i18n/resources/it.json</files>
  <action>
1. Install i18next packages:
   npm install i18next react-i18next expo-localization

2. Create i18n/resources/en.json with English translations:
   {
     "common": {
       "appName": "Plantid",
       "ok": "OK",
       "cancel": "Cancel",
       "save": "Save",
       "delete": "Delete",
       "retry": "Retry",
       "loading": "Loading...",
       "error": "Error",
       "close": "Close"
     },
     "camera": {
       "title": "Identify Plant",
       "takePhoto": "Take Photo",
       "fromGallery": "From Gallery",
       "selectOrgan": "Select Plant Part",
       "organ_leaf": "Leaf",
       "organ_flower": "Flower",
       "organ_fruit": "Fruit",
       "organ_bark": "Bark",
       "organ_auto": "Auto-detect",
       "retake": "Retake",
       "usePhoto": "Use Photo"
     },
     "results": {
       "title": "Identification Results",
       "confidence": "Confidence",
       "scientificName": "Scientific Name",
       "family": "Family",
       "commonNames": "Common Names",
       "viewCare": "View Care Info",
       "addToCollection": "Add to Collection",
       "lowConfidenceWarning": "Low confidence result. Try a clearer photo for better accuracy.",
       "careComingSoon": "Care info coming soon for this species."
     },
     "collection": {
       "title": "My Plants",
       "empty": "You haven't saved any plants yet.",
       "emptyCTA": "Identify your first plant!",
       "gridView": "Grid View",
       "listView": "List View",
       "plantAdded": "Plant added to collection",
       "plantDeleted": "Plant removed from collection"
     },
     "detail": {
       "title": "Plant Details",
       "addedOn": "Added on {{date}}",
       "location": "Location",
       "nickname": "Nickname",
       "notes": "Notes",
       "watering": "Watering",
       "sunlight": "Sunlight",
       "temperature": "Temperature",
       "soil": "Soil",
       "difficulty": "Difficulty",
       "toxicPets": "Toxic to Pets"
     },
     "settings": {
       "title": "Settings",
       "language": "Language",
       "about": "About",
       "privacy": "Privacy Policy",
       "attribution": "Powered by Pl@ntNet"
     },
     "rateLimit": {
       "title": "Daily Limit Reached",
       "message": "You've used all {{limit}} identifications for today. Come back tomorrow!",
       "remaining": "{{remaining}} remaining today"
     },
     "errors": {
       "networkError": "Network error. Please check your connection.",
       "cameraPermission": "Camera permission is required to take photos.",
       "apiError": "Unable to identify plant. Please try again.",
       "invalidImage": "Unable to process this image."
     }
   }

3. Create i18n/resources/it.json with Italian translations (match keys exactly, translate values):
   - Translate all English values to Italian
   - Keep {{variable}} placeholders unchanged
   - Use formal "Lei" form where appropriate (Italian app convention)
   - "Identify Plant" → "Identifica Pianta"
   - "My Plants" → "Le Mie Piante"
   - "Powered by Pl@ntNet" → "Grazie a Pl@ntNet"
   - And so on for all keys

DO NOT change key names—only translate values.
  </action>
  <verify>
node -e "JSON.parse(require('fs').readFileSync('i18n/resources/en.json'))" && node -e "JSON.parse(require('fs').readFileSync('i18n/resources/it.json'))" (both files are valid JSON)
  </verify>
  <done>
i18n/resources/en.json and it.json created with matching keys, all UI text translated
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure i18next with AsyncStorage persistence</name>
  <files>i18n/index.ts</files>
  <action>
Create i18n/index.ts:

import * as Localization from 'expo-localization';
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import { getLocales } from 'expo-localization';
import AsyncStorage from '@react-native-async-storage/async-storage';
import en from './resources/en.json';
import it from './resources/it.json';

const LANGUAGE_KEY = '@plantid_language';

export const initI18n = async () => {
  // Get saved language or detect device language
  let savedLang = await AsyncStorage.getItem(LANGUAGE_KEY);

  if (!savedLang) {
    const deviceLang = getLocales()[0]?.languageCode || 'en';
    savedLang = ['en', 'it'].includes(deviceLang) ? deviceLang : 'en';
  }

  await i18n.use(initReactI18next).init({
    resources: {
      en: { translation: en },
      it: { translation: it },
    },
    lng: savedLang,
    fallbackLng: 'en',
    compatibilityJSON: 'v3',
    interpolation: {
      escapeValue: false, // React Native already escapes
    },
  });

  return i18n;
};

export const changeLanguage = async (lang: string) => {
  await i18n.changeLanguage(lang);
  await AsyncStorage.setItem(LANGUAGE_KEY, lang);
};

export const getCurrentLanguage = (): string => {
  return i18n.language;
};

export default initI18n;

Export initI18n, changeLanguage, getCurrentLanguage.
  </action>
  <verify>
grep -q "export.*initI18n" i18n/index.ts && grep -q "export.*changeLanguage" i18n/index.ts && grep -q "fallbackLng" i18n/index.ts
  </verify>
  <done>
initI18n() loads saved lang or detects device lang, changeLanguage() persists to AsyncStorage, fallback to 'en'
  </done>
</task>

</tasks>

<verification>
1. i18next, react-i18next, expo-localization in package.json
2. i18n/resources/en.json has all translation keys
3. i18n/resources/it.json has matching keys with Italian values
4. i18n/index.ts exports initI18n, changeLanguage, getCurrentLanguage
5. initI18n() reads from AsyncStorage with key '@plantid_language'
6. Fallback language is 'en'
7. No hardcoded UI strings in configuration files
</verification>

<success_criteria>
- i18next initialized with IT and EN resources
- Device language auto-detected on first launch
- Language changes persist to AsyncStorage
- Fallback to English if translation missing
- Translation files cover all UI text for Phase 1 screens
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-core-loop/01-02-SUMMARY.md`
</output>
