---
phase: 01-foundation-and-core-loop
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - services/careDB.ts
autonomous: true
requirements:
  - CARE-01
  - CARE-02
  - CARE-03
  - CARE-04
  - CARE-05

must_haves:
  truths:
    - "Care database has 100 common species"
    - "Each species has structured care data (water, sun, temp, soil, toxicity)"
    - "getCareInfo() returns null for unknown species"
    - "Care info includes bilingual tips (IT/EN)"
  artifacts:
    - path: "services/careDB.ts"
      provides: "Plant care database lookup"
      exports: ["getCareInfo", "CARE_DB"]
      min_lines: 200
  key_links:
    - from: "Results screen"
      to: "services/careDB.ts"
      via: "getCareInfo(scientificName) call"
      pattern: "getCareInfo"
---

<objective>
Create care database with 100 common plant species and structured care information.

Purpose: Display species-specific care guidance on identification results.
Output: services/careDB.ts with CARE_DATA array and getCareInfo() lookup function
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-core-loop/01-RESEARCH.md
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create care database with 100 species and lookup function</name>
  <files>services/careDB.ts</files>
  <action>
Create services/careDB.ts with:

1. Import PlantCareInfo type from @/types

2. Create CARE_DATA array with 100 common species (examples to include):
   - Monstera deliciosa
   - Ficus lyrata
   - Sansevieria trifasciata
   - Spathiphyllum
   - Dracaena marginata
   - [95 more common indoor plants]

3. Each entry needs:
   - id, commonName, scientificName
   - waterFrequencyDays (number)
   - sunlight ('full-sun' | 'partial-sun' | 'shade' | 'low-light')
   - tempMin, tempMax (Celsius)
   - soil (optional string)
   - humidity ('low' | 'medium' | 'high')
   - difficulty ('easy' | 'medium' | 'hard')
   - toxicPets (boolean)
   - tips: { it: string, en: string }

4. Export getCareInfo() function:
   ```typescript
   export function getCareInfo(scientificName: string): PlantCareInfo | null {
     return CARE_DATA.find(plant => 
       plant.scientificName.toLowerCase() === scientificName.toLowerCase()
     ) || null;
   }
   ```

5. Export CARE_DATA for testing

Use Claude to generate the 100 species data - this is data entry work.
  </action>
  <verify>
npx tsc --noEmit succeeds; grep -c "id:" services/careDB.ts returns at least 100
  </verify>
  <done>
CARE_DATA has 100+ entries, getCareInfo() lookup works, TypeScript compiles
  </done>
</task>

</tasks>

<verification>
1. CARE_DATA array has 100+ species
2. Each species has all required PlantCareInfo fields
3. getCareInfo() returns PlantCareInfo | null
4. Tips are bilingual (it/en)
5. TypeScript compiles
</verification>

<success_criteria>
- [ ] 100+ species in CARE_DATA
- [ ] All required fields present
- [ ] getCareInfo() lookup by scientificName
- [ ] Returns null for unknown species
- [ ] Bilingual tips included
- [ ] TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-core-loop/01-04-SUMMARY.md`
</output>
