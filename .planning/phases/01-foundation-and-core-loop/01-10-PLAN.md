---
phase: 01-foundation-and-core-loop
plan: 10
type: execute
wave: 2
depends_on:
  - 01-03
  - 01-04
  - 01-09
files_modified:
  - app/plant/[id].tsx
  - components/Detail/CareInfo.tsx
autonomous: true
requirements:
  - COLL-04
  - COLL-05
  - CARE-01
  - UI-04

must_haves:
  truths:
    - "Plant detail shows full info (photo, names, care, notes)"
    - "Care info from care database or 'coming soon'"
    - "Delete button removes plant from collection"
    - "Edit notes functionality"
  artifacts:
    - path: "app/plant/[id].tsx"
      provides: "Plant detail screen"
      min_lines: 120
    - path: "components/Detail/CareInfo.tsx"
      provides: "Care info display component"
      exports: ["CareInfo"]
      min_lines: 80
  key_links:
    - from: "app/plant/[id].tsx"
      to: "stores/plantsStore.ts"
      via: "getPlant(id) and removePlant(id)"
      pattern: "getPlant|removePlant"
    - from: "app/plant/[id].tsx"
      to: "services/careDB.ts"
      via: "getCareInfo(scientificName)"
      pattern: "getCareInfo"
    - from: "components/Detail/CareInfo.tsx"
      to: "types/index.ts"
      via: "PlantCareInfo type usage"
      pattern: "PlantCareInfo"
---

<objective>
Create plant detail screen with full plant info, care details, notes editing, and delete functionality.

Purpose: View and manage saved plant details.
Output: Plant detail screen with care info display, editable notes, delete button.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-core-loop/01-CONTEXT.md
@.planning/phases/01-foundation-and-core-loop/01-RESEARCH.md
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Build plant detail screen with care info and delete</name>
  <files>app/plant/[id].tsx, components/Detail/CareInfo.tsx</files>
  <action>
1. Create app/plant/[id].tsx (Expo Router dynamic route):
   - Get id from useLocalSearchParams()
   - Load plant from usePlantsStore().getPlant(id)
   - If not found: show error or redirect
   - Display:
     * Photo (large)
     * Scientific name + common names
     * Added date
     * Location/nickname (editable via state)
     * Notes (multiline TextInput)
     * Care section (via CareInfo component)
     * Delete button (with confirmation)
   - Save edits: call updatePlant(id, updates) on blur/submit
   - Delete: show confirmation modal, then removePlant(id), navigate back

2. Create components/Detail/CareInfo.tsx:
   - Props: PlantCareInfo | null
   - If null: show "Care info coming soon for this species"
   - If present: display structured care data
     * Watering: "Every X days"
     * Sunlight: icon + label (full-sun/partial-sun/shade/low-light)
     * Temperature: "Min°C - Max°C"
     * Soil: if available
     * Humidity: if available
     * Difficulty: easy/medium/hard with visual indicator
     * Toxic to pets: warning icon if true
     * Tips: localized tip (use i18n.language to choose it/en)
   - Use icons for visual appeal (Ionicons)

3. Use i18n for labels and empty state message
4. Style with user's discretion

Note: Watering history is Phase 2, omit for now.
  </action>
  <verify>
npx tsc --noEmit succeeds; plant/[id].tsx gets plant from store; CareInfo displays PlantCareInfo fields; delete calls removePlant
  </verify>
  <done>
Detail screen with photo, names, care info, editable notes, delete button; care from database or "coming soon"
  </done>
</task>

</tasks>

<verification>
1. app/plant/[id].tsx uses useLocalSearchParams for id
2. Plant loaded via usePlantsStore().getPlant(id)
3. CareInfo component displays care fields
4. "Care info coming soon" shown when getCareInfo returns null
5. Delete button confirmation before removePlant
6. Notes editable via TextInput
7. Care info includes water, sun, temp, soil, toxicity
8. Tips localized (it/en)
9. TypeScript compiles
</verification>

<success_criteria>
- [ ] Plant loaded from store by id
- [ ] Full care info displayed
- [ ] "Coming soon" fallback
- [ ] Notes editable
- [ ] Delete with confirmation
- [ ] TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-core-loop/01-10-SUMMARY.md`
</output>
