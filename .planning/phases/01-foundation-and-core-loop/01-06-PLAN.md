---
phase: 01-foundation-and-core-loop
plan: 06
type: execute
wave: 2
depends_on:
  - 01-03
  - 01-02
files_modified:
  - components/Onboarding.tsx
  - app/(tabs)/_layout.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Onboarding shows on first launch only"
    - "2-3 onboarding screens with swipe navigation"
    - "Onboarding completion persists"
    - "Home screen shows after onboarding"
  artifacts:
    - path: "components/Onboarding.tsx"
      provides: "Onboarding carousel with 2-3 screens"
      min_lines: 150
    - path: "app/(tabs)/index.tsx"
      provides: "Home screen with onboarding check"
      contains: "useOnboardingStore"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "components/Onboarding.tsx"
      via: "Conditional render if !hasSeenOnboarding"
      pattern: "hasSeenOnboarding"
    - from: "components/Onboarding.tsx"
      to: "stores/onboardingStore.ts"
      via: "setOnboardingComplete call"
      pattern: "setOnboardingComplete"
---

<objective>
Create onboarding flow with 2-3 screens explaining key features, shown only on first launch.

Purpose: Welcome new users and explain core features (camera, identification, collection).
Output: Onboarding component with swipeable screens and completion tracking
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-core-loop/01-RESEARCH.md
@.planning/phases/01-foundation-and-core-loop/01-03-PLAN.md
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create onboarding screens with first-launch detection</name>
  <files>components/Onboarding.tsx, app/(tabs)/index.tsx</files>
  <action>
1. Create components/Onboarding.tsx:
   ```typescript
   import { View, Text, StyleSheet, Dimensions } from 'react-native';
   import { SafeAreaView } from 'react-native-safe-area-context';
   import Swiper from 'react-native-swiper'; // or manual ScrollView
   import { useOnboardingStore } from '@/stores/onboardingStore';
   import { useTranslation } from 'react-i18next';

   const Onboarding = () => {
     const { setOnboardingComplete } = useOnboardingStore();
     const { t } = useTranslation();

     const screens = [
       {
         title: t('onboarding.screen1.title'),
         description: t('onboarding.screen1.description'),
         icon: '🌱',
       },
       {
         title: t('onboarding.screen2.title'),
         description: t('onboarding.screen2.description'),
         icon: '📷',
       },
       {
         title: t('onboarding.screen3.title'),
         description: t('onboarding.screen3.description'),
         icon: '🌿',
       },
     ];

     return (
       <Swiper loop={false} onIndexChanged={(index) => {
         if (index === screens.length - 1) {
           setOnboardingComplete();
         }
       }}>
         {screens.map((screen, i) => (
           <View key={i} style={styles.screen}>
             <Text style={styles.icon}>{screen.icon}</Text>
             <Text style={styles.title}>{screen.title}</Text>
             <Text style={styles.description}>{screen.description}</Text>
             {i === screens.length - 1 && (
               <Button title={t('onboarding.getStarted')} onPress={() => {
                 setOnboardingComplete();
               }} />
             )}
           </View>
         ))}
       </Swiper>
     );
   };
   ```

2. Add onboarding translations to i18n/resources/en.json and it.json:
   - onboarding.screen1.title/description
   - onboarding.screen2.title/description
   - onboarding.screen3.title/description
   - onboarding.getStarted

3. Update app/(tabs)/index.tsx (Home screen):
   ```typescript
   import { useOnboardingStore } from '@/stores/onboardingStore';
   import Onboarding from '@/components/Onboarding';

   export default function HomeScreen() {
     const hasSeenOnboarding = useOnboardingStore(state => state.hasSeenOnboarding);

     if (!hasSeenOnboarding) {
       return <Onboarding />;
     }

     // Rest of home screen (collection grid)
   }
   ```

4. Style with appealing visual design (user's discretion on exact styling)

Note: May need to install react-native-swiper or use react-native ScrollView with paging.
  </action>
  <verify>
npx tsc --noEmit succeeds; Onboarding component uses useOnboardingStore; Home checks hasSeenOnboarding
  </verify>
  <done>
Onboarding component created, 3 screens defined, shows on first launch, completion persists
  </done>
</task>

</tasks>

<verification>
1. Onboarding component created with 2-3 screens
2. Uses useOnboardingStore
3. Calls setOnboardingComplete on finish
4. Home screen conditionally renders Onboarding
5. Translations added for onboarding text
6. TypeScript compiles
</verification>

<success_criteria>
- [ ] 2-3 onboarding screens
- [ ] Swipeable navigation
- [ ] Shows on first launch only
- [ ] Completion persists
- [ ] Translations for all text
- [ ] TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-core-loop/01-06-SUMMARY.md`
</output>
