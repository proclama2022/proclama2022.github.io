---
phase: 04-tabbed-layout-and-content-reorganization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - types/index.ts
  - i18n/resources/en.json
  - i18n/resources/it.json
autonomous: true
requirements:
  - TAB-01
  - TAB-02
  - TAB-03
  - TAB-04
  - TAB-05
  - CARE-01
  - CARE-02
  - CARE-03
  - CARE-04
  - CARE-05
  - CARE-06
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - NOTE-04
  - NOTE-05
  - NOTE-06
  - NOTE-07

must_haves:
  truths:
    - "New npm packages are installed and importable without errors"
    - "Extended PlantCareInfo type compiles with new optional fields (seasonalTemps, fertilization, pruning, pests)"
    - "Extended SavedPlant type compiles with 4 new optional metadata fields (purchaseDate, purchasePrice, purchaseOrigin, giftFrom)"
    - "All new i18n keys exist in both en.json and it.json without raw key strings rendering"
    - "Existing types/consumers are unbroken — all optional fields so no migration needed"
    - "All Phase 4 tab components can import new types and i18n keys without raw key strings appearing in the UI"
  artifacts:
    - path: "types/index.ts"
      provides: "SeasonalTemp, FertilizationInfo, PruningInfo, PestEntry interfaces + extended PlantCareInfo and SavedPlant"
      contains: "SeasonalTemp"
    - path: "i18n/resources/en.json"
      provides: "Tab labels, care section labels, notes metadata labels in English"
      contains: "detail.tabs.info"
    - path: "i18n/resources/it.json"
      provides: "Tab labels, care section labels, notes metadata labels in Italian"
      contains: "detail.tabs.info"
  key_links:
    - from: "types/index.ts"
      to: "components/Detail/CareTab.tsx"
      via: "PlantCareInfo extended type import"
      pattern: "SeasonalTemp|FertilizationInfo|PruningInfo|PestEntry"
    - from: "types/index.ts"
      to: "stores/plantsStore.ts"
      via: "SavedPlant extended type — additive fields, no store changes needed"
      pattern: "purchaseDate|purchasePrice|purchaseOrigin|giftFrom"
---

<objective>
Install the two new npm dependencies required for tabbed navigation and extend the TypeScript types and i18n translation files to support all new Phase 4 features.

Purpose: Establishes the type contracts and translation keys all subsequent plans depend on. All new fields on existing types are optional — zero breaking changes to existing code.
Output: Installed packages, extended types/index.ts with 4 new interfaces + extended PlantCareInfo and SavedPlant, complete i18n keys for tabs/care/notes in EN and IT.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tabbed-layout-and-content-reorganization/04-CONTEXT.md
@.planning/phases/04-tabbed-layout-and-content-reorganization/04-RESEARCH.md
@types/index.ts
@i18n/resources/en.json
@i18n/resources/it.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install tab navigation packages</name>
  <files>package.json</files>
  <action>
Run the following install command in the project root:

```
npx expo install @react-navigation/material-top-tabs react-native-pager-view
```

This installs @react-navigation/material-top-tabs@7.4.13 and react-native-pager-view using the Expo-managed install path (ensures version compatibility with the installed Expo SDK). Do NOT use plain `npm install` — expo install resolves correct peer-compatible versions.

Do NOT install react-native-gesture-handler — it is not needed for material-top-tabs v7 with the pager-view backend and is not in this project's dependency tree.
  </action>
  <verify>
    <automated>cd "/Users/martha2022/Documents/Claude code/Plantid" && node -e "const p = require('./package.json'); console.log('@react-navigation/material-top-tabs:', p.dependencies['@react-navigation/material-top-tabs'] || p.dependencies['@react-navigation/material-top-tabs']); console.log('react-native-pager-view:', p.dependencies['react-native-pager-view'])"</automated>
    <manual>Both package names are present in package.json dependencies</manual>
  </verify>
  <done>package.json contains both @react-navigation/material-top-tabs and react-native-pager-view entries</done>
</task>

<task type="auto">
  <name>Task 2: Extend TypeScript types for Phase 4 features</name>
  <files>types/index.ts</files>
  <action>
Add the following new interfaces and extend the existing PlantCareInfo and SavedPlant interfaces. ALL new fields are optional (?) to ensure zero breaking changes.

**New interfaces to add after the existing PlantCareInfo interface:**

```typescript
export interface SeasonalTemp {
  season: 'spring' | 'summer' | 'autumn' | 'winter';
  minTemp: number; // Celsius
  maxTemp: number; // Celsius
}

export interface FertilizationInfo {
  schedule: { it: string; en: string }; // e.g. "Every 2 weeks in spring/summer"
  type: { it: string; en: string };      // e.g. "Balanced liquid fertilizer"
}

export interface PruningInfo {
  when: { it: string; en: string };
  how: { it: string; en: string };
}

export interface PestEntry {
  name: { it: string; en: string };
  description: { it: string; en: string }; // Brief visible description
  remedy: { it: string; en: string };       // Revealed on expand
}
```

**Extend PlantCareInfo** (add these optional fields inside the existing interface, after the existing `aliases?` field):

```typescript
  seasonalTemps?: SeasonalTemp[];
  fertilization?: FertilizationInfo;
  pruning?: PruningInfo;
  pests?: PestEntry[];
```

**Extend SavedPlant** (add these optional fields inside the existing interface, after the existing `notes?` field):

```typescript
  purchaseDate?: string;    // ISO date string or free text, user-entered
  purchasePrice?: string;   // Free text (e.g. "€12.50")
  purchaseOrigin?: string;  // Free text (e.g. "IKEA Milano")
  giftFrom?: string;        // Free text (e.g. "Grandma")
```
  </action>
  <verify>
    <automated>cd "/Users/martha2022/Documents/Claude code/Plantid" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>TypeScript compiles with no new errors; grep for SeasonalTemp in types/index.ts returns a result</manual>
  </verify>
  <done>npx tsc --noEmit exits 0; types/index.ts contains SeasonalTemp, FertilizationInfo, PruningInfo, PestEntry interfaces and both PlantCareInfo and SavedPlant have the new optional fields</done>
</task>

<task type="auto">
  <name>Task 3: Add i18n translation keys for tabs, care sections, and notes metadata</name>
  <files>i18n/resources/en.json, i18n/resources/it.json</files>
  <action>
Add the following translation keys to both en.json and it.json. Insert them inside the existing `"detail"` object, after the existing keys.

**en.json additions inside "detail":**

```json
"tabs": {
  "info": "Info",
  "care": "Care",
  "history": "History",
  "notes": "Notes"
},
"care": {
  "watering": "Watering",
  "light": "Light",
  "temperature": "Temperature",
  "seasonalTemps": "Seasonal Temperatures",
  "fertilization": "Fertilization",
  "pruning": "Pruning",
  "pests": "Pests & Remedies",
  "notAvailable": "Not available for this species",
  "tapToExpand": "Tap for remedy",
  "spring": "Spring",
  "summer": "Summer",
  "autumn": "Autumn",
  "winter": "Winter",
  "tempRange": "{{min}}–{{max}}°C"
},
"notes": {
  "placeholder": "Add notes about this plant...",
  "saved": "Saved ✓",
  "charsRemaining": "{{count}} remaining",
  "purchaseDate": "Purchase Date",
  "purchasePrice": "Price",
  "purchaseOrigin": "Where Purchased",
  "giftFrom": "Gift From",
  "metadataSection": "Additional Details"
},
"history": {
  "comingSoon": "History coming soon",
  "comingSoonDetail": "Watering history and growth tracking will appear here."
}
```

**it.json additions inside "detail"** (same structure, Italian values):

```json
"tabs": {
  "info": "Info",
  "care": "Cura",
  "history": "Cronologia",
  "notes": "Note"
},
"care": {
  "watering": "Annaffiatura",
  "light": "Luce",
  "temperature": "Temperatura",
  "seasonalTemps": "Temperature Stagionali",
  "fertilization": "Fertilizzazione",
  "pruning": "Potatura",
  "pests": "Parassiti e Rimedi",
  "notAvailable": "Non disponibile per questa specie",
  "tapToExpand": "Tocca per il rimedio",
  "spring": "Primavera",
  "summer": "Estate",
  "autumn": "Autunno",
  "winter": "Inverno",
  "tempRange": "{{min}}–{{max}}°C"
},
"notes": {
  "placeholder": "Aggiungi note su questa pianta...",
  "saved": "Salvato ✓",
  "charsRemaining": "{{count}} rimanenti",
  "purchaseDate": "Data d'Acquisto",
  "purchasePrice": "Prezzo",
  "purchaseOrigin": "Luogo d'Acquisto",
  "giftFrom": "Regalo da",
  "metadataSection": "Dettagli Aggiuntivi"
},
"history": {
  "comingSoon": "Cronologia in arrivo",
  "comingSoonDetail": "La cronologia dell'annaffiatura e il monitoraggio della crescita appariranno qui."
}
```

IMPORTANT: Merge these into the existing "detail" object — do not create a second "detail" key. The existing keys (title, addedOn, location, nickname, notes, watering, sunlight, temperature, soil, difficulty, toxicPets) must remain unchanged.
  </action>
  <verify>
    <automated>cd "/Users/martha2022/Documents/Claude code/Plantid" && node -e "const en = require('./i18n/resources/en.json'); const it = require('./i18n/resources/it.json'); console.log('EN tabs.info:', en.detail.tabs.info); console.log('IT tabs.care:', it.detail.tabs.care); console.log('EN care.pests:', en.detail.care.pests); console.log('IT notes.saved:', it.detail.notes.saved);"</automated>
    <manual>All 4 sections (tabs, care, notes, history) present in both EN and IT; existing detail keys still present</manual>
  </verify>
  <done>Both JSON files parse without error, contain detail.tabs, detail.care, detail.notes, detail.history keys in both languages, and existing detail keys are intact</done>
</task>

</tasks>

<verification>
Run TypeScript check: `cd "/Users/martha2022/Documents/Claude code/Plantid" && npx tsc --noEmit`
Verify packages installed: check package.json for @react-navigation/material-top-tabs and react-native-pager-view
Verify i18n keys: `node -e "const e=require('./i18n/resources/en.json'); console.log(JSON.stringify(e.detail.tabs))"`
</verification>

<success_criteria>
- npx tsc --noEmit exits 0 (no TypeScript errors)
- package.json has both new dependencies
- types/index.ts has SeasonalTemp, FertilizationInfo, PruningInfo, PestEntry interfaces
- PlantCareInfo has seasonalTemps?, fertilization?, pruning?, pests? optional fields
- SavedPlant has purchaseDate?, purchasePrice?, purchaseOrigin?, giftFrom? optional fields
- en.json and it.json both have detail.tabs.info, detail.care.pests, detail.notes.saved, detail.history.comingSoon keys
</success_criteria>

<output>
After completion, create `.planning/phases/04-tabbed-layout-and-content-reorganization/04-01-SUMMARY.md`
</output>
