---
phase: 04-tabbed-layout-and-content-reorganization
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/plant/[id].tsx
  - components/Detail/InfoTab.tsx
  - components/Detail/HistoryTab.tsx
autonomous: true
requirements:
  - TAB-01
  - TAB-02
  - TAB-03
  - TAB-04
  - TAB-05

must_haves:
  truths:
    - "Plant detail screen shows 4 tabs (Info, Care, History, Notes) with underline indicator"
    - "User can tap a tab label to switch tabs"
    - "User can swipe horizontally to switch tabs"
    - "App defaults to Info tab every time the plant detail screen opens"
    - "Only the active tab is rendered (lazy loading) — other tabs render on first visit"
    - "Compact header with thumbnail photo, plant name, confidence %, and action buttons remains visible above all tabs"
    - "Info tab displays: identification photo, species name, confidence %, common name, brief description"
    - "History tab shows a placeholder 'coming soon' message"
  artifacts:
    - path: "app/plant/[id].tsx"
      provides: "Tab host screen — compact header + MaterialTopTabNavigator"
      contains: "NavigationIndependentTree"
    - path: "components/Detail/InfoTab.tsx"
      provides: "Info tab content — identification details"
      exports: ["InfoTab"]
    - path: "components/Detail/HistoryTab.tsx"
      provides: "Placeholder History tab — coming soon message"
      exports: ["HistoryTab"]
  key_links:
    - from: "app/plant/[id].tsx"
      to: "components/Detail/InfoTab.tsx"
      via: "Tab.Screen component prop"
      pattern: "InfoTab"
    - from: "app/plant/[id].tsx"
      to: "NavigationIndependentTree"
      via: "import from @react-navigation/native"
      pattern: "NavigationIndependentTree"
    - from: "components/Detail/InfoTab.tsx"
      to: "stores/plantsStore.ts"
      via: "useLocalSearchParams + usePlantsStore"
      pattern: "useLocalSearchParams|usePlantsStore"
---

<objective>
Refactor app/plant/[id].tsx from a single-scroll screen into a tab host with a compact sticky header and 4-tab MaterialTopTabNavigator. Create InfoTab (identification details) and HistoryTab (coming soon placeholder). CareTab and NotesTab are built in the next plans.

Purpose: Establishes the tab navigation shell that CareTab (plan 03) and NotesTab (plan 04) plug into. The existing content from [id].tsx is redistributed — nothing is deleted from the app.
Output: Refactored [id].tsx with tab layout, new InfoTab.tsx and HistoryTab.tsx components.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tabbed-layout-and-content-reorganization/04-CONTEXT.md
@.planning/phases/04-tabbed-layout-and-content-reorganization/04-RESEARCH.md
@.planning/phases/04-tabbed-layout-and-content-reorganization/04-01-SUMMARY.md
@app/plant/[id].tsx
@types/index.ts
@components/Detail/CareInfo.tsx
@stores/plantsStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InfoTab and HistoryTab components</name>
  <files>components/Detail/InfoTab.tsx, components/Detail/HistoryTab.tsx</files>
  <action>
**Create components/Detail/InfoTab.tsx:**

This component displays the identification-focused content: identification photo, species name, confidence %, common name (if different from display name), brief species description, added date, and the existing editable fields (nickname, location). The MarkWateredButton and CareInfo are NOT here — those move to their own tabs.

Get the plant ID via `useLocalSearchParams<{ id: string }>()` from expo-router and the plant data from `usePlantsStore`. If `useLocalSearchParams` does not resolve correctly inside NavigationIndependentTree (a known open question from research), fall back to receiving `plantId` as a prop from the Tab.Screen initialParams.

Content to render in InfoTab (ScrollView layout):
1. Identification photo — full-width image, 220px tall (NOT the 280px hero from old layout — compact per locked decisions)
2. Plant name block: displayName (nickname || commonName || species), scientificName in italic, commonName if different from displayName, addedDate formatted as "Added on [date]"
3. Editable fields card: Nickname TextInput (blur saves via updatePlant), Location TextInput (blur saves via updatePlant) — reuse the existing onBlur save pattern from the old [id].tsx
4. Action buttons: existing "save to collection" and "share" concepts aren't present in [id].tsx — skip, per locked decisions the header action buttons (back + delete) remain in the parent screen header

Import from: `@/stores/plantsStore`, `expo-router` (useLocalSearchParams), `react-native`, `@expo/vector-icons`, `react-i18next`, `expo-haptics`, `@/components/Themed`.

Include a KeyboardAvoidingView wrapping the ScrollView with `behavior={Platform.OS === 'ios' ? 'padding' : 'height'}` and `keyboardShouldPersistTaps="handled"`.

**Create components/Detail/HistoryTab.tsx:**

Simple placeholder. Render a centered View with:
- Ionicons `time-outline` icon, size 48, color `#aaa`
- Text: use `t('detail.history.comingSoon')` (the key added in Plan 01)
- Subtext: use `t('detail.history.comingSoonDetail')`
- Minimal styling — centered, muted colors

No ScrollView needed. Just a flex:1 centered View.
  </action>
  <verify>
    <automated>cd "/Users/martha2022/Documents/Claude code/Plantid" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Both files exist; InfoTab.tsx and HistoryTab.tsx each export a named component</manual>
  </verify>
  <done>npx tsc --noEmit exits 0; components/Detail/InfoTab.tsx and HistoryTab.tsx exist and export InfoTab and HistoryTab respectively</done>
</task>

<task type="auto">
  <name>Task 2: Refactor [id].tsx into tab host with compact header</name>
  <files>app/plant/[id].tsx</files>
  <action>
Rewrite app/plant/[id].tsx to be a tab host screen. The file keeps its existing route, error handling (plant not found guard), delete modal, and navigation header — but the main content becomes a MaterialTopTabNavigator.

**Structure:**

```
SafeAreaView (flex:1, backgroundColor: #f5f5f5)
  ├── Header row (back button | plant name | delete button)  — unchanged from current
  ├── Compact plant header block (~90px tall)
  │   ├── Small thumbnail photo (80x80px, rounded, left-aligned)
  │   └── Right side: displayName, scientificName italic, confidence % badge
  ├── NavigationIndependentTree
  │   └── NavigationContainer
  │       └── Tab.Navigator (initialRouteName="Info", lazy=true, swipeEnabled=true)
  │           ├── Tab.Screen name="Info"    component={InfoTab}
  │           ├── Tab.Screen name="Care"    component={CareTab}   (stub — use a placeholder View for now)
  │           ├── Tab.Screen name="History" component={HistoryTab}
  │           └── Tab.Screen name="Notes"  component={NotesTab}  (stub — use a placeholder View for now)
BannerAdWrapper  — outside SafeAreaView as before
Delete confirmation Modal  — inside SafeAreaView as before
```

**Tab.Navigator screenOptions (per locked decisions + research):**
```typescript
screenOptions={{
  tabBarActiveTintColor: '#2e7d32',
  tabBarInactiveTintColor: '#888',
  tabBarIndicatorStyle: { backgroundColor: '#2e7d32', height: 2 },
  tabBarStyle: {
    backgroundColor: '#fff',
    shadowOpacity: 0,
    elevation: 0,
    borderBottomWidth: 1,
    borderBottomColor: '#eee',
  },
  tabBarLabelStyle: { fontSize: 13, fontWeight: '600', textTransform: 'none' },
  lazy: true,
  swipeEnabled: true,
}}
```

**Tab labels:** Use `t('detail.tabs.info')`, etc. from i18n. Set via `Tab.Screen options={{ title: t('detail.tabs.info') }}`.

**CareTab and NotesTab stubs (temporary, replaced in Plans 03 and 04):**
Create minimal inline placeholder components inside [id].tsx (not separate files yet):
```typescript
function CareTabPlaceholder() {
  return <View style={{ flex: 1, backgroundColor: '#f5f5f5' }} />;
}
function NotesTabPlaceholder() {
  return <View style={{ flex: 1, backgroundColor: '#f5f5f5' }} />;
}
```
These will be replaced when CareTab.tsx and NotesTab.tsx are created in Plans 03 and 04.

**NavigationIndependentTree import:**
```typescript
import { NavigationIndependentTree } from '@react-navigation/native';
import { NavigationContainer } from '@react-navigation/native';
import { createMaterialTopTabNavigator } from '@react-navigation/material-top-tabs';
```

If `NavigationIndependentTree` is not exported from `@react-navigation/native@7.1.28` (check at implementation time), use the following fallback: import `TabView` from `react-native-pager-view` directly and build a minimal custom tab bar (only needed if NavigationIndependentTree is unavailable). Document the approach taken in the SUMMARY.

**Plant data access in [id].tsx:**
The parent screen still reads `id` via `useLocalSearchParams` and passes `plant` data to the compact header. It does NOT pass the plant down to tab screens — each tab screen reads directly from the Zustand store using `useLocalSearchParams` (or receives plantId via Tab.Screen initialParams if useLocalSearchParams doesn't resolve inside NavigationIndependentTree).

**Remove from [id].tsx** (now owned by tab components):
- The ScrollView with all its content (photo, names, editable fields, CareInfo, watering components)
- The KeyboardAvoidingView wrapping the ScrollView (now per-tab)
- useState for notes, nickname, location (now in InfoTab and NotesTab)
- saveNotes, saveNickname, saveLocation callbacks (now in respective tab components)
- Imports no longer needed after the refactor

**Keep in [id].tsx:**
- plant not-found guard
- `showDeleteModal` state + `handleDeleteConfirm`
- `displayName`, `formattedDate` derived values (used in compact header)
- Delete confirmation Modal
- BannerAdWrapper
- Header row (back + title + delete icon)
  </action>
  <verify>
    <automated>cd "/Users/martha2022/Documents/Claude code/Plantid" && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run `npx expo start` and navigate to any plant detail screen. Confirm: 4 tabs visible with underline indicator, can swipe and tap between tabs, Info tab shows plant photo and names, History tab shows placeholder text</manual>
  </verify>
  <done>npx tsc --noEmit exits 0; [id].tsx renders 4-tab layout with Info/Care/History/Notes tabs; swipe and tap navigation work; Info tab shows plant details; History tab shows coming soon text; active tab is remembered during the same screen session — navigating to Care tab and then back via tab tap returns to Care, not Info</done>
</task>

</tasks>

<verification>
TypeScript check: `cd "/Users/martha2022/Documents/Claude code/Plantid" && npx tsc --noEmit`
Structure check: grep for NavigationIndependentTree in app/plant/[id].tsx
Component check: grep for "export function InfoTab" in components/Detail/InfoTab.tsx
</verification>

<success_criteria>
- npx tsc --noEmit exits 0
- app/plant/[id].tsx imports and renders MaterialTopTabNavigator wrapped in NavigationIndependentTree
- Tab.Navigator has initialRouteName="Info", lazy=true, swipeEnabled=true
- Tab bar uses underline indicator style (#2e7d32 color)
- components/Detail/InfoTab.tsx exists with InfoTab component reading plant from Zustand
- components/Detail/HistoryTab.tsx exists with coming soon placeholder
- Compact header visible above tabs with thumbnail photo and plant name
</success_criteria>

<output>
After completion, create `.planning/phases/04-tabbed-layout-and-content-reorganization/04-02-SUMMARY.md`

Document in SUMMARY: which approach was used for NavigationIndependentTree (available or fallback), and whether useLocalSearchParams works inside NavigationIndependentTree (affects Plans 03 and 04).
</output>
