---
phase: 11-auth-infrastructure-supabase-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env
  - lib/supabase/client.ts
  - lib/supabase/storageAdapter.ts
  - types/index.ts
autonomous: true
requirements:
  - AUTH-05
  - AUTH-07

must_haves:
  truths:
    - Supabase client uses lazy initialization pattern
    - Session tokens stored in Expo SecureStore (encrypted)
    - Supabase initialization does NOT block app launch
    - v1.x features (plant ID, collection) work without Supabase configured
    - Environment variables validate before client creation
  artifacts:
    - path: "lib/supabase/client.ts"
      provides: "Lazy-initialized Supabase client singleton"
      contains: "getSupabaseClient"
      min_lines: 40
    - path: "lib/supabase/storageAdapter.ts"
      provides: "Expo SecureStore adapter for session persistence"
      contains: "SecureStore"
      min_lines: 15
    - path: "types/index.ts"
      provides: "Auth-related TypeScript types"
      contains: "AuthState"
      min_lines: 20 (added to existing file)
    - path: "package.json"
      provides: "@supabase/supabase-js dependency"
      contains: "@supabase/supabase-js"
    - path: ".env"
      provides: "Supabase environment variables"
      contains: "EXPO_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "lib/supabase/client.ts"
      to: "lib/supabase/storageAdapter.ts"
      via: "storage adapter import"
      pattern: "import.*storageAdapter"
    - from: "lib/supabase/client.ts"
      to: "types/index.ts"
      via: "SupabaseClient type usage"
      pattern: "import.*@/types"
---

<objective>
Set up Supabase client infrastructure with lazy initialization and secure session persistence. This foundation must NOT interfere with v1.x offline-first features—plant identification and collection management must continue working without network connectivity or Supabase configuration.

Purpose: Enable optional authentication without breaking existing functionality.
Output: Lazy Supabase client singleton with Expo SecureStore adapter, auth types defined, environment configured.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-auth-infrastructure-supabase-setup/11-RESEARCH.md
@.planning/phases/11-auth-infrastructure-supabase-setup/11-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@types/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase dependency and configure environment</name>
  <files>package.json, .env</files>
  <action>
1. Install @supabase/supabase-js:
   npm install @supabase/supabase-js@^2.39.0

2. Add environment variables to .env (create if doesn't exist):
   EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

Note: User will replace these with actual Supabase project values. Use placeholder values for now.

3. Verify expo-secure-store is available (included in Expo SDK 54):
   Check package.json for "expo-secure-store" or install if missing:
   npm install expo-secure-store@~13.0.2
  </action>
  <verify>
grep -q "@supabase/supabase-js" package.json && grep -q "EXPO_PUBLIC_SUPABASE_URL" .env
  </verify>
  <done>
@supabase/supabase-js@^2.39.0 installed; EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY in .env
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Expo SecureStore storage adapter</name>
  <files>lib/supabase/storageAdapter.ts</files>
  <action>
1. Create directory structure:
   mkdir -p lib/supabase

2. Create lib/supabase/storageAdapter.ts:
   - Import * as SecureStore from 'expo-secure-store'
   - Export secureAdapter object with getItem, setItem, removeItem
   - Use SecureStore.getItemAsync, setItemAsync, deleteItemAsync
   - Add JSDoc comments explaining encryption (iOS Keychain, Android Keystore)

Implementation pattern from RESEARCH.md:
```typescript
import * as SecureStore from 'expo-secure-store';

export const secureAdapter = {
  getItem: (key: string) => SecureStore.getItemAsync(key),
  setItem: (key: string, value: string) => SecureStore.setItemAsync(key, value),
  removeItem: (key: string) => SecureStore.deleteItemAsync(key),
};
```

3. Add TypeScript types for storage adapter (extend in types/index.ts):
   - StorageAdapter interface with getItem, setItem, removeItem methods
   - Return types: Promise<string | null> for getItem, Promise<void> for setItem/removeItem
  </action>
  <verify>
grep -q "SecureStore" lib/supabase/storageAdapter.ts && grep -q "getItemAsync" lib/supabase/storageAdapter.ts
  </verify>
  <done>
secureAdapter exported with getItem/setItem/removeItem using SecureStore APIs
  </done>
</task>

<task type="auto">
  <name>Task 3: Create lazy-initialized Supabase client singleton</name>
  <files>lib/supabase/client.ts</files>
  <action>
Create lib/supabase/client.ts with lazy initialization:

1. Import createClient from '@supabase/supabase-js' and secureAdapter from './storageAdapter'

2. Define module-level variable: let supabaseInstance: SupabaseClient | null = null

3. Export getSupabaseClient() function:
   - Check if supabaseInstance exists, return if true
   - Read EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY from process.env
   - Throw error if either is missing with descriptive message
   - Create Supabase client with createClient(url, key, {
       auth: {
         storage: secureAdapter,
         autoRefreshToken: true,
         persistSession: true,
         detectSessionInUrl: false,
       },
     })
   - Store in supabaseInstance and return

4. Add JSDoc comments:
   - Explain lazy initialization (prevents blocking app launch)
   - Document that first call initializes, subsequent calls return cached instance
   - Note: Safe to call multiple times

5. Export type { SupabaseClient } from '@supabase/supabase-js' for use in other modules

Reference pattern from 11-RESEARCH.md section "Pattern 1: Lazy-Initialized Supabase Client"
  </action>
  <verify>
grep -q "getSupabaseClient" lib/supabase/client.ts && grep -q "supabaseInstance" lib/supabase/client.ts && grep -q "detectSessionInUrl: false" lib/supabase/client.ts
  </verify>
  <done>
getSupabaseClient() exported; creates singleton on first call; uses secureAdapter; autoRefreshToken and persistSession enabled
  </done>
</task>

<task type="auto">
  <name>Task 4: Add auth types to types/index.ts</name>
  <files>types/index.ts</files>
  <action>
1. Open types/index.ts and add auth-related types at end of file:

2. Add StorageAdapter interface:
   - getItem(key: string): Promise<string | null>
   - setItem(key: string, value: string): Promise<void>
   - removeItem(key: string): Promise<void>

3. Add AuthState interface (for future authStore):
   - user: User | null
   - session: Session | null
   - isLoading: boolean
   - error: string | null

4. Add AuthError type union:
   - type AuthError = 'network_error' | 'invalid_credentials' | 'email_not_confirmed' | 'session_expired' | 'unknown'

5. Import User and Session from '@supabase/supabase-js':
   - Add import at top: import { User, Session } from '@supabase/supabase-js'

6. Export all new types

DO NOT modify existing plant-related types (SavedPlant, PlantCareInfo, etc.).
  </action>
  <verify>
grep -q "AuthState" types/index.ts && grep -q "StorageAdapter" types/index.ts && grep -q "import.*User.*Session.*from '@supabase/supabase-js'" types/index.ts
  </verify>
  <done>
AuthState, StorageAdapter, AuthError types added; User and Session imported from @supabase/supabase-js
  </done>
</task>

<task type="auto">
  <name>Task 5: Create barrel export for lib/supabase</name>
  <files>lib/supabase/index.ts</files>
  <action>
Create lib/supabase/index.ts to export all supabase-related modules:

1. Export getSupabaseClient and SupabaseClient type from './client'
2. Export secureAdapter from './storageAdapter'
3. Add JSDoc comment explaining this is the main entry point for Supabase integration

Pattern:
```typescript
/**
 * Supabase integration for Plantid v2.0 Community
 *
 * Provides lazy-initialized Supabase client and secure storage adapter.
 * All auth operations should use getSupabaseClient() to access the client.
 */

export { getSupabaseClient } from './client';
export type { SupabaseClient } from './client';
export { secureAdapter } from './storageAdapter';
```

This allows clean imports: import { getSupabaseClient } from '@/lib/supabase';
  </action>
  <verify>
grep -q "getSupabaseClient" lib/supabase/index.ts && grep -q "secureAdapter" lib/supabase/index.ts
  </verify>
  <done>
lib/supabase/index.ts exports getSupabaseClient, SupabaseClient type, and secureAdapter
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: npx tsc --noEmit
2. lib/supabase/client.ts contains getSupabaseClient() function with lazy initialization
3. lib/supabase/storageAdapter.ts uses SecureStore APIs
4. types/index.ts has AuthState and StorageAdapter interfaces
5. .env contains EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY
6. package.json lists @supabase/supabase-js@^2.39.0
7. Existing plant identification still works (no breaking changes to services/plantnet.ts)
8. App launches without crashing even with invalid Supabase credentials
</verification>

<success_criteria>
- Supabase client initializes only when first called (lazy pattern)
- Session tokens encrypted via Expo SecureStore
- v1.x features unaffected (plant ID, collection, settings all work offline)
- Environment variables properly configured
- TypeScript types defined for auth state and storage
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-infrastructure-supabase-setup/11-01-SUMMARY.md`
</output>
