---
phase: 11-auth-infrastructure-supabase-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - stores/authStore.ts
  - services/authService.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-06

must_haves:
  truths:
    - Auth state managed via Zustand store (follows existing plantsStore pattern)
    - Email/password sign up uses supabase.auth.signUp()
    - Google OAuth uses supabase.auth.signInWithOAuth() with Google provider
    - Apple OAuth uses supabase.auth.signInWithOAuth() with Apple provider
    - Password reset uses supabase.auth.resetPasswordForEmail()
    - Sign out uses supabase.auth.signOut() and clears auth store
    - All auth operations return structured { success, error?, data? } responses
  artifacts:
    - path: "stores/authStore.ts"
      provides: "Zustand auth state management"
      contains: "useAuthStore"
      min_lines: 50
    - path: "services/authService.ts"
      provides: "Auth operations (signUp, signIn, signOut, resetPassword)"
      contains: "signUpWithEmail"
      min_lines: 150
  key_links:
    - from: "services/authService.ts"
      to: "stores/authStore.ts"
      via: "useAuthStore import for state updates"
      pattern: "import.*useAuthStore"
    - from: "services/authService.ts"
      to: "lib/supabase/client.ts"
      via: "getSupabaseClient calls"
      pattern: "import.*getSupabaseClient"
    - from: "stores/authStore.ts"
      to: "types/index.ts"
      via: "AuthState type usage"
      pattern: "import.*AuthState"
---

<objective>
Create auth state management with Zustand and implement core auth service operations (email/password signup, OAuth, password reset, sign out). These functions will be used by auth UI components in the next plan.

Purpose: Centralize auth logic and state following existing Plantid patterns.
Output: Zustand authStore with session/user state, authService with all CRUD auth operations.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-auth-infrastructure-supabase-setup/11-RESEARCH.md
@.planning/phases/11-auth-infrastructure-supabase-setup/11-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@stores/plantsStore.ts
@stores/settingsStore.ts
@types/index.ts
@lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zustand authStore</name>
  <files>stores/authStore.ts</files>
  <action>
Create stores/authStore.ts following existing plantsStore.ts pattern:

1. Import create from 'zustand'
2. Import User and Session types from '@supabase/supabase-js'

3. Define AuthState interface:
   - user: User | null
   - session: Session | null
   - isLoading: boolean
   - error: string | null

4. Define AuthActions interface:
   - setUser: (user: User | null) => void
   - setSession: (session: Session | null) => void
   - setLoading: (loading: boolean) => void
   - setError: (error: string | null) => void
   - clearAuth: () => void

5. Combine into AuthStore type: AuthState & AuthActions

6. Create useAuthStore with create<AuthStore>((set) => ({ ... })):
   - Initialize state: user: null, session: null, isLoading: false, error: null
   - Implement setUser: (user) => set({ user })
   - Implement setSession: (session) => set({ session, user: session?.user ?? null })
   - Implement setLoading: (isLoading) => set({ isLoading })
   - Implement setError: (error) => set({ error })
   - Implement clearAuth: () => set({ user: null, session: null, error: null })

7. Add JSDoc comments explaining each action

Reference existing plantsStore.ts for Zustand pattern. DO NOT add persist middleware (session handled by Supabase SecureStore).
  </action>
  <verify>
grep -q "useAuthStore" stores/authStore.ts && grep -q "setSession" stores/authStore.ts && grep -q "clearAuth" stores/authStore.ts
  </verify>
  <done>
useAuthStore exported with user, session, isLoading, error state; setUser, setSession, setLoading, setError, clearAuth actions implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Create authService with email/password operations</name>
  <files>services/authService.ts</files>
  <action>
Create services/authService.ts with email/password auth:

1. Import getSupabaseClient from '@/lib/supabase'
2. Import useAuthStore from '@/stores/authStore'

3. Implement signUpWithEmail(email, password):
   - Call supabase.auth.signUp({ email, password, options: { emailRedirectTo: 'plantidtemp://auth/callback' } })
   - On error: return { success: false, error: error.message }
   - On success with null session: return { success: true, requiresConfirmation: true, message: 'Check your email for confirmation link' }
   - On success with session: update store with setSession, return { success: true, session }
   - Use try-catch, set loading state in store

4. Implement signInWithEmail(email, password):
   - Call supabase.auth.signInWithPassword({ email, password })
   - On error: return { success: false, error: error.message }
   - On success: update store with setSession, return { success: true, session }
   - Use try-catch, set loading state in store

5. Implement resetPassword(email):
   - Call supabase.auth.resetPasswordForEmail(email, { redirectTo: 'plantidtemp://auth/reset-password' })
   - On error: return { success: false, error: error.message }
   - On success: return { success: true, message: 'Password reset email sent. Check your inbox.' }
   - Use try-catch

6. Implement signOut():
   - Call supabase.auth.signOut()
   - On error: return { success: false, error: error.message }
   - On success: call useAuthStore.getState().clearAuth(), return { success: true }
   - Use try-catch

7. Add JSDoc comments for each function

Reference 11-RESEARCH.md "Email/Password Sign Up" and "Password Reset" sections.
  </action>
  <verify>
grep -q "signUpWithEmail" services/authService.ts && grep -q "signInWithEmail" services/authService.ts && grep -q "resetPassword" services/authService.ts && grep -q "signOut" services/authService.ts
  </verify>
  <done>
signUpWithEmail, signInWithEmail, resetPassword, signOut functions implemented; all return { success, error? } responses; update authStore on success
  </done>
</task>

<task type="auto">
  <name>Task 3: Add OAuth operations to authService</name>
  <files>services/authService.ts</files>
  <action>
Extend services/authService.ts with OAuth operations:

1. Implement signInWithGoogle():
   - Call supabase.auth.signInWithOAuth({
       provider: 'google',
       options: {
         redirectTo: 'plantidtemp://auth/callback',
         queryParams: { access_type: 'offline', prompt: 'consent' },
       },
     })
   - On error: return { success: false, error: error.message }
   - On success: return { success: true, url: data.url } (browser will open this URL)
   - Note: Session will be set in OAuth callback screen (plan 11-03)
   - Use try-catch, set loading state

2. Implement signInWithApple():
   - Call supabase.auth.signInWithOAuth({
       provider: 'apple',
       options: { redirectTo: 'plantidtemp://auth/callback' },
     })
   - On error: return { success: false, error: error.message }
   - On success: return { success: true, url: data.url }
   - Same note: Session set in callback screen
   - Use try-catch, set loading state

3. Add helper function getSession():
   - Call supabase.auth.getSession()
   - Return { session, error } tuple
   - Used by OAuth callback to retrieve session after redirect

4. Add JSDoc comments for OAuth functions explaining browser redirect flow

Reference 11-RESEARCH.md "OAuth Sign In" section.

Important: OAuth flow is:
1. User taps Google/Apple button
2. signInWithGoogle/Apple returns URL
3. App opens URL in browser (user completes sign-in)
4. Browser redirects to plantidtemp://auth/callback
5. Callback screen (plan 11-03) calls getSession() to retrieve session
  </action>
  <verify>
grep -q "signInWithGoogle" services/authService.ts && grep -q "signInWithApple" services/authService.ts && grep -q "provider: 'google'" services/authService.ts && grep -q "provider: 'apple'" services/authService.ts
  </verify>
  <done>
signInWithGoogle and signInWithApple implemented; both return { success, url } for browser redirect; getSession helper added
  </done>
</task>

<task type="auto">
  <name>Task 4: Add session state listener to authService</name>
  <files>services/authService.ts</files>
  <action>
Add session state management utilities to services/authService.ts:

1. Implement initializeAuth():
   - Call supabase.auth.getSession() to check for existing session
   - If session exists: call useAuthStore.getState().setSession(session)
   - Set up supabase.auth.onAuthStateChange listener:
     - On INITIAL_SESSION: set session in store
     - On SIGNED_IN: set session in store
     - On SIGNED_OUT: call useAuthStore.getState().clearAuth()
     - On TOKEN_REFRESHED: update session in store
     - On USER_UPDATED: update user in store
   - Return unsubscribe function for cleanup
   - Use try-catch, return null if Supabase not configured

2. Implement getCurrentUser():
   - Return useAuthStore.getState().user (convenience getter)

3. Implement isAuthenticated():
   - Return Boolean(useAuthStore.getState().session) (convenience checker)

4. Add JSDoc comments explaining when to call initializeAuth() (in app root layout)

Note: initializeAuth() will be called in app/_layout.tsx in plan 11-04 to restore session on app launch.

Reference 11-RESEARCH.md "Session State Listener" section.
  </action>
  <verify>
grep -q "initializeAuth" services/authService.ts && grep -q "onAuthStateChange" services/authService.ts && grep -q "getCurrentUser" services/authService.ts && grep -q "isAuthenticated" services/authService.ts
  </verify>
  <done>
initializeAuth() gets session on launch and listens for changes; getCurrentUser() and isAuthenticated() convenience getters added
  </done>
</task>

<task type="auto">
  <name>Task 5: Add error handling utilities to authService</name>
  <files>services/authService.ts</files>
  <action>
Add error handling helpers to services/authService.ts:

1. Create getAuthErrorMessage(error) function:
   - Switch on error.message or error.status:
     - 'Invalid login credentials': return 'Invalid email or password'
     - 'Email not confirmed': return 'Please check your email for confirmation link'
     - 'User already registered': return 'An account with this email already exists'
     - 'Network request failed': return 'Unable to connect. Check your internet connection.'
     - Default: return error.message or 'An unknown error occurred'
   - Return user-friendly error string

2. Update all auth functions to use getAuthErrorMessage():
   - signUpWithEmail: wrap error in getAuthErrorMessage()
   - signInWithEmail: wrap error in getAuthErrorMessage()
   - resetPassword: wrap error in getAuthErrorMessage()
   - signOut: wrap error in getAuthErrorMessage()
   - OAuth functions: wrap error in getAuthErrorMessage()

3. Add clearError() helper:
   - Call useAuthStore.getState().setError(null)
   - Returns void

4. Update JSDoc comments to mention error message translation

This ensures users see friendly error messages instead of raw Supabase error text.
  </action>
  <verify>
grep -q "getAuthErrorMessage" services/authService.ts && grep -q "clearError" services/authService.ts && grep -q "Unable to connect" services/authService.ts
  </verify>
  <done>
getAuthErrorMessage() translates Supabase errors; all auth functions use it; clearError() helper added
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: npx tsc --noEmit
2. stores/authStore.ts exists with useAuthStore export
3. services/authService.ts exports: signUpWithEmail, signInWithEmail, signInWithGoogle, signInWithApple, resetPassword, signOut, initializeAuth, getCurrentUser, isAuthenticated, getAuthErrorMessage, clearError
4. All auth functions return { success, error?, data? } structure
5. Auth store uses same pattern as plantsStore.ts (Zustand with actions)
6. OAuth functions return URL for browser redirect
7. Error messages are user-friendly (not raw Supabase errors)
</verification>

<success_criteria>
- Zustand authStore manages user, session, loading, error state
- Email/password signup, sign-in, password reset, sign out all implemented
- Google and Apple OAuth sign-in implemented
- Session state listener for auth changes
- Convenience getters (getCurrentUser, isAuthenticated)
- User-friendly error messages
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-infrastructure-supabase-setup/11-02-SUMMARY.md`
</output>
