---
phase: 11-auth-infrastructure-supabase-setup
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - components/auth/AuthModal.tsx
  - components/auth/EmailAuthForm.tsx
  - components/auth/OAuthButtons.tsx
  - app/auth/callback.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - Auth modal is full-screen with logo and tabs (Sign In / Create Account)
    - OAuth buttons (Google, Apple) displayed prominently at top
    - Email/password form displayed below OAuth with divider
    - Apple Sign In button only shows on iOS (Platform.OS === 'ios')
    - Email form validates email format and password length (min 6 chars)
    - OAuth callback screen handles deep link redirect
    - Loading states shown during auth operations
    - Error messages displayed inline
  artifacts:
    - path: "components/auth/AuthModal.tsx"
      provides: "Full-screen auth modal with tabs"
      contains: "AuthModal"
      min_lines: 80
    - path: "components/auth/EmailAuthForm.tsx"
      provides: "Email/password auth form with validation"
      contains: "EmailAuthForm"
      min_lines: 120
    - path: "components/auth/OAuthButtons.tsx"
      provides: "Google + Apple OAuth buttons"
      contains: "OAuthButtons"
      min_lines: 60
    - path: "app/auth/callback.tsx"
      provides: "OAuth callback handler for deep links"
      contains: "getSessionFromUrl"
      min_lines: 50
  key_links:
    - from: "components/auth/AuthModal.tsx"
      to: "components/auth/EmailAuthForm.tsx"
      via: "component import"
      pattern: "import.*EmailAuthForm"
    - from: "components/auth/AuthModal.tsx"
      to: "components/auth/OAuthButtons.tsx"
      via: "component import"
      pattern: "import.*OAuthButtons"
    - from: "components/auth/EmailAuthForm.tsx"
      to: "services/authService.ts"
      via: "authService import"
      pattern: "import.*authService"
    - from: "components/auth/OAuthButtons.tsx"
      to: "services/authService.ts"
      via: "authService import"
      pattern: "import.*authService"
    - from: "app/auth/callback.tsx"
      to: "lib/supabase/client.ts"
      via: "getSupabaseClient import"
      pattern: "import.*getSupabaseClient"
---

<objective>
Create auth UI components (full-screen modal, email form, OAuth buttons) and OAuth callback screen. This enables users to sign up/sign in with email/password, Google, or Apple.

Purpose: Provide user-facing authentication interface.
Output: Modal auth component with tabs, email form with validation, OAuth buttons with iOS-specific Apple button, callback screen for OAuth redirects.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-auth-infrastructure-supabase-setup/11-RESEARCH.md
@.planning/phases/11-auth-infrastructure-supabase-setup/11-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@components/Themed.tsx
@constants/Colors.ts
@services/authService.ts
@stores/authStore.ts
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OAuthButtons component</name>
  <files>components/auth/OAuthButtons.tsx</files>
  <action>
Create components/auth/OAuthButtons.tsx:

1. Create directory: mkdir -p components/auth

2. Import dependencies:
   - React, { useState } from 'react'
   - { View, Text, TouchableOpacity, ActivityIndicator, Platform } from 'react-native'
   - { useRouter } from 'expo-router'
   - { Ionicons } from '@expo/vector-icons'
   - authService from '@/services/authService'
   - useAuthStore from '@/stores/authStore'
   - Colors from '@/constants/Colors'

3. Define component props:
   - interface OAuthButtonsProps { mode: 'signIn' | 'signUp'; onError?: (error: string) => void }

4. Implement component:
   - State: loading Provider | null (for button-specific loading)
   - Helper: handleGooglePress():
     - Set loading 'google'
     - Call authService.signInWithGoogle()
     - On success: open URL with Linking.openURL(url)
     - On error: call onError(error.message)
     - Finally: set loading null
   - Helper: handleApplePress():
     - Set loading 'apple'
     - Call authService.signInWithApple()
     - On success: open URL with Linking.openURL(url)
     - On error: call onError(error.message)
     - Finally: set loading null
   - Render:
     - View with row layout
     - Google button (TouchableOpacity): red background, Google icon, "Continue with Google" text
     - Apple button (TouchableOpacity, only if Platform.OS === 'ios'): black background, Apple icon, "Continue with Apple" text
     - Show ActivityIndicator in button when loading
     - Disable buttons while any is loading

5. Style buttons:
   - Full width, 48px height, rounded corners
   - Row layout with icon left, text center
   - Disabled opacity when loading
   - Use theme colors from Colors.ts

6. Add JSDoc comments

Reference CONTEXT.md "Sign-in UX" section for button layout.
  </action>
  <verify>
grep -q "OAuthButtons" components/auth/OAuthButtons.tsx && grep -q "signInWithGoogle" components/auth/OAuthButtons.tsx && grep -q "Platform.OS === 'ios'" components/auth/OAuthButtons.tsx
  </verify>
  <done>
OAuthButtons component with Google and Apple (iOS-only) buttons; loading states per button; opens OAuth URL on success
  </done>
</task>

<task type="auto">
  <name>Task 2: Create EmailAuthForm component</name>
  <files>components/auth/EmailAuthForm.tsx</files>
  <action>
Create components/auth/EmailAuthForm.tsx:

1. Import dependencies:
   - React, { useState } from 'react'
   - { View, Text, TextInput, TouchableOpacity, ActivityIndicator, KeyboardAvoidingView, Platform } from 'react-native'
   - authService from '@/services/authService'
   - useAuthStore from '@/stores/authStore'
   - Colors from '@/constants/Colors'

2. Define component props:
   - interface EmailAuthFormProps { mode: 'signIn' | 'signUp'; onSuccess?: () => void; onError?: (error: string) => void }

3. Implement component:
   - State: email (string), password (string), isLoading (boolean), errors ({ email?: string, password?: string })
   - Validation: validateEmail() uses regex, validatePassword() checks length >= 6
   - Handler: handleSubmit():
     - Validate email and password, set errors
     - If valid: set isLoading true
     - Call authService.signUpWithEmail() or authService.signInWithEmail() based on mode
     - On success: call onSuccess(), clear form
     - On error: call onError(error.message)
     - Finally: set isLoading false
   - Render:
     - KeyboardAvoidingView for keyboard handling
     - Email TextInput: placeholder "Email", keyboardType="email-address", autoCapitalize="none", error shown below
     - Password TextInput: placeholder "Password (min 6 characters)", secureTextEntry, error shown below
     - Submit button: "Sign In" or "Create Account" based on mode, disabled while loading
     - ActivityIndicator when loading
     - "Forgot password?" link (only for signIn mode) - opens reset password flow

4. Style form:
   - Input fields: 48px height, border, rounded corners, padding
   - Error text: red color, small font
   - Button: full width, 48px height, theme color background
   - Spacing between elements

5. Add JSDoc comments

Use TextInput with error state (red border when error exists).
  </action>
  <verify>
grep -q "EmailAuthForm" components/auth/EmailAuthForm.tsx && grep -q "validateEmail" components/auth/EmailAuthForm.tsx && grep -q "handleSubmit" components/auth/EmailAuthForm.tsx && grep -q "min 6" components/auth/EmailAuthForm.tsx
  </verify>
  <done>
EmailAuthForm with email/password inputs; validation (email format, password >= 6 chars); mode-specific submit (signIn/signUp); inline error messages
  </done>
</task>

<task type="auto">
  <name>Task 3: Create AuthModal component</name>
  <files>components/auth/AuthModal.tsx</files>
  <action>
Create components/auth/AuthModal.tsx:

1. Import dependencies:
   - React, { useState } from 'react'
   - { View, Text, Modal, StyleSheet, TouchableOpacity, ScrollView, SafeAreaView } from 'react-native'
   - { Ionicons } from '@expo/vector-icons'
   - { useColorScheme } from '@/components/useColorScheme'
   - OAuthButtons from './OAuthButtons'
   - EmailAuthForm from './EmailAuthForm'
   - Colors from '@/constants/Colors'

2. Define component props:
   - interface AuthModalProps { visible: boolean; onClose: () => void; initialMode?: 'signIn' | 'signUp' }

3. Implement component:
   - State: mode ('signIn' | 'signUp'), error (string | null)
   - Handler: handleSuccess(): close modal, call onSuccess callback
   - Handler: handleError(error): set error state, show toast/message
   - Handler: switchMode(): toggle between signIn and signUp
   - Render:
     - Modal with visible prop, animationType="slide", presentationStyle="fullScreen"
     - SafeAreaView with full height
     - Header: close button (X icon) top right
     - Logo/Title: "Plantid" text with large font
     - Tab switcher: "Sign In" | "Create Account" buttons at top
     - Content: ScrollView with OAuthButtons, divider ("or"), EmailAuthForm
     - Footer: "By continuing, you agree to our Terms and Privacy Policy"
     - Error message: red text at top if error state exists
     - Close button top-right to dismiss modal

4. Style modal:
   - Full screen presentation
   - White/dark background based on theme
   - Tab buttons: underline for active tab
   - Divider: horizontal line with "or" text
   - Consistent padding and spacing

5. Add JSDoc comments

Reference CONTEXT.md "Sign-in UX" for full-screen modal with tabs layout.

Note: Modal doesn't auto-dismiss on success—let onSuccess callback handle navigation (e.g., to Settings or Community).
  </action>
  <verify>
grep -q "AuthModal" components/auth/AuthModal.tsx && grep -q "OAuthButtons" components/auth/AuthModal.tsx && grep -q "EmailAuthForm" components/auth/AuthModal.tsx && grep -q "presentationStyle=\"fullScreen\"" components/auth/AuthModal.tsx
  </verify>
  <done>
AuthModal full-screen modal with Sign In/Create Account tabs; OAuthButtons top, EmailAuthForm below with "or" divider; close button top-right
  </done>
</task>

<task type="auto">
  <name>Task 4: Create OAuth callback screen</name>
  <files>app/auth/callback.tsx</files>
  <action>
Create app/auth/callback.tsx to handle OAuth redirects:

1. Create directory: mkdir -p app/auth

2. Import dependencies:
   - React, { useEffect, useState } from 'react'
   - { View, Text, ActivityIndicator } from 'react-native'
   - { useRouter, useLocalSearchParams } from 'expo-router'
   - * as Linking from 'expo-linking'
   - { getSupabaseClient } from '@/lib/supabase'
   - { useAuthStore } from '@/stores/authStore'

3. Implement component:
   - State: error (string | null), processing (boolean)
   - useEffect:
     - Get URL from useLocalSearchParams() or Linking.getInitialURL()
     - If URL exists:
       - Call supabase.auth.getSessionFromUrl(url)
       - On success with session:
         - Call useAuthStore.getState().setSession(session)
         - Router.replace('/(tabs)') or router.back()
       - On error:
         - Set error state, show message
         - Router.replace('/(tabs)/settings') after delay
     - Else:
       - Set error "No callback URL found"
   - Render:
     - Full-screen View centered
     - ActivityIndicator while processing
     - Error message if error state exists
     - "Signing you in..." text while processing

4. Style screen:
   - Centered content
   - Use Themed View for theme support
   - Loading spinner with text

5. Add JSDoc comments explaining OAuth flow

Reference 11-RESEARCH.md "Pattern 3: OAuth Deep Link Handling".

Note: This screen is accessed via deep link plantidtemp://auth/callback after user completes OAuth in browser.
  </action>
  <verify>
grep -q "getSessionFromUrl" app/auth/callback.tsx && grep -q "useLocalSearchParams" app/auth/callback.tsx && grep -q "setSession" app/auth/callback.tsx
  </verify>
  <done>
Callback screen handles OAuth redirect; extracts session from URL; updates authStore; redirects to tabs or shows error
  </done>
</task>

<task type="auto">
  <name>Task 5: Create barrel export for auth components</name>
  <files>components/auth/index.ts</files>
  <action>
Create components/auth/index.ts barrel export:

1. Export all auth components:
   - export { AuthModal } from './AuthModal'
   - export { EmailAuthForm } from './EmailAuthForm'
   - export { OAuthButtons } from './OAuthButtons'

2. Add JSDoc comment explaining auth components

This allows clean imports: import { AuthModal } from '@/components/auth'
  </action>
  <verify>
grep -q "AuthModal" components/auth/index.ts && grep -q "EmailAuthForm" components/auth/index.ts && grep -q "OAuthButtons" components/auth/index.ts
  </verify>
  <done>
components/auth/index.ts exports AuthModal, EmailAuthForm, OAuthButtons
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: npx tsc --noEmit
2. components/auth/AuthModal.tsx renders full-screen modal with tabs
3. components/auth/EmailAuthForm.tsx validates email and password
4. components/auth/OAuthButtons.tsx shows Google + Apple (iOS only) buttons
5. app/auth/callback.tsx handles OAuth deep link
6. Apple button only renders on iOS (Platform.OS check)
7. Loading states shown during auth operations
8. Error messages displayed inline
9. Modal dismisses with close button
10. Tab switcher toggles between Sign In and Create Account
</verification>

<success_criteria>
- Full-screen auth modal with Sign In / Create Account tabs
- OAuth buttons (Google, Apple iOS-only) prominent at top
- Email form with validation below OAuth with divider
- Loading and error states handled
- OAuth callback screen handles browser redirect
- Apple Sign In conditional rendering for iOS
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-infrastructure-supabase-setup/11-03-SUMMARY.md`
</output>
