---
phase: 03-monetization
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - components/ProUpgradeModal.tsx
  - app/(tabs)/settings.tsx
  - i18n/en.ts
  - i18n/it.ts
  - app/(tabs)/camera.tsx
  - components/Results/ResultCard.tsx
  - hooks/useRateLimit.ts
autonomous: true
requirements:
  - AD-02
  - PRO-01
  - PRO-02
  - PRO-03

must_haves:
  truths:
    - ProUpgradeModal shows benefits list, price (€4.99), and "No subscription" message
    - Modal triggered when user hits scan limit or plant collection limit
    - Purchase button calls useProStatus().purchase()
    - Settings screen has "Upgrade to Pro" button for free users
    - Settings has "Restore Purchases" button for all users
    - Pro status display shown in Settings ("Free" or "Pro" badge)
    - Camera screen shows upgrade modal when canIdentify returns allowed=false
    - ResultCard shows upgrade modal when addPlant returns false
  artifacts:
    - path: "components/ProUpgradeModal.tsx"
      provides: "Benefits-focused upgrade prompt modal"
      exports: ["ProUpgradeModal"]
      contains: "€4.99"
    - path: "app/(tabs)/settings.tsx"
      provides: "Updated settings with Pro upgrade/restore buttons"
      contains: "Upgrade to Pro|Restore Purchases"
    - path: "app/(tabs)/camera.tsx"
      provides: "Camera screen with upgrade modal integration"
      contains: "ProUpgradeModal|setUpgradeModalVisible"
    - path: "components/Results/ResultCard.tsx"
      provides: "ResultCard with upgrade modal integration"
      contains: "ProUpgradeModal|upgradeModalVisible"
  key_links:
    - from: "components/ProUpgradeModal.tsx"
      to: "hooks/useProStatus.ts"
      via: "Hook usage for purchase flow"
      pattern: "useProStatus"
    - from: "app/(tabs)/settings.tsx"
      to: "components/ProUpgradeModal.tsx"
      via: "Import and render"
      pattern: "ProUpgradeModal"
    - from: "app/(tabs)/settings.tsx"
      to: "hooks/useProStatus.ts"
      via: "Hook usage for restore purchase"
      pattern: "useProStatus"
    - from: "app/(tabs)/camera.tsx"
      to: "hooks/useRateLimit.ts"
      via: "Import and canIdentify check"
      pattern: "canIdentify"
    - from: "app/(tabs)/camera.tsx"
      to: "components/ProUpgradeModal.tsx"
      via: "Import and render with scan_limit trigger"
      pattern: "ProUpgradeModal.*scan_limit"
    - from: "components/Results/ResultCard.tsx"
      to: "stores/plantsStore.ts"
      via: "Import and addPlant call with boolean check"
      pattern: "const success = addPlant"
    - from: "components/Results/ResultCard.tsx"
      to: "components/ProUpgradeModal.tsx"
      via: "Import and render with collection_limit trigger"
      pattern: "ProUpgradeModal.*collection_limit"
---

<objective>
Create the Pro upgrade UI: a modal showing Pro benefits and purchase flow, plus Settings screen integration with upgrade/restore buttons. This plan implements the user-facing monetization interface that converts free users to Pro when they hit limits.

Purpose: Convert users to Pro by presenting clear value proposition at natural trigger points (scan limit, collection limit) and providing easy upgrade access in Settings.

Output: ProUpgradeModal component, updated Settings screen with Pro status display and upgrade/restore buttons, translation keys.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monetization/03-CONTEXT.md
@.planning/phases/03-monetization/03-RESEARCH.md

@hooks/useProStatus.ts
@stores/proStore.ts
@app/(tabs)/settings.tsx
@components/Results/ResultCard.tsx
@i18n/en.ts
@i18n/it.ts
</context>

<tasks>

<task type="auto">
  <name>Create ProUpgradeModal component with benefits list</name>
  <files>components/ProUpgradeModal.tsx</files>
  <action>
Create components/ProUpgradeModal.tsx following CONTEXT.md specifications:

1. Import dependencies:
   import React from 'react';
   import { View, Text, StyleSheet, Modal, TouchableOpacity, ActivityIndicator } from 'react-native';
   import { useTranslation } from 'react-i18next';
   import { useSafeAreaInsets } from 'react-native-safe-area-context';
   import { useProStatus } from '@/hooks/useProStatus';
   import { PurchaseError } from '@/types';

2. Define props interface:
   interface ProUpgradeModalProps {
     visible: boolean;
     onClose: () => void;
     triggerReason?: 'scan_limit' | 'collection_limit' | 'manual';
   }

3. Create component structure:
   - Modal with animationType="slide" and transparent background
   - Safe area container
   - Header with close button
   - Title: "Unlock Pro" / "Sblocca Pro"
   - Benefits list (checkmark bullets):
     * 15 plant identifications per day
     * Unlimited plant collection
     * No ads, ever
     * One-time purchase (€4.99)
   - Prominent "No subscription" message
   - Price display: €4.99 (one-time)
   - Purchase button: "Remove Ads & Unlock All"
   - Loading state during purchase
   - Error display (toast message on failure)

4. Implement purchase flow:
   - Get { purchase, loading } from useProStatus()
   - On purchase button press:
     * Set loading state
     * Call purchase()
     * On success: call onClose()
     * On error: show error message, keep modal open

5. Style with:
   - Centered modal with white background
   - Rounded corners
   - Green CTA button (#2e7d32 or similar)
   - Checkmark icons for benefits
   - Bold "No subscription" text

6. Export component as default

DO NOT include subscription options — emphasize one-time payment only.
  </action>
  <verify>grep -E "ProUpgradeModal|No subscription|€4.99" components/ProUpgradeModal.tsx returns matches</verify>
  <done>ProUpgradeModal created with benefits list, purchase button, loading state, error handling</done>
</task>

<task type="auto">
  <name>Update Settings screen with Pro upgrade/restore buttons</name>
  <files>app/(tabs)/settings.tsx</files>
  <action>
Modify app/(tabs)/settings.tsx to add Pro upgrade and restore functionality:

1. Import dependencies (if not already present):
   import { useProStatus } from '@/hooks/useProStatus';
   import { useProStore } from '@/stores/proStore';
   import { ProUpgradeModal } from '@/components/ProUpgradeModal';

2. Add Pro status display:
   - Get isPro from useProStore
   - Display badge: "Free" (gray) or "Pro" (gold/green)
   - Position near top of settings screen

3. Add "Upgrade to Pro" button:
   - Show only if !isPro
   - Call useState to control ProUpgradeModal visibility
   - On press: set modal visible true
   - Style as prominent CTA button

4. Add "Restore Purchases" button:
   - Show for all users (required for App Store)
   - Get { restore, loading } from useProStatus()
   - On press: call restore()
   - Show success/error toast/alert based on result
   - Style as secondary button (less prominent)

5. Add ProUpgradeModal to render:
   - Controlled by local state
   - Pass onClose handler
   - Set triggerReason based on context (use 'manual')

6. If Pro, show "Thank you for being a Pro user!" message instead of upgrade button

Follow existing Settings screen structure and styling patterns.
  </action>
  <verify>grep -E "Upgrade to Pro|Restore Purchases|ProUpgradeModal" app/(tabs)/settings.tsx returns matches</verify>
  <done>Settings has Pro status badge, upgrade button (free users), restore button (all users), Pro upgrade modal</done>
</task>

<task type="auto">
  <name>Add Pro translation keys for upgrade modal</name>
  <files>i18n/en.ts, i18n/it.ts</files>
  <action>
Add translation keys for Pro upgrade modal to both i18n/en.ts and i18n/it.ts:

Create new "pro" namespace in both files:

en.ts pro namespace:
- upgradeTitle: "Unlock Pro"
- upgradeSubtitle: "Get more from Plantid"
- benefit15Scans: "15 plant identifications per day"
- benefitUnlimited: "Unlimited plant collection"
- benefitNoAds: "No ads, ever"
- benefitOneTime: "One-time purchase"
- noSubscription: "No subscription, ever"
- priceTag: "€4.99"
- oneTimePayment: "One-time payment"
- ctaButton: "Remove Ads & Unlock All — €4.99"
- proThankYou: "Thank you for being a Pro user!"
- restorePurchases: "Restore Purchases"
- restoreSuccess: "Your Pro purchase has been restored!"
- restoreFailed: "No previous purchases found"
- restoreError: "Could not restore purchases. Check your connection."
- purchaseError: "Purchase failed. Please try again."

it.ts pro namespace (formal Italian):
- upgradeTitle: "Sblocca Pro"
- upgradeSubtitle: "Ottieni di più da Plantid"
- benefit15Scans: "15 identificazioni piante al giorno"
- benefitUnlimited: "Collezione piante illimitata"
- benefitNoAds: "Nessuna pubblicità, mai"
- benefitOneTime: "Acquisto unico"
- noSubscription: "Nessun abbonamento, mai"
- priceTag: "€4.99"
- oneTimePayment: "Pagamento una tantum"
- ctaButton: "Rimuovi Annunci e Sblocca Tutto — €4.99"
- proThankYou: "Grazie per essere un utente Pro!"
- restorePurchases: "Ripristina Acquisti"
- restoreSuccess: "Il tuo acquisto Pro è stato ripristinato!"
- restoreFailed: "Nessun acquisto precedente trovato"
- restoreError: "Impossibile ripristinare gli acquisti. Verifica la connessione."
- purchaseError: "Acquisto fallito. Riprova."

Follow existing i18n structure with proper namespacing.
  </action>
  <verify>grep -A 20 "pro:" i18n/en.ts returns namespace with all keys</verify>
  <done>Pro translation namespace added in both languages with all upgrade modal keys</done>
</task>

<task type="auto">
  <name>Integrate upgrade modal with limit triggers</name>
  <files>hooks/useRateLimit.ts, app/(tabs)/camera.tsx, components/Results/ResultCard.tsx</files>
  <action>
Connect ProUpgradeModal to limit trigger points with exact wiring:

**Wiring 1: Camera screen (scan limit trigger)**

1. Update app/(tabs)/camera.tsx:
   - Import ProUpgradeModal: import { ProUpgradeModal } from '@/components/ProUpgradeModal';
   - Add state: const [upgradeModalVisible, setUpgradeModalVisible] = useState(false);
   - Get { canIdentify } from useRateLimit() hook
   - When user attempts to scan and canIdentify returns { allowed: false }:
     * Call setUpgradeModalVisible(true)
   - Render ProUpgradeModal at end of JSX:
     <ProUpgradeModal
       visible={upgradeModalVisible}
       onClose={() => setUpgradeModalVisible(false)}
       triggerReason="scan_limit"
     />

**Wiring 2: ResultCard component (collection limit trigger)**

2. Update components/Results/ResultCard.tsx:
   - Import ProUpgradeModal: import { ProUpgradeModal } from '@/components/ProUpgradeModal';
   - Add state: const [upgradeModalVisible, setUpgradeModalVisible] = useState(false);
   - Update handleAdd function (from Task 2 of plan 03-03):
     const success = addPlant(plant);
     if (!success) {
       setUpgradeModalVisible(true); // Show upgrade modal
       return;
     }
   - Pass setUpgradeModalVisible as optional prop to ResultCard:
     interface ResultCardProps {
       // ... existing props
       onUpgradeNeeded?: () => void;
     }
   - Alternative: Let ResultCard manage its own modal state as shown above
   - Render ProUpgradeModal at end of JSX:
     <ProUpgradeModal
       visible={upgradeModalVisible}
       onClose={() => setUpgradeModalVisible(false)}
       triggerReason="collection_limit"
     />

**Wiring 3: Update useRateLimit hook**

3. Update hooks/useRateLimit.ts (if needed):
   - Verify canIdentify returns { allowed: boolean, remaining: number, limit: number }
   - No changes to useRateLimit needed if it already returns allowed boolean
   - The calling components (camera.tsx) will check allowed and trigger modal

This integration creates natural upsell moments when users hit limits.
  </action>
  <verify>grep -r "ProUpgradeModal" app/ components/ returns matches in camera.tsx and ResultCard.tsx showing modal state and render</verify>
  <done>Upgrade modal integrated at scan limit (camera.tsx) and collection limit (ResultCard.tsx) with exact wiring specified</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. ProUpgradeModal component exists with all required sections
3. Modal shows benefits, price, "No subscription" message
4. Purchase button works with loading state
5. Settings screen has Pro status badge
6. Settings has "Upgrade to Pro" button (free users only)
7. Settings has "Restore Purchases" button (all users)
8. Upgrade modal triggered at scan limit
9. Upgrade modal triggered at collection limit
10. All translations exist in both languages
</verification>

<success_criteria>
1. Users see Pro upgrade modal when hitting scan or plant limits
2. Modal clearly communicates benefits and one-time pricing
3. "No subscription, ever" message is prominent
4. Purchase flow completes successfully with visual feedback
5. Settings screen provides upgrade access and restore functionality
6. Pro status visible in Settings
7. Restore purchases works for existing Pro users
</success_criteria>

<output>
After completion, create `.planning/phases/03-monetization/03-04-SUMMARY.md`
</output>
