---
phase: 03-monetization
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - services/rateLimiter.ts
  - stores/plantsStore.ts
  - hooks/useRateLimit.ts
  - components/Results/ResultCard.tsx
autonomous: true
requirements:
  - RATE-02
  - PRO-02
  - PRO-03

must_haves:
  truths:
    - Pro users have daily scan limit of 15 (vs 5 for free users)
    - Free users limited to 10 plants in collection, Pro users unlimited
    - Rate limiter checks Pro status from proStore before applying limits
    - Plant collection enforced at addPlant() level in plantsStore
    - All limit checks use Pro status dynamically (no cached values)
    - ResultCard handles boolean return from addPlant() correctly
  artifacts:
    - path: "services/rateLimiter.ts"
      provides: "Pro-aware rate limiting"
      exports: ["canIdentify", "incrementIdentificationCount", "getRateLimitInfo"]
      contains: "getDailyLimit"
    - path: "stores/plantsStore.ts"
      provides: "Collection limit enforcement for free users"
      exports: ["addPlant"]
      contains: "MAX_PLANTS_FREE"
    - path: "hooks/useRateLimit.ts"
      provides: "Updated hook with Pro limit support"
      exports: ["useRateLimit"]
    - path: "components/Results/ResultCard.tsx"
      provides: "Handles boolean return from addPlant"
      contains: "const success = addPlant"
  key_links:
    - from: "services/rateLimiter.ts"
      to: "stores/proStore.ts"
      via: "Import and direct store access"
      pattern: "useProStore"
    - from: "stores/plantsStore.ts"
      to: "stores/proStore.ts"
      via: "Import and direct store access"
      pattern: "useProStore"
    - from: "hooks/useRateLimit.ts"
      to: "services/rateLimiter.ts"
      via: "Import and function calls"
      pattern: "canIdentify|getRateLimitInfo"
    - from: "components/Results/ResultCard.tsx"
      to: "stores/plantsStore.ts"
      via: "Import and addPlant call with boolean check"
      pattern: "const success = addPlant"
---

<objective>
Modify rate limiting and plant collection logic to respect Pro status. Pro users get elevated limits: 15 scans per day (vs 5) and unlimited plant collection (vs 10 plant cap for free users). This plan implements the core value proposition of Pro unlock while maintaining fair usage limits.

Purpose: Deliver the Pro benefits promised in the upgrade flow — higher scan volume and unrestricted plant collection.

Output: Updated rateLimiter.ts with Pro-aware limits, plantsStore.ts with collection cap, useRateLimit.ts hook reflecting new limits.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monetization/03-CONTEXT.md
@.planning/phases/03-monetization/03-RESEARCH.md

@stores/proStore.ts
@services/rateLimiter.ts
@stores/plantsStore.ts
@hooks/useRateLimit.ts
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Modify rateLimiter.ts for Pro-aware scan limits</name>
  <files>services/rateLimiter.ts</files>
  <action>
Modify services/rateLimiter.ts to support Pro user limits:

1. Import useProStore at top:
   import { useProStore } from '@/stores/proStore';

2. Replace hardcoded DAILY_LIMIT constant with a function:
   function getDailyLimit(): number {
     const { isPro } = useProStore.getState();
     return isPro ? 15 : 5;
   }

3. IMPORTANT: Remove the old DAILY_LIMIT constant if it exists:
   - Search for: const DAILY_LIMIT = 5;
   - Delete this line completely
   - All references must use getDailyLimit() instead

4. Update canIdentify() function:
   - Replace DAILY_LIMIT references with getDailyLimit()
   - The logic remains the same, but limit is now dynamic based on Pro status
   - Return value should reflect the user's current limit

5. Update incrementIdentificationCount() function:
   - No changes needed — just increments count, limits are enforced at check time

6. Update getRateLimitInfo() function:
   - Replace DAILY_LIMIT references with getDailyLimit()
   - Ensure returned limit value reflects Pro status

7. Add JSDoc comment explaining Pro limits:
   /**
    * Free users: 5 identifications per day
    * Pro users: 15 identifications per day
    * Resets at midnight in user's local timezone
    */

DO NOT modify the mutex logic, date handling, or AsyncStorage operations — only the limit value.
  </action>
  <verify>grep -E "getDailyLimit|useProStore" services/rateLimiter.ts returns matches && ! grep -q "const DAILY_LIMIT" services/rateLimiter.ts</verify>
  <done>rateLimiter uses getDailyLimit() function that checks Pro status, old DAILY_LIMIT constant removed</done>
</task>

<task type="auto">
  <name>Add collection limit to plantsStore.ts</name>
  <files>stores/plantsStore.ts</files>
  <action>
Modify stores/plantsStore.ts to enforce collection limit for free users:

1. Import useProStore at top (if not already present):
   import { useProStore } from '@/stores/proStore';

2. Add constant for free user limit:
   const MAX_PLANTS_FREE = 10;

3. Modify addPlant() function:
   - Add collection limit check at start of function
   - Get Pro status: const { isPro } = useProStore.getState();
   - Check limit: if (!isPro && plants.length >= MAX_PLANTS_FREE) return;
   - Or return a boolean success/failure instead of silent return

4. Better approach — return boolean from addPlant:
   addPlant: (plant: SavedPlant) => {
     const { isPro } = useProStore.getState();
     const plants = get().plants;
     if (!isPro && plants.length >= MAX_PLANTS_FREE) {
       return false; // Collection full
     }
     set((state) => ({
       plants: [{ ...plant, id: plant.id || crypto.randomUUID() }, ...state.plants]
     }));
     return true; // Success
   }

5. Update PlantsState interface to reflect boolean return:
   addPlant: (plant: SavedPlant) => boolean;

6. Add JSDoc comment:
   /**
    * Add plant to collection. Returns false if free user has reached limit (10 plants).
    * Pro users have unlimited collection size.
    */

DO NOT modify removePlant, getPlant, updatePlant, or cancelAllNotifications functions.
  </action>
  <verify>grep -E "MAX_PLANTS_FREE|isPro.*plants\\.length" stores/plantsStore.ts returns matches</verify>
  <done>Collection limit enforced in addPlant, Pro users bypass limit, function returns boolean success</done>
</task>

<task type="auto">
  <name>Update addPlant() call sites to handle boolean return</name>
  <files>components/Results/ResultCard.tsx</files>
  <action>
Update components/Results/ResultCard.tsx to handle the boolean return from addPlant():

1. Locate the handleAdd function (around line 85)
2. Update to check return value:
   const handleAdd = () => {
     if (added) return;

     const plant: SavedPlant = {
       id: ExpoCrypto.randomUUID(),
       species: scientificNameStr,
       scientificName: scientificNameStr,
       commonName: species.commonNames?.[0],
       photo: imageUri,
       addedDate: new Date().toISOString(),
       waterHistory: [],
     };

     const success = addPlant(plant);
     if (!success) {
       // Collection full - do not set added state
       // Upgrade modal will be shown by parent component
       return;
     }

     setAdded(true);

     if (onAddToCollection) {
       onAddToCollection(plant);
     }
   };

3. The parent component (results.tsx) will detect the failed add and show ProUpgradeModal

This prevents the UI from showing "Added" when the plant wasn't actually saved due to the collection limit.
  </action>
  <verify>grep -E "const success = addPlant|if \(!success\)" components/Results/ResultCard.tsx returns matches</verify>
  <done>ResultCard checks addPlant return value, doesn't show "Added" state when collection is full</done>
</task>

<task type="auto">
  <name>Update useRateLimit hook to reflect Pro limits</name>
  <files>hooks/useRateLimit.ts</files>
  <action>
Modify hooks/useRateLimit.ts to reflect dynamic Pro limits:

1. Review existing hook implementation
2. Ensure limit value displayed to users is dynamic (not hardcoded)
3. Update any UI text references to "5/day" to reflect the actual limit
4. Add comment explaining Pro users see "15/day"

If the hook uses rateLimiter.getRateLimitInfo(), it should already return the correct dynamic limit. No changes may be needed — just verify and add comments.

Key changes (if needed):
- Ensure displayed limit comes from getRateLimitInfo().limit
- Add Pro status check for UI messaging (e.g., "Upgrade to Pro for 15 scans/day")
- Keep all existing logic intact

DO NOT modify the core rate limit check logic — that's handled in rateLimiter.ts.
  </action>
  <verify>grep -E "getRateLimitInfo|limit" hooks/useRateLimit.ts returns matches showing dynamic limit usage</verify>
  <done>Hook displays dynamic limit from rateLimiter, no hardcoded limit values</done>
</task>

<task type="auto">
  <name>Add translation keys for limit messages</name>
  <files>i18n/en.ts, i18n/it.ts</files>
  <action>
Add translation keys for Pro-related limit messages:

en.ts — add to appropriate namespace (e.g., 'common' or new 'pro' namespace):
- collectionFull: "Free users can save up to 10 plants. Upgrade to Pro for unlimited collection."
- scanLimitPro: "Pro users get 15 scans per day. You've used {{used}} of {{limit}}."
- upgradeForMore: "Upgrade to Pro"

it.ts — formal Italian translations:
- collectionFull: "Gli utenti gratuiti possono salvare fino a 10 piante. Passa a Pro per una collezione illimitata."
- scanLimitPro: "Gli utenti Pro hanno 15 scansioni al giorno. Hai usato {{used}} di {{limit}}."
- upgradeForMore: "Passa a Pro"

Follow existing i18n structure and interpolation patterns.
  </action>
  <verify>grep -E "collectionFull|scanLimitPro|upgradeForMore" i18n/en.ts returns matches</verify>
  <done>Translation keys added in both languages for Pro-related limit messages</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. rateLimiter.ts uses getDailyLimit() function that checks Pro status
3. Free users limited to 5 scans/day, Pro users limited to 15 scans/day
4. plantsStore.ts enforces 10 plant cap for free users
5. Pro users bypass collection limit (unlimited plants)
6. addPlant() returns boolean indicating success/failure
7. Translation keys exist for limit messages
8. useRateLimit hook displays correct dynamic limit
</verification>

<success_criteria>
1. Rate limiter respects Pro status (5 vs 15 scans per day)
2. Plant collection capped at 10 for free users, unlimited for Pro
3. addPlant returns false when free user hits collection limit
4. UI can display appropriate upgrade prompts at limits
5. All limit checks use real-time Pro status (no caching issues)
</success_criteria>

<output>
After completion, create `.planning/phases/03-monetization/03-03-SUMMARY.md`
</output>
