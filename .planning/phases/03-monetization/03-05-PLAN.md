---
phase: 03-monetization
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - app/(tabs)/index.tsx
  - app/(tabs)/camera.tsx
  - app/(tabs)/settings.tsx
  - app/plant/[id].tsx
autonomous: true
requirements:
  - AD-01
  - AD-02
  - PRO-01

must_haves:
  truths:
    - BannerAdWrapper rendered on all tab screens (Home, Camera, Settings)
    - BannerAdWrapper rendered on plant detail screen
    - Pro users see no ads on any screen (component returns null)
    - Free users see banner ad at bottom of screen
    - Ad positioned above tab bar (not overlapping)
    - Safe area padding applied on notched devices
  artifacts:
    - path: "app/(tabs)/index.tsx"
      provides: "Home screen with banner ad"
      contains: "BannerAdWrapper"
    - path: "app/(tabs)/camera.tsx"
      provides: "Camera screen with banner ad"
      contains: "BannerAdWrapper"
    - path: "app/(tabs)/settings.tsx"
      provides: "Settings screen with banner ad"
      contains: "BannerAdWrapper"
    - path: "app/plant/[id].tsx"
      provides: "Plant detail screen with banner ad"
      contains: "BannerAdWrapper"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "components/BannerAdWrapper.tsx"
      via: "Import and render"
      pattern: "BannerAdWrapper"
    - from: "app/(tabs)/camera.tsx"
      to: "components/BannerAdWrapper.tsx"
      via: "Import and render"
      pattern: "BannerAdWrapper"
    - from: "app/(tabs)/settings.tsx"
      to: "components/BannerAdWrapper.tsx"
      via: "Import and render"
      pattern: "BannerAdWrapper"
    - from: "app/plant/[id].tsx"
      to: "components/BannerAdWrapper.tsx"
      via: "Import and render"
      pattern: "BannerAdWrapper"
---

<objective>
Integrate BannerAdWrapper into all main screens of the app. This plan places banner ads on the Home, Camera, Settings, and Plant Detail screens, completing the ad monetization implementation. Pro users see no ads (BannerAdWrapper returns null), while free users see non-intrusive banners at the bottom of each screen.

Purpose: Complete the ad-based monetization layer while maintaining clean UX for Pro users who have paid to remove ads.

Output: All tab screens and plant detail screen render BannerAdWrapper, ads show for free users, hidden for Pro users.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monetization/03-CONTEXT.md
@.planning/phases/03-monetization/03-RESEARCH.md

@components/BannerAdWrapper.tsx
@app/(tabs)/_layout.tsx
@app/(tabs)/index.tsx
@app/(tabs)/camera.tsx
@app/(tabs)/settings.tsx
@app/plant/[id].tsx
</context>

<tasks>

<task type="auto">
  <name>Integrate BannerAdWrapper into Home screen (index.tsx)</name>
  <files>app/(tabs)/index.tsx</files>
  <action>
Modify app/(tabs)/index.tsx to render BannerAdWrapper:

1. Import BannerAdWrapper at top:
   import { BannerAdWrapper } from '@/components/BannerAdWrapper';

2. Import View style helper (if needed for positioning):
   The ad wrapper uses absolute positioning, so it should overlay correctly without layout changes.

3. Add BannerAdWrapper to render:
   - Place at the end of the component's JSX tree
   - Render outside of ScrollView (as a sibling to avoid scroll issues)
   - Wrap main content and BannerAdWrapper in a Fragment or View

Example structure:
   return (
     <>
       <ScrollView style={styles.container}>
         {/* existing content */}
       </ScrollView>
       <BannerAdWrapper />
     </>
   );

4. Verify z-index layering:
   - BannerAdWrapper should be above tab bar (z-index handled by component)
   - Content should remain scrollable without obstruction

DO NOT modify existing Home screen layout or functionality beyond adding the ad wrapper.
  </action>
  <verify>grep -q "BannerAdWrapper" app/(tabs)/index.tsx</verify>
  <done>Home screen renders BannerAdWrapper, no layout issues, ad positioned at bottom</done>
</task>

<task type="auto">
  <name>Integrate BannerAdWrapper into Camera screen</name>
  <files>app/(tabs)/camera.tsx</files>
  <action>
Modify app/(tabs)/camera.tsx to render BannerAdWrapper:

1. Import BannerAdWrapper at top:
   import { BannerAdWrapper } from '@/components/BannerAdWrapper';

2. Add BannerAdWrapper to render:
   - Place at end of JSX tree (outside camera preview container)
   - Use Fragment wrapper if needed
   - Ensure ad doesn't overlay camera preview controls

Example:
   return (
     <View style={styles.container}>
       {/* camera preview and controls */}
       <BannerAdWrapper />
     </View>
   );

3. Test that ad doesn't interfere with:
   - Camera permission prompt
   - Take photo button
   - Gallery pick button
   - Organ selector

DO NOT modify existing camera functionality.
  </action>
  <verify>grep -q "BannerAdWrapper" app/(tabs)/camera.tsx</verify>
  <done>Camera screen renders BannerAdWrapper, controls remain accessible</done>
</task>

<task type="auto">
  <name>Integrate BannerAdWrapper into Settings screen</name>
  <files>app/(tabs)/settings.tsx</files>
  <action>
Modify app/(tabs)/settings.tsx to render BannerAdWrapper:

1. Import BannerAdWrapper at top (if not already imported from plan 03-04):
   import { BannerAdWrapper } from '@/components/BannerAdWrapper';

2. Add BannerAdWrapper to render:
   - Place at end of JSX tree
   - Outside ScrollView if present
   - Use Fragment wrapper

Example:
   return (
     <>
       <ScrollView style={styles.container}>
         {/* existing settings content */}
       </ScrollView>
       <BannerAdWrapper />
     </>
   );

3. Ensure ad doesn't overlap:
   - Pro upgrade button
   - Restore purchases button
   - Language switcher
   - Other settings controls

DO NOT modify existing Settings functionality.
  </action>
  <verify>grep -q "BannerAdWrapper" app/(tabs)/settings.tsx</verify>
  <done>Settings screen renders BannerAdWrapper, all controls accessible</done>
</task>

<task type="auto">
  <name>Integrate BannerAdWrapper into Plant Detail screen</name>
  <files>app/plant/[id].tsx</files>
  <action>
Modify app/plant/[id].tsx to render BannerAdWrapper:

1. Import BannerAdWrapper at top:
   import { BannerAdWrapper } from '@/components/BannerAdWrapper';

2. Add BannerAdWrapper to render:
   - Place at end of JSX tree
   - Outside ScrollView (plant details can be long)
   - Use Fragment wrapper

Example:
   return (
     <>
       <ScrollView style={styles.container}>
         {/* plant details, care info, watering history */}
       </ScrollView>
       <BannerAdWrapper />
     </>
   );

3. Ensure ad doesn't overlap:
   - "Mark Watered" button
   - Plant image
   - Care information
   - Watering history calendar
   - Delete button

DO NOT modify existing plant detail functionality.
  </action>
  <verify>grep -q "BannerAdWrapper" app/plant/[id].tsx</verify>
  <done>Plant detail screen renders BannerAdWrapper, all content accessible</done>
</task>

<task type="auto">
  <name>Verify ad behavior and Pro gating</name>
  <files>components/BannerAdWrapper.tsx</files>
  <action>
Verify BannerAdWrapper behavior across all screens:

1. Check Pro status gating:
   - Confirm useProStore is imported and used
   - Verify early return null when isPro is true
   - Test: Pro users should see no ads on any screen

2. Check ad positioning:
   - Verify position: 'absolute', bottom: 0
   - Verify SafeArea inset applied (paddingBottom: insets.bottom)
   - Ensure ad doesn't overlap tab bar (should be above it)

3. Check error handling:
   - Verify onAdFailedToLoad handler exists
   - Confirm ad hides cleanly on failure (no placeholder)

4. Check ad loading state:
   - Verify space reserved for ad (no layout shift)
   - Confirm white background behind ad

5. Test ad visibility on different screen sizes:
   - iPhone notch (SafeArea handling)
   - Android notch/gesture bar
   - Regular phones

Create a checklist of verification steps for user testing:
- [ ] Free user sees ad on Home screen
- [ ] Free user sees ad on Camera screen
- [ ] Free user sees ad on Settings screen
- [ ] Free user sees ad on Plant Detail screen
- [ ] Pro user sees NO ads on any screen
- [ ] Ad positioned above tab bar (not overlapping)
- [ ] Safe area padding correct on notched devices
- [ ] Ad hides cleanly on load failure

NO CODE CHANGES REQUIRED — this is a verification task. Add comments to BannerAdWrapper.tsx documenting expected behavior if helpful.
  </action>
  <verify>grep -E "isPro.*return null|onAdFailedToLoad|position.*absolute" components/BannerAdWrapper.tsx returns matches</verify>
  <done>Ad behavior verified across all screens, Pro gating working, positioning correct</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. BannerAdWrapper imported in all 4 screens
3. Each screen renders BannerAdWrapper at bottom
4. Pro users see no ads (component returns null)
5. Free users see banner ads on all screens
6. Ads positioned above tab bar (not overlapping controls)
7. Safe area padding applied on notched devices
8. No layout shifts when ad loads
9. Clean fallback on ad load failure
</verification>

<success_criteria>
1. All main screens display banner ads for free users
2. Pro users see completely ad-free experience
3. Ads don't interfere with app functionality
4. Ad positioning is consistent across screens
5. Safe area handling correct on all devices
6. Error handling prevents broken UI when ads fail
</success_criteria>

<output>
After completion, create `.planning/phases/03-monetization/03-05-SUMMARY.md`
</output>
