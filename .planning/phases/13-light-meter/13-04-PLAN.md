---
phase: 13-light-meter
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - v2.1-light-meter

must_haves:
  truths:
    - "REQUIREMENTS.md contains a dedicated v2.1-light-meter section distinct from v2.0 community requirements"
    - "The traceability table maps v2.1-light-meter to Phase 13-light-meter with status Complete"
    - "FEED-01 through MODR-11 remain mapped to Phase 13 (Community Feed) — no data is removed"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements document with v2.1 Smart Features section and light meter requirement"
      contains: "v2.1-light-meter"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "Phase 13-light-meter"
      via: "v2.1-light-meter row"
      pattern: "v2\\.1-light-meter.*Phase 13-light-meter"
---

<objective>
Update REQUIREMENTS.md to add a `v2.1 Smart Features` section with the light meter requirement (`v2.1-light-meter`), and add a traceability row mapping it to Phase 13-light-meter.

Purpose: The verification report found that REQUIREMENTS.md has no entry for the light meter feature. The document only covers v2.0 Community requirements (FEED-01 through MODR-11), which belong to Phase 13 (Community Feed). The 13-light-meter phase uses `v2.1-light-meter` as its requirement ID, but this ID is never defined or traced in REQUIREMENTS.md, creating a documentation gap.

Output: Updated REQUIREMENTS.md with v2.1 Smart Features section and complete traceability row for v2.1-light-meter.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-light-meter/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v2.1 Smart Features section and traceability to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Read the full current REQUIREMENTS.md first.

    Make two additions:

    **Addition 1 — New section before "Out of Scope":**

    Insert a new top-level section `## v2.1 Requirements` between the end of the `## v2.1+ Requirements` block (Plant ID Help Feed / Care Tips Wiki / Push Notifications) and the `## Out of Scope` section. Use this content:

    ```markdown
    ## v2.1 Smart Features

    Requirements for the Smart Features milestone. Inserted between Phase 12 and Phase 13.

    ### Light Meter

    - [x] **v2.1-light-meter**: Users can measure ambient light levels (lux) to determine optimal plant placement — Android uses native expo-sensors LightSensor (±15% accuracy), iOS uses camera-based brightness estimation (±30% estimate); color-coded category gauge (Low/Medium/Bright Indirect/Direct Sun); plant recommendations per category; save measurement to plant location field; bilingual EN/IT support
    ```

    **Addition 2 — Traceability table:**

    In the Traceability section (`## Traceability`), add a new row after the `PROF-08` row and before the `FEED-01` row:

    ```
    | v2.1-light-meter | Phase 13-light-meter | Complete |
    ```

    Also update the Coverage counts at the bottom of the Traceability section:
    - Change `v2.0 requirements: 50 total` to `v2.0 requirements: 50 total`  (unchanged)
    - Add below it: `v2.1 requirements: 1 total`
    - Change `Mapped to phases: 50` to `Mapped to phases: 51`
    - Change `Unmapped: 0` to `Unmapped: 0`

    Also update the `*Last updated:` line at the bottom to `2026-03-04 after light meter gap closure`.

    Do NOT change or remove any existing content. Only add the new section and the traceability row.
  </action>
  <verify>
    <automated>grep -n "v2.1-light-meter" /Users/martha2022/Documents/Plantid/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md contains the string "v2.1-light-meter" in both the requirements section and the traceability table. The existing FEED-01 through MODR-11 rows are untouched.
  </done>
</task>

</tasks>

<verification>
After task completion:
1. `grep -n "v2.1-light-meter" .planning/REQUIREMENTS.md` returns at least 2 lines (section definition + traceability row)
2. `grep -n "FEED-01" .planning/REQUIREMENTS.md` still returns results (existing content preserved)
3. `grep -n "Phase 13-light-meter" .planning/REQUIREMENTS.md` returns the traceability row
</verification>

<success_criteria>
REQUIREMENTS.md has a `## v2.1 Smart Features` section with `v2.1-light-meter` defined and checked off, and the traceability table maps it to Phase 13-light-meter with status Complete. All v2.0 community requirements (FEED-01 through MODR-11) remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/13-light-meter/13-04-SUMMARY.md` following the standard summary template.
</output>
