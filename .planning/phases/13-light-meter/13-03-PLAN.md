---
phase: 13-light-meter
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - components/LightMeter/LightMeterGauge.tsx
  - components/LightMeter/LightCategoryBar.tsx
  - app/light-meter.tsx
  - i18n/resources/en.json
  - i18n/resources/it.json
autonomous: false
requirements:
  - v2.1-light-meter
must_haves:
  truths:
    - Light meter screen shows real-time lux value with large prominent display
    - Color-coded progress bar indicates current light category
    - Plant recommendations appear based on current light category
    - Users can save measurements linked to specific plants
    - Accuracy indicator shows "±15%" on Android, "Estimate (±30%)" on iOS
    - Feature accessible from plant onboarding flow
  artifacts:
    - path: "components/LightMeter/LightMeterGauge.tsx"
      provides: "Main gauge component with lux display and category bar"
      exports: ["LightMeterGauge"]
    - path: "components/LightMeter/LightCategoryBar.tsx"
      provides: "Color-coded category visualization"
      exports: ["LightCategoryBar"]
    - path: "app/light-meter.tsx"
      provides: "Full light meter screen with routing"
      min_lines: 150
    - path: "i18n/resources/en.json"
      provides: "English translations for light meter"
      contains: ["lightMeter.title", "lightMeter.categories.low"]
    - path: "i18n/resources/it.json"
      provides: "Italian translations for light meter"
      contains: ["lightMeter.title", "lightMeter.categories.low"]
  key_links:
    - from: "app/light-meter.tsx"
      to: "hooks/useLightSensor.ts"
      via: "hook import for Android"
      pattern: "useLightSensor"
    - from: "app/light-meter.tsx"
      to: "hooks/useCameraLightEstimator.ts"
      via: "hook import for iOS"
      pattern: "useCameraLightEstimator"
    - from: "components/LightMeter/LightMeterGauge.tsx"
      to: "types/lightMeter.ts"
      via: "LightCategory type"
      pattern: "LightCategory"
    - from: "app/light-meter.tsx"
      to: "stores/plantsStore.ts"
      via: "save measurement to plant"
      pattern: "plantsStore"
user_setup: []
---

<objective>
Create complete Light Meter UI with gauge, category visualization, plant recommendations, and onboarding integration.

Purpose: Provide intuitive, visually appealing interface for measuring light levels with actionable plant guidance.
Output: Full-featured light meter screen with bilingual support, plant linking, and seamless UX.
</objective>

<execution_context>
@/Users/martha2022/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martha2022/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-light-meter/13-CONTEXT.md
@.planning/phases/13-light-meter/13-RESEARCH.md
@types/lightMeter.ts
@hooks/useLightSensor.ts
@hooks/useCameraLightEstimator.ts

## Prior Art

From RESEARCH.md competitor analysis (PlantIn):
```
[Camera Preview / Sensor Reading]
[Large Lux Value Display]
[Progress Bar: Low ▓▓▓░░░ Bright]
[Recommendation Card: "Perfect for Snake Plants!"]
[Save Measurement Button]
```

### Colors by Category (Locked Decision)
- Low Light: Blue/Gray tones
- Medium Light: Green/Yellow tones
- Bright Indirect: Orange/Amber tones
- Direct Sun: Red/Yellow tones

### Integration Points
- Add to main navigation or plant detail
- Accessible during "Add Plant" onboarding flow
- Can save measurement to plant's location data

### Existing Patterns
- ThemedCard wrapper from Phase 12 (surface background color)
- Pill buttons from search filter (colors.tint background)
- Ionicons for icons (already used throughout app)
- i18next for translations (en/it supported)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LightCategoryBar component</name>
  <files>components/LightMeter/LightCategoryBar.tsx</files>
  <action>
Create the color-coded category progress bar component:

1. Component props:
   ```typescript
   interface LightCategoryBarProps {
     currentLux: number | null;
     style?: ViewStyle;
   }
   ```

2. Visual design:
   - Horizontal bar divided into 4 segments representing categories
   - Segment widths proportional to lux range:
     - Low (500-2500): ~10% width
     - Medium (2500-10000): ~37% width
     - Bright Indirect (10000-20000): ~33% width
     - Direct Sun (20000+): ~20% width
   - Each segment has distinct color:
     - Low: #607D8B (blue-grey)
     - Medium: #8BC34A (light green)
     - Bright Indirect: #FF9800 (orange)
     - Direct Sun: #F44336 (red)
   - Active segment highlighted/brighter
   - Current position indicator (marker or arrow)

3. Labels:
   - Show category names below bar
   - Highlight current category label
   - Use themed text colors from Colors.ts

4. Animation:
   - Smooth transition when lux changes
   - Indicator animates to new position

5. Empty state:
   - When currentLux is null, show grayed-out bar
   - "Tap measure to start" hint

Use React Native Animated for smooth transitions.
  </action>
  <verify>
    <automated>grep -q "LightCategoryBar" components/LightMeter/LightCategoryBar.tsx && grep -q "currentLux" components/LightMeter/LightCategoryBar.tsx && echo "Category bar component created"</automated>
  </verify>
  <done>Component renders 4-segment bar with colors, indicator, and labels</done>
</task>

<task type="auto">
  <name>Task 2: Create LightMeterGauge component</name>
  <files>components/LightMeter/LightMeterGauge.tsx</files>
  <action>
Create the main gauge component with lux display:

1. Component props:
   ```typescript
   interface LightMeterGaugeProps {
     lux: number | null;
     category: LightCategory | null;
     accuracy: 'high' | 'estimate';
     isLoading?: boolean;
     onMeasure?: () => void;
     onSave?: () => void;
   }
   ```

2. Layout structure:
   ```
   ┌─────────────────────────────┐
   │  [Accuracy Badge]           │
   │                             │
   │        ┌─────┐              │
   │        │ LUX │              │
   │        │1240 │              │
   │        └─────┘              │
   │                             │
   │  Medium Light               │
   │  [=====>.......]            │
   │                             │
   │  [Recommendations...]       │
   │                             │
   │  [Measure] [Save]           │
   └─────────────────────────────┘
   ```

3. Elements:
   - Large lux value (48pt font minimum)
   - "LUX" label above value
   - Accuracy badge: pill shape
     - High: "±15% accuracy" (green)
     - Estimate: "Estimate (±30%)" (orange)
   - Category name below value
   - LightCategoryBar component integrated
   - Recommendation card showing suitable plants
   - Action buttons row

4. Recommendation logic:
   - Map categories to plant types:
     - Low: Snake Plant, ZZ Plant, Pothos
     - Medium: Monstera, Philodendron, Spider Plant
     - Bright Indirect: Fiddle Leaf Fig, Bird of Paradise
     - Direct Sun: Cacti, Succulents, Citrus
   - Show 2-3 suggestions as chips
   - "See all plants" link (future enhancement)

5. Styling:
   - Use themed colors from Colors.ts
   - Card container with surface background
   - Consistent padding/spacing with app standards
   - Responsive layout for different screen sizes

6. Loading state:
   - Skeleton or spinner when isLoading=true
   - Disable buttons during loading

Import LightCategoryBar from same directory.
  </action>
  <verify>
    <automated>grep -q "LightMeterGauge" components/LightMeter/LightMeterGauge.tsx && grep -q "LightCategoryBar" components/LightMeter/LightMeterGauge.tsx && echo "Gauge component with category bar"</automated>
  </verify>
  <done>Gauge displays lux value, accuracy badge, category bar, recommendations, action buttons</done>
</task>

<task type="auto">
  <name>Task 3: Create light meter screen</name>
  <files>app/light-meter.tsx</files>
  <action>
Create the full light meter screen with platform adaptation:

1. Screen structure:
   - Stack.Screen with title "Light Meter" / "Misuratore Luce"
   - ScrollView for content
   - SafeAreaView for notch handling

2. Platform detection and hook selection:
   ```typescript
   const isAndroid = Platform.OS === 'android';
   const { lux, category, status, start, stop } = isAndroid
     ? useLightSensor()
     : useCameraLightEstimator();
   ```

3. States to handle:
   - Initial: Show instructions, "Start Measurement" button
   - Measuring: Show camera preview (iOS) or sensor active (Android)
   - Results: Show LightMeterGauge with reading
   - Saving: Modal or inline form to link to plant

4. iOS-specific UI:
   - CameraPreview component when measuring
   - Calibration button in header or settings
   - Permission request UI if camera denied

5. Android-specific UI:
   - Visual indicator when sensor active
   - Fallback message if sensor unavailable

6. Save measurement flow:
   - Tap "Save to Plant" opens plant selector
   - List user's plants from plantsStore
   - Option to save as "location" measurement
   - Store: { plantId, lux, category, timestamp, location? }

7. History section (optional enhancement):
   - Show last 3-5 measurements
   - Timestamps and locations

8. Educational content:
   - "How to measure" tips
   - Brief explanation of light categories
   - Disclaimer about consumer-grade accuracy

Use existing navigation patterns from other app screens.
  </action>
  <verify>
    <automated>grep -q "useLightSensor\|useCameraLightEstimator" app/light-meter.tsx && grep -q "LightMeterGauge" app/light-meter.tsx && echo "Screen integrates hooks and gauge"</automated>
  </verify>
  <done>Screen works on both platforms, handles permissions, shows gauge, supports saving</done>
</task>

<task type="auto">
  <name>Task 4: Add i18n translations</name>
  <files>i18n/resources/en.json, i18n/resources/it.json</files>
  <action>
Add bilingual translations for light meter feature:

1. English translations (en.json):
   ```json
   {
     "lightMeter": {
       "title": "Light Meter",
       "subtitle": "Measure ambient light for optimal plant placement",
       "start": "Start Measurement",
       "stop": "Stop",
       "save": "Save to Plant",
       "measuring": "Measuring...",
       "accuracy": {
         "high": "±15% accuracy",
         "estimate": "Estimate (±30%)"
       },
       "categories": {
         "low": "Low Light",
         "medium": "Medium Light",
         "brightIndirect": "Bright Indirect",
         "directSun": "Direct Sun"
       },
       "recommendations": {
         "title": "Good for:",
         "low": "Snake Plants, ZZ Plants, Pothos",
         "medium": "Monstera, Philodendron, Spider Plant",
         "brightIndirect": "Fiddle Leaf Fig, Bird of Paradise",
         "directSun": "Cacti, Succulents, Citrus"
       },
       "instructions": {
         "title": "How to measure",
         "point": "Point your device toward the light source",
         "steady": "Hold steady for best results",
         "multiple": "Take multiple readings throughout the day"
       },
       "disclaimer": "This is a guide only. For precise measurements, use professional equipment.",
       "calibration": {
         "title": "Calibrate",
         "instruction": "Place a white paper in view and select a known light level",
         "reference": "Reference brightness",
         "save": "Save Calibration"
       }
     }
   }
   ```

2. Italian translations (it.json):
   ```json
   {
     "lightMeter": {
       "title": "Misuratore Luce",
       "subtitle": "Misura la luce ambiente per il posizionamento ottimale delle piante",
       "start": "Avvia Misurazione",
       "stop": "Stop",
       "save": "Salva su Pianta",
       "measuring": "Misurazione in corso...",
       "accuracy": {
         "high": "Precisione ±15%",
         "estimate": "Stima (±30%)"
       },
       "categories": {
         "low": "Luce Bassa",
         "medium": "Luce Media",
         "brightIndirect": "Luce Indiretta Forte",
         "directSun": "Sole Diretto"
       },
       "recommendations": {
         "title": "Ideale per:",
         "low": "Sansevieria, Zamioculcas, Pothos",
         "medium": "Monstera, Philodendron, Chlorophytum",
         "brightIndirect": "Ficus Lyrata, Sterlizia",
         "directSun": "Cactus, Succulente, Agrumi"
       },
       "instructions": {
         "title": "Come misurare",
         "point": "Punta il dispositivo verso la fonte di luce",
         "steady": "Tieni fermo per risultati migliori",
         "multiple": "Effettua più misurazioni durante il giorno"
       },
       "disclaimer": "Questa è solo una guida. Per misurazioni precise, usa attrezzatura professionale.",
       "calibration": {
         "title": "Calibra",
         "instruction": "Posiziona un foglio bianco nell'inquadratura e seleziona un livello di luce noto",
         "reference": "Luminosità di riferimento",
         "save": "Salva Calibrazione"
       }
     }
   }
   ```

3. Follow existing i18n patterns in the project.
   </action>
  <verify>
    <automated>grep -q "lightMeter" i18n/resources/en.json && grep -q "lightMeter" i18n/resources/it.json && echo "Translations added for both languages"</automated>
  </verify>
  <done>Both en.json and it.json contain complete lightMeter namespace with all UI strings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Light Meter feature with dual-platform support</what-built>
  <how-to-verify>
    1. Open the app and navigate to the Light Meter screen
    2. **Android test:**
       - Tap "Start Measurement"
       - Verify real-time lux readings appear
       - Check that accuracy shows "±15%"
       - Move device between light/dark areas - values should change
    3. **iOS test:**
       - Grant camera permission when prompted
       - Verify camera preview appears
       - Point at different light sources - values should change
       - Check that accuracy shows "Estimate (±30%)"
       - Verify auto-stop after 30 seconds
    4. **Both platforms:**
       - Verify color-coded category bar updates
       - Check plant recommendations match category
       - Try "Save to Plant" flow
       - Switch language - verify Italian translations appear
    5. Check dark mode compatibility
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] LightCategoryBar shows 4 colored segments with indicator
- [ ] LightMeterGauge displays large lux value with accuracy badge
- [ ] Plant recommendations appear based on current category
- [ ] app/light-meter.tsx handles both Android and iOS
- [ ] Camera preview shown on iOS during measurement
- [ ] Save to Plant flow works end-to-end
- [ ] i18n translations complete for EN and IT
- [ ] Dark mode styling applied
- [ ] User verified functionality on device/simulator
</verification>

<success_criteria>
1. Light meter screen accessible from app navigation
2. Real-time readings on both platforms with appropriate accuracy labels
3. Color-coded gauge visualizes light category clearly
4. Plant recommendations help users understand what grows well
5. Measurements can be saved to specific plants
6. Full bilingual support (EN/IT)
7. Feature works offline (no API calls)
8. User tested and approved functionality
</success_criteria>

<output>
After completion, create `.planning/phases/13-light-meter/13-03-SUMMARY.md`
</output>
